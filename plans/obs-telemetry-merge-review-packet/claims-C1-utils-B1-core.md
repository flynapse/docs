# Claims packet — Phase C1 (utils) and Phase B1 (core)

Assembled 2026-09-20 from `docs/plans/observability-telemetry-merge-and-completion.md` §2.3a, §5.5/§5.6, §6,
§7 and the SDD ledger `.superpowers/sdd/observability-telemetry-merge-and-completion/progress.md`.
Every `file:line` below was resolved in the named worktree on the day of assembly. Nothing was committed,
edited or run against either tree.

| phase | repo | worktree | branch | range | merge commit | review-triage commit |
|---|---|---|---|---|---|---|
| C1 | utils | `/home/aditya/Code/utils-obsm` | `obs-merge` | `289ba71..e6b464e` | `9c5a8d7` | `e6b464e` |
| B1 | core | `/home/aditya/Code/core-obsm` | `obs-merge` | `8b7dfad..d7f7b54` | `5d40d70` | `d7f7b54` |

Test lanes, as recorded (shared `api` Poetry env, `DEBUG=false`, `POSTGRES_DB=copilot_mro_test`,
`PYTHONPATH` pinned to the merged worktree):

| phase | pre-merge baseline (ledger A.4) | at merge commit | at triage commit |
|---|---|---|---|
| C1 utils | 1190 passed / 0 skipped | 1203 / 0 (`9c5a8d7` message) | **1209 / 0** (`e6b464e`; plan §7 and ledger both cite 1209) |
| B1 core | 2829 passed / 2 skipped / 2 xfailed | 2866 / 2 / 2 (`5d40d70`; plan §7 cites this figure) | **2879 / 2 / 2** (`d7f7b54`; ledger lane table cites this figure) |

Neither range contains a **tier 0** claim: every mutation-proved guard here protects a decision that is
itself consequential (a signal attribute, a privacy property, a defect fix), so §2.3a's tier-0 category
("mechanical resolutions, additive files, renames, test-only and doc changes") does not apply to any row.

---

## Phase C1 — utils, `289ba71..e6b464e`

| # | Repo | File:line | Decision taken | Why | Evidence it is right | Guard test | Mutation-proved? | Tier | Chunk | Claim state |
|---|---|---|---|---|---|---|---|---|---|---|
| C1-1 | utils | `utils/s3_service.py:227` | Take their `s3.download` CLIENT span; freeze its name and attribute set (`rpc.system`/`rpc.service`/`rpc.method`/`operation.outcome`/`storage.downloaded_bytes`) | The span name and dimensions are what the collector's span-metrics and the dependency board key on | Success test asserts the attribute dict **exactly**, so an added or renamed attribute fails | `tests/unit/observability/test_nonagent_storage_client_spans.py::test_s3_download_emits_a_content_free_client_child_span` (:66) | not recorded | 2 | F1 | ASSERTED |
| C1-2 | utils | `utils/weaviate_service.py:1012` | Take their `weaviate.hybrid_search` CLIENT span; the public method becomes a wrapper over a renamed `_hybrid_search` (`:1042`) | Same contract argument; wrapping preserves the existing API | Exact-dict assertion incl. `db.system`, `db.operation`, `tenant.id`, `search.mode/requested_count/result_count` | `…::test_weaviate_hybrid_search_emits_a_content_free_client_child_span` (:162) | not recorded | 2 | F1 | ASSERTED |
| C1-3 | utils | `utils/s3_service.py:223` and `utils/weaviate_service.py:1005` | Add `server.address` to both spans — host only, never the endpoint URL, dropped when empty rather than exported as a placeholder | The collector promotes it as a span-metrics dimension; without it S3 sat in the empty-label bucket of "Client call rate by dependency" and was excluded from "DB client p95 by system" | Plan §5.6 P1 names the two CATALOGUE panels; the empty case is pinned separately so no placeholder series is created | `…::test_s3_span_carries_the_endpoint_host_for_the_dependency_board` (:298), `…::test_s3_span_omits_the_endpoint_when_there_is_none` (:314), and the exact-dict Weaviate test (:179) | **yes** — plan §7 C1: "`server.address` on each span" is 2 of the 5 named mutations, "each mutation fails exactly one test" | 2 | F1 | SETTLED |
| C1-4 | utils | `utils/s3_service.py:230`, `utils/weaviate_service.py:1015` | Keep `record_exception=False` / `set_status_on_exception=False` on both spans, and pin them | The review flipped `record_exception` to `True` and **all 1203 tests passed** while exporting `exception.message` and a `Type: message` stacktrace to Tempo — R22's shape relocated to the one pipe nothing inspected | The pre-fix flip and its result are recorded in plan §7 C1, the ledger and the `e6b464e` message | `_assert_no_exception_event` (:55), called by `…::test_s3_missing_object_is_an_explicit_returned_failure` (:117) and `…::test_weaviate_hybrid_search_marks_and_reraises_failure` (:205) | partially recorded — the **pre-fix** flip is recorded (it passed, i.e. proved the guard absent); no post-fix mutation is itemised. Ledger's blanket "Both fixed and mutation-proven" is the only claim | 2 | F1 | ASSERTED |
| C1-5 | utils | `utils/weaviate_service.py:268` | Replace `traceback.format_exc()` on the Weaviate connect-failure log with `failure_fields(exc)` | A rendered traceback's last line is `Type: message`, and a Weaviate connect failure's message quotes the endpoint | Guard drives a real connect failure whose message carries a URL and a key, then asserts it is absent from **both** pipes | `…::test_weaviate_connect_failure_log_carries_frames_not_the_exception_text` (:322) | **yes** — plan §7 C1 names "connect-failure log" as 1 of the 5 mutations | 2 | F1 | SETTLED |
| C1-6 | utils | `utils/s3_service.py:273`, `:283`; `utils/weaviate_service.py:1097`, `:1195` | C1.1: route the four hand-rolled `error_type=type(error).__name__` log kwargs through `failure_fields(error)` (type **and** frames, never the message); keep semconv `error.type` on the spans | R22 policy. The review proved C1.1 **inert**: reverting to the bare kwarg passed the whole lane, because the estate's two R22 AST guards sweep fixed module lists containing neither `s3_service` nor `weaviate_service`, and P0-GUARD's sweep is assigned to copilot-mro (D.8) | Fixed by asserting `stack` is present at both sites. The Weaviate site had **no** R22 test at all — a full f-string leak passed because the only assertions covered the query and the collection name, neither of which that path logs | `…::test_s3_failure_log_carries_frames_not_the_exception_text` (:240), `…::test_weaviate_failure_log_carries_frames_not_the_exception_text` (:274) | partially recorded — the **pre-fix** inertness is recorded; these two sites are not among the 5 named post-fix mutations | 2 | F1 | ASSERTED |
| C1-7 | utils | `tests/unit/observability/test_nonagent_storage_client_spans.py:35` | Widen the privacy helper `_all_exported_text` from log records only to log records **+ span attributes + span events** | Reading only the log pipe was the blind spot that let C1-4 ship; span attributes and events export exactly as logs do | Every privacy assertion in the file now reads through it | the helper itself; no self-test that the helper covers both pipes | n/a (test helper) | 2 | F1 | ASSERTED |
| C1-8 | utils | `utils/s3_service.py:28`, `:40`, `:275` | C1.3: a `quiet=True` 404 becomes new outcome `miss` with status OK; the loud miss stays `not_found` + ERROR | Sole caller `ad_parser._download_from_s3` treats a 404 as expected cache-fallback flow, so ERROR made the dependency board's S3 error rate a function of cache-hit rate. Premise re-verified across all ten repos | Their `test_s3_missing_object_…` passed `quiet=True` while asserting ERROR; it was retargeted to the loud path, which is what its name claims | `…::test_s3_quiet_cache_miss_is_not_an_error_span` (:221) — asserts outcome `miss`, status not ERROR, and `error.type` absent | not recorded | 2 | F1 | ASSERTED |
| C1-9 | utils | `utils/s3_service.py:273` (change **rejected**) | Reject the review's P2-7: do **not** add the bucket name to the S3 failure log lines | Their own privacy test asserts the bucket is absent from every export — a ruling this phase does not get to overturn on its own; the trace id is already on every record, which is what makes the line actionable | Recorded in plan §7 C1 as "Recorded, not applied" | the rejection is enforced by the test it cites: `…::test_s3_missing_object_is_an_explicit_returned_failure` (:120) asserts `"private-bucket" not in exported` | not recorded | 2 | F1 | ASSERTED |
| C1-10 | utils | `utils/s3_service.py:31-45` | C1.2: drop the two fabricated `RuntimeError`s built only so the span helper could read a type name; `_finish_s3_span` now decides status from the **outcome**, not from whether an exception object exists | Neither was ever raised and both reported the same useless name; a failure the code detects itself (no client, empty file) must still be an ERROR span with no fake type | Self-detected failure still yields ERROR + `operation.outcome="unavailable"` | `…::test_s3_unavailable_client_is_an_explicit_returned_failure` (:123) pins ERROR + outcome, but does **not** assert that `error.type` is absent on that path | not recorded | 1 | F2 | ASSERTED |
| C1-11 | utils | `utils/weaviate_service.py:27-44`, `:235`; `utils/config.py:176` | C1.5 second clause: `grpc_secure` follows the URL scheme via `urlsplit` (not `startswith`), with a `WEAVIATE_GRPC_SECURE` override | A hard-coded `False` silently downgrades every https deployment to a plaintext gRPC dial that then fails init for reasons never naming TLS; the scheme heuristic is wrong for exactly one topology (proxy terminating TLS in front of a plaintext gRPC backend) | Four tests cover https, plain http, the override, and a leading-space/uppercase scheme | `tests/unit/weaviate/test_weaviate_lazy_connect.py::test_grpc_secure_follows_the_url_scheme` (:89), `::test_grpc_secure_stays_off_for_plain_http` (:105), `::test_grpc_secure_override_wins_over_the_url_scheme` (:116), `::test_grpc_secure_ignores_leading_space_and_case` (:130) | **yes** — 2 of the 5 named mutations (`WEAVIATE_GRPC_SECURE` override; `urlsplit` vs `startswith`) | 1 | F2 | SETTLED |
| C1-12 | utils | `utils/config.py:172`, `utils/weaviate_service.py:234`, `.env.sample:64`, `README.md:203` | Take their `WEAVIATE_GRPC_PORT` setting (default 50051, back-compatible) | Back-compatible, and it is the knob C1.5 needs | Default and override both pinned at the connection boundary | `tests/unit/weaviate/test_weaviate_lazy_connect.py::test_connection_uses_default_grpc_port` (:63), `::test_connection_uses_configured_grpc_port` (:76) | not recorded | 1 | F2 | ASSERTED |
| C1-13 | utils | `tests/unit/observability/test_nonagent_storage_client_spans.py:260` | C1.4: add the missing no-op-tracer guard for the Weaviate wrapper (S3 already had one at :131) | Telemetry-off transparency. RV4 catalogued five "disabled OTel is transparent" tests on their branch that all passed with the telemetry deleted, so the category is known-weak | The test drives the real wrapper under a `NoOpTracerProvider` and asserts the result still comes back | `…::test_weaviate_hybrid_search_keeps_working_with_a_noop_tracer` (:260) | not recorded | 1 | F2 | ASSERTED |
| C1-14 | utils | `utils/weaviate_service.py:222-230` (docstring) | C1.6: **record, do not fix** — the client span belongs on the connection factory, not only on `hybrid_search` | The spec deliberately put the wrapper on `weaviate_connection()`; `bm25_search`, `vector_search`, `query_objects`, `get_object_by_id`, `list_all_objects` and the delete/schema paths are untraced doors into the same dependency, so the board's Weaviate row measures one query shape out of many | Docstring names the six methods; work assigned to plan G.10 | **none** | not recorded | 1 | F3 | OPEN |
| C1-15 | utils | `.env.sample:64` / `README.md:203` are the only sites — first clause of C1.5 is **NOT done** | Owed: mirror `WEAVIATE_GRPC_PORT` into the nine sites outside utils (`copilot-mro/.env.sample`, `copilot-mro/copilot_mro/app/.env.example`, four copilot-mro compose files, `deployment/weaviate-local/weaviate-docker-compose.yml`, `api/.env.example`, `iac/apprunner.tf:41`, `iac/lambda.tf:108`) | Every current `WEAVIATE_URL` in the estate is `http://`, so nothing is broken today; the gap is that an operator has no declared knob | Re-verified at assembly: `WEAVIATE_GRPC_PORT` matches in **`utils-obsm` only** (4 files); **0 hits** in `copilot-mro-obsm`, `api-obsm`, `core-obsm`, `dashboard-obsm`, `iac`. Ledger records the same 0-hit sweep twice | **none** | not recorded | 1 | F3 | OPEN |
| C1-16 | utils | `utils/s3_service.py:28` | Deferred (plan §6): `_S3_NON_ERROR_OUTCOMES` is a hard-coded frozenset and the five `operation.outcome` values are bare literals at the call sites | A new *failure* outcome defaults correctly to ERROR, but a new *non-failure* outcome (`cached`, `not_modified`) is wrong until someone remembers the set. Deferred because the complete fix is plan F.4's vocabulary reconciliation — `operation.outcome` collides with the spec's `agent.outcome`/`tool.outcome`, and this merge shipped five more values into that unreconciled namespace | plan §6 | **none** | not recorded | 2 | F3 | OPEN |
| C1-17 | utils | `utils/s3_service.py:227`, `utils/weaviate_service.py:1012` | Deferred (plan §6): a `BaseException` escapes both spans as **UNSET** with no `operation.outcome` at all | Both spans set `set_status_on_exception=False` and both bodies catch `Exception`, so `KeyboardInterrupt`/`SystemExit`/`CancelledError` ends the span neither OK nor ERROR — invisible to every outcome query and non-error on the board. Complete fix is a `finally` stamping `interrupted`, applied to every wrapper of this shape (Task R / G.1) | plan §6 | **none** | not recorded | 1 | F3 | OPEN |

**C1 counts — 17 rows.** Tier: 0 = 0, 1 = 7, 2 = 10. Chunk: F1 = 9, F2 = 4, F3 = 4.
Claim state: SETTLED = 3 (C1-3, C1-5, C1-11), ASSERTED = 10, OPEN = 4 (C1-14, C1-15, C1-16, C1-17).

---

## Phase B1 — core, `8b7dfad..d7f7b54`

Range note: `8b7dfad` is **their** tip, so this range also contains our own pre-merge commits
(`ed22e1f`…`e10a9ce`: PA8 optimizer panels, the C9 499 work, the department-delete cascade). Rows below are
restricted to what the merge `5d40d70` and the triage `d7f7b54` decided.

| # | Repo | File:line | Decision taken | Why | Evidence it is right | Guard test | Mutation-proved? | Tier | Chunk | Claim state |
|---|---|---|---|---|---|---|---|---|---|---|
| B1-1 | core | `core/db/table_definitions.py:108-112` | **M-CAPTURE**: flip `tenants.llm_content_capture_enabled` from their opt-IN `boolean DEFAULT false` to opt-OUT `boolean DEFAULT true` | Spec ruling 4 / owner ruling M-CAPTURE is capture ON by default with a tenant opt-OUT. The plan had assigned M-CAPTURE only to D.4 (copilot-mro's policy reader), so left as shipped the merge would have flipped the reader to honour a column whose every row said `false` — capture off estate-wide, with **both repos individually correct** | Verified in `copilot_mro_test` by query after the all-registry migration: `llm_content_capture_enabled DEFAULT true` | `tests/unit/db/test_tenant_llm_content_capture_policy.py::test_tenant_content_capture_policy_defaults_on_in_the_database` (:7) — asserts both the field type and the literal in the generated SQL | not recorded (triage records "six mutations, six caught" without naming them) | 2 | F1 | ASSERTED |
| B1-2 | core | `core/resources/tenants/models.py` (via the guard) | Keep the column out of `TenantUpdate` **and** `TenantResponse`, in both directions | The tenant's decision arrives through the Appendix A contract clause and an operator clears the flag; a self-service toggle would leave the retention posture and the signed contract free to disagree | Their branch already pinned exclusion; the merge kept it and restated the reason under the flipped default | `tests/unit/db/test_tenant_llm_content_capture_policy.py::test_tenant_content_capture_policy_is_not_customer_self_service` (:24) | not recorded | 2 | F1 | ASSERTED |
| B1-3 | core | `scripts/set_tenant_content_capture.py:1-133` (switch group at `:109`) | Give the opt-out a **write path**: owner connection, `--database` required and never inferred, protected names refused without `--i-understand-this-is-live`, `--dry-run`, a mutually exclusive and **required** `--enable/--disable`, non-zero exit on an unknown tenant | The review's headline: the flip changed the consequence of a **missing** mechanism from harmless to harmful. Nothing in core, api, copilot-mro, utils or dashboard writes the column (`update_tenant` is allowlist-driven; `create_tenant` names its columns). Under `DEFAULT false` that absence was fail-CLOSED; under `DEFAULT true` it is fail-OPEN — every tenant recorded as consenting and no path able to record a refusal | Shaped on the `purge_product_events.py` precedent already in the repo; §4c still lists the Appendix A contract clause as owed | `tests/unit/analytics/test_tenant_content_capture_script.py` (5 tests, incl. `::test_the_switch_is_required_so_a_run_cannot_default_to_consent` :46, `::test_a_protected_database_refuses_without_the_explicit_flag` :52) + `tests/db/analytics/test_tenant_content_capture_script_db.py` (4 tests, incl. `::test_the_opt_out_is_performable_and_persists` :60) | partially recorded — covered only by the triage's unnamed "six mutations, six caught" | 2 | F1 | ASSERTED |
| B1-4 | core | `core/db/table_definitions.py:1516-1546`, registered at `:1717`/`:1752` | Take their server-owned `dashboard_profiles` table: tenant-class, PK `(tenant_id, profile_id)`, four CHECK constraints (`default_only`, `version >= 1`, `panel_ids` is a JSON array, `presentation` is a JSON object) | A server-owned presentation allow-list the resolver intersects with the registry, the feature gates and the caller's `PanelRequirements` — it never grants access | DB state verified by query after migration: table present with RLS **enabled and forced** and a `dashboard_profiles_isolation` policy | `tests/unit/db/test_dashboard_profiles_definition.py::test_registry_emits_tenant_rls_policy_and_forces_rls` (:55); `tests/db/analytics/test_dashboard_profiles_db.py::test_one_tenant_cannot_read_another_tenants_profile` (:102) | not recorded — and these guards came from **their** branch untouched, so §2.3a's 35-inert-test warning applies to them unverified | 2 | F1 | ASSERTED |
| B1-5 | core | `core/resources/analytics/analytics_endpoints.py:81` | B1.2: add the tenant-admin gate to `GET /analytics/dashboard-profile` | It was the only GET on that router without one; without it the route answered 200 with an empty tab list, which still discloses that a profile exists and its version | Triage corrected the *justification*: this is the gate `/chat-quality` carries, **not** a router-wide rule — `POST /analytics/events` deliberately has none because every user emits product events, and a reader applying the old sentence literally would kill telemetry for non-admins | `tests/api/analytics/test_dashboard_profile_endpoint.py::test_profile_refuses_a_caller_without_dashboard_access` (:169) — patches the loader to throw, so a gate that ran *after* the read would fail | not recorded | 2 | F1 | ASSERTED |
| B1-6 | core | `core/resources/analytics/events_store.py:93` | `schema_version` is stamped by the **server**, not taken from the client; the field is still accepted on the wire | `extra="forbid"` plus the per-event attribute allow-list mean every stored row matched *this* model, so a client's `schema_version: 7` is an unfalsifiable claim written into a NOT NULL column consumers read to decide how to parse `attributes`. Contrast `event_id`, which stays the client's because its whole purpose is browser-retry idempotency | Triage added the honest caveat at `:85-92`: it records the **validator's** version, so a v2 process stamps 2 onto a v1 browser's batch during a rolling upgrade; nothing reads the column today (swept across six repos) — it is write-only | `tests/unit/analytics/test_product_events_schema.py::test_schema_version_is_the_servers_claim_not_the_clients` (:165) | not recorded | 2 | F1 | ASSERTED |
| B1-7 | core | `core/resources/analytics/events_store.py:154`; PK at `core/db/table_definitions.py:1447` (`primary_key=["event_id"]`, with `tenant_id` injected first by the tenancy builder) | Take their product-event idempotency: `ON CONFLICT (tenant_id, event_id) DO NOTHING RETURNING`, `ProductEventInsertResult`, and `duplicates` on the 202 body | Verified rather than trusted: the PK really resolves to `(tenant_id, event_id)` so the conflict target is real and a replayed id cannot collide across tenants; `MAX_EVENTS_PER_BATCH=50` keeps the widened INSERT at ≤750 bound parameters, clear of the 65535 wire limit | `utils/table_builder.py::_lead_with` prepends the injected tenancy column, so the emitted DDL is `PRIMARY KEY (tenant_id, event_id)` | `tests/unit/db/test_product_events_definition.py::test_the_primary_key_is_the_tenant_then_the_event_id` (:46); `tests/db/analytics/test_product_events_store_db.py::test_replaying_an_event_is_a_duplicate_and_does_not_add_a_row` (:105), `::test_the_same_event_id_is_independent_in_two_tenants` (:149); plus the estate scan `tests/unit/db/test_tenanted_write_paths.py::test_on_conflict_targets_lead_with_the_tenancy_columns` (:59) | not recorded | 2 | F1 | ASSERTED |
| B1-8 | core | `core/resources/analytics/events_store.py:164-172` | Keep the `Counter` back-matching (`accepted_keys[key] -= 1`) and pin it | The review deleted the decrement and **all 256 analytics tests passed** — no test ever put the same `event_id` twice in one batch, the only case the Counter exists for, so a plain `set` would have passed too. Without it the handler answers `accepted: 2, duplicates: 0` and emits two ops-log lines for one stored row: permanent double-counting in every log-derived metric | Confirmed against a real Postgres that `ON CONFLICT DO NOTHING` **skips** the intra-statement self-conflict (rather than raising as `DO UPDATE` would), so `RETURNING` yields one key for two input rows | `tests/db/analytics/test_product_events_store_db.py::test_one_batch_carrying_the_same_event_id_twice_accepts_it_once` (:119) | **yes** — `d7f7b54` records the specific mutation (delete `accepted_keys[key] -= 1`) and its result ("Now pinned by a db test; the mutation fails it") | 1 | F2 | SETTLED |
| B1-9 | core | `core/resources/analytics/events_endpoints.py:138` | A deduplicated (dropped) event now logs **WARNING**; the clean path stays DEBUG | `duplicates` was unobservable end to end: DEBUG is below the deployed INFO floor and the 202 body is discarded by the browser client after the status check, so a client minting colliding ids lost events silently on both sides of the wire | Guard captures at INFO, i.e. at the deployed floor, not at DEBUG | `tests/api/analytics/test_events_endpoint.py::test_a_dropped_event_is_visible_at_the_deployed_log_floor` (:214) | not recorded (inside the unnamed "six") | 1 | F2 | ASSERTED |
| B1-10 | core | `core/resources/analytics/events_endpoints.py:159`, `:169` | **P0-499 needed no action, and that is the result.** Keep our `ClientDisconnect` → 499 path; their `duplicates` change was grafted inside the same `try` by the clean auto-merge | §5.5 flagged that their `events_endpoints.py` predates our 499 work and edits the same `try` block | Confirmed by **reading the merged handler**, explicitly not by trusting the absence of a conflict | `tests/api/analytics/test_events_client_abort.py::test_a_client_gone_before_the_body_is_read_gets_499_and_one_info_line` (:163); sibling lane `tests/api/logging/test_ingest_client_abort.py::test_a_client_gone_before_the_body_is_read_gets_499_and_one_info_line` (:153) | not recorded | 1 | F2 | ASSERTED |
| B1-11 | core | `tests/api/analytics/test_events_client_abort.py:66` | **P0-ABORT**, both causes: an `_InsertSink` subclass returns a real `ProductEventInsertResult` (the `None` double made the merged handler 500 on `result.accepted`), and the 202 assertion now carries `duplicates` | §5.5: our abort test failed on merge for two independent reasons. The subclass keeps the double that *must* return a result distinct from the one that must not | Both the happy path and the 500 funnel still pass through the same harness | `…::test_a_complete_batch_is_still_accepted_through_the_same_harness` (:186), `…::test_a_store_failure_still_takes_the_500_funnel` (:201) | not recorded | 1 | F2 | ASSERTED |
| B1-12 | core | `core/resources/analytics/dashboard_profiles.py:99-117` | B1.3: `_coerce_panel_ids` returns `None` for a non-list (corruption → warn → fall back to the offer) and `()` for an empty list (a legitimate "offer nothing") | Collapsing both to `()` is what made two failures silent **and opposite**: "no row" widened to every panel, a corrupt row narrowed to none | Triage added honesty rather than more code: `CHECK (jsonb_typeof(panel_ids) = 'array')` makes the corrupt branch unreachable through any normal write, and its test drives it by patching `fetch_one` — so it defends the Python contract, not a shape the DB can produce. Widening is an offer, never a grant: the resolver still removes every unauthorized and feature-gated panel | `tests/unit/analytics/test_dashboard_profiles.py::test_a_corrupt_panel_ids_column_falls_back_to_the_offer_not_to_a_blank_dashboard` (:206), `::test_an_empty_panel_ids_list_is_honoured_as_offer_nothing` (:226), `::test_absent_row_widens_to_the_full_registry` (:157), `::test_profile_filtering_does_not_bypass_direct_panel_authorization` (:91) | not recorded | 1 | F2 | ASSERTED |
| B1-13 | core | `core/resources/analytics/dashboard_profiles.py:134-155` | `load_dashboard_profile` distinguishes "no profile" (DEBUG) from "no tenancy binding" (WARNING), reading `current_db_tenancy()` | Under FORCE RLS an unbound read sees zero rows for a tenant that may well have a profile, then widens to the whole registry — a caller bug, not a missing profile, and it was invisible at the deployed floor while the *unreachable* corrupt-row branch got WARNING | `tests/unit/analytics/test_dashboard_profiles.py` covers both directions, including the quiet case so the WARNING is not unconditional | `::test_an_unbound_read_is_loud_because_rls_makes_it_look_like_no_profile` (:167), `::test_an_absent_profile_under_a_correct_binding_stays_quiet` (:190) | not recorded | 2 | F1 | ASSERTED |
| B1-14 | core | `core/resources/analytics/registry.py:154`; readers at `dashboard_profiles.py:81` and `panel_service.py:118` | B1.4: extract the improvement-tab feature gate to `registry.feature_enabled` | The profile resolver and the panel service each carried a copy of one rule — exactly how a profile comes to offer a panel the panel endpoint refuses. Deliberately kept separate from `authorize_panel`: this is a deployment switch, not a permission | Both readers now import the single function; the endpoint still layers `improvement_hidden` on top so the UI can say "under review" rather than "no access" | `tests/unit/analytics/test_dashboard_profiles.py::test_owner_only_and_feature_gated_panels_stay_sensitive` (:105); registry suite `tests/unit/analytics/test_analytics_registry.py` | not recorded | 1 | F2 | ASSERTED |
| B1-15 | core | `core/resources/analytics/dashboard_profiles.py:169`, `:173` | Deferred (plan §6): `int(row.get("profile_version") or DEFAULT_PROFILE_VERSION)` swallows a stored `0` | It becomes `1` instead of raising through `DashboardProfileConfig.__post_init__`'s `version < 1` guard, so the dataclass's own validation can never fire on the database path. Unreachable today (`dashboard_profiles_version_check CHECK (profile_version >= 1)`). Complete fix: stop using `or` for a value whose falsy case is meaningful, and let the dataclass be the single validator | plan §6 | **none** | not recorded | 1 | F3 | OPEN |
| B1-16 | core | `core/db/table_definitions.py:1447` (PK) with `core/resources/analytics/events_endpoints.py` (no capability gate) | Deferred (plan §6): the idempotency key is **tenant-scoped, not user-scoped** | Any authenticated member writes into a tenant-wide id namespace, so a member who predicts another member's `event_id` suppresses that event permanently and silently — no row, no log line, `202 {"duplicates": 1}`. Not fixed now: the mint is 122-bit `crypto.randomUUID()` and the fix is a PK migration. Complete solution `(tenant_id, user_id, event_id)` plus a conflict-target guard. **Sequence with B2.1**, which fixes that same mint throwing on non-secure-context origins | plan §6 | **none** for the *user-scoping*. (Note: plan §6 states "core has no conflict-target test" — `tests/unit/db/test_tenanted_write_paths.py::test_on_conflict_targets_lead_with_the_tenancy_columns` does exist and rglob-scans all of `core`; it pins the tenancy **prefix**, not the full key) | not recorded | 2 | F3 | OPEN |
| B1-17 | core | `core/resources/analytics/events_store.py:83` (`event.event_id or uuid4()`) | Deferred (plan §6): a client that omits `event_id` gets no idempotency **and no signal** | `rows_for` mints a fresh server-side id per attempt, so a batch retried after a network timeout double-inserts, `duplicates` reads 0 and the 202 says everything was accepted. The compatibility window has no expiry, no metric and no test. Complete fix: require `event_id` once the dashboard client is known to send it (it does, on their branch), and count omissions until then | plan §6 | **none** | not recorded | 1 | F3 | OPEN |
| B1-18 | core | `core/db/table_definitions.py:1519` (`dashboard_profiles`, declared FK-free) | Deferred (plan §6): FK-free tenant-classed relations survive tenant teardown | `delete_tenant` relies entirely on FK cascade, and `dashboard_profiles` is FK-free deliberately, matching `product_events`, which has the same hole and is swept only by a retention purge. Pre-existing estate contract question extended to one more relation, not a defect this merge introduced. Complete fix: a declared teardown contract — an explicit delete list derived from the registry and asserted against it (estate-wide, do with Task R / G.1) | plan §6 | **none, and by construction there cannot be one today**: `test_exactly_the_eight_identity_relations_cascade_with_a_tenant` asserts the set of relations *referencing* `tenants`, so a relation with no FK is outside its scope | not recorded | 2 | F3 | OPEN |
| B1-19 | core | `core/resources/analytics/analytics_endpoints.py:76-80` (comment) and `core/resources/analytics/events_store.py:83-92` (comment) | Two review findings answered by correcting **comments**, not code | (a) "the same coarse gate every other route on this router carries" was wrong and dangerous read literally — `POST /analytics/events` deliberately has no gate. (b) `schema_version` is not *misread*, it is write-only, and what it records is the validator's version | Triage commit `d7f7b54` states both, and the code was deliberately left unchanged | **none** (comments carry no guard) | n/a | 1 | F3 | OPEN |

**B1 counts — 19 rows.** Tier: 0 = 0, 1 = 9, 2 = 10. Chunk: F1 = 8, F2 = 6, F3 = 5.
Claim state: SETTLED = 1 (B1-8), ASSERTED = 13, OPEN = 5 (B1-15, B1-16, B1-17, B1-18, B1-19).

---

## Open claims, tier 2 first

### Tier 2, OPEN — no guard, judgment only

| id | Repo | Claim | What an auditor has to settle |
|---|---|---|---|
| **B1-16** | core | The product-event idempotency key is tenant-scoped, not user-scoped: any authenticated member can permanently and silently suppress another member's event by predicting its `event_id` | Is a 122-bit `crypto.randomUUID()` mint an acceptable mitigation while B2.1 is still un-merged, given B2.1 exists *because that mint throws* on non-secure-context origins and may acquire a lower-entropy fallback? |
| **B1-18** | core | FK-free tenant-classed relations (`dashboard_profiles`, and already `product_events`) survive tenant teardown; the existing cascade test cannot see them by construction | Is extending a known estate hole to one more relation acceptable inside a merge, or does the teardown contract belong before `dashboard_profiles` ships? |
| **C1-16** | utils | This merge shipped five `operation.outcome` literals into a namespace that collides with the spec's `agent.outcome` / `tool.outcome`, with the non-error set hard-coded next to them | Does `operation.outcome` stay, get renamed, or fold into the spec vocabulary at F.4 — and is deferring that while the values are already being written the right call? |

### Tier 1, OPEN

| id | Repo | Claim |
|---|---|---|
| **C1-14** | utils | The Weaviate client span sits on `hybrid_search` only; six query methods plus the delete and schema paths are untraced, so the dependency board's Weaviate row measures one query shape out of many. Recorded in a docstring, assigned G.10. |
| **C1-15** | utils | C1.5's first clause is undone: `WEAVIATE_GRPC_PORT` exists in `utils-obsm` only. Re-verified at assembly — 0 hits in `copilot-mro-obsm`, `api-obsm`, `core-obsm`, `dashboard-obsm` and `iac`. Nine named sites owed to D/E, C2 and the two `iac/*.tf` files. |
| **C1-17** | utils | A `BaseException` unwinding through `download_pdf` or `hybrid_search` ends the span UNSET with no `operation.outcome` — invisible to every outcome query and non-error on the board. |
| **B1-15** | core | `int(row.get("profile_version") or DEFAULT_PROFILE_VERSION)` swallows a stored `0`, so `DashboardProfileConfig`'s own `version < 1` validation can never fire on the database path. |
| **B1-17** | core | A client that omits `event_id` gets no idempotency and no signal; the compatibility window has no expiry, metric or test. |
| **B1-19** | core | Two review findings were closed by correcting comments rather than code; nothing guards the corrected statements. |

### ASSERTED claims whose guard came from the colleague's branch and was never mutation-checked here

§2.3a's amended rule says a guard earns tier 0 only once it has been shown to fail when the property is
removed, and RV4 catalogued 35 inert tests on that branch. These rows inherit guards from it:

- **B1-4** — `tests/unit/db/test_dashboard_profiles_definition.py` and `tests/db/analytics/test_dashboard_profiles_db.py` (RLS enabled/forced, isolation policy, cross-tenant read). Corroborated independently by a live DB query recorded in plan §7 and the ledger, but not mutation-checked.
- **C1-1 / C1-2** — the span-shape guards originate from their `395a3b5`; they were extended (privacy helper, `server.address`) but the span-name and attribute assertions themselves were not mutated.
