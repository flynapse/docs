# Claims packet — Phase B2 (dashboard)

Assembled 2026-09-20 from `docs/plans/observability-telemetry-merge-and-completion.md` §3 (B2.1–B2.4),
§2.2a, §4b, §5.5, §6 and §7 ("Phase B2 — dashboard"), plus the five commit bodies. This is the **missing
ninth slice**: B2 closed with two independent adversarial reviewers but never emitted the claims table
§2.3a makes a phase-exit gate. Every `file:line` below was resolved in the worktree on the day of
assembly. Nothing was committed, edited or run against the tree.

| phase | repo | worktree | branch | range | merge commit | review-response commit |
|---|---|---|---|---|---|---|
| B2 | dashboard | `/home/aditya/Code/dashboard-obsm` | `obs-merge` | `4a2898b..3afd524` | `42b3380` | `3afd524` |

Five commits: `42b3380` (the merge — **no resolution at all**, see B2-1), `acd8b78` (B2.1), `9002ed9`
(B2.2 / M-FALLBACK), `cc17cf4` (B2.3), `3afd524` (review triage). The branch head is `5011f4f` (the
browser-signal contract) — **later work, excluded from this slice**; it touches only
`contracts/browser-signals.json`, `scripts/generate-browser-signal-contract.mts` and two test files,
none of which B2 wrote.

Test lane, as recorded in §7:

| lane | pre-merge `4a2898b` | at `3afd524` | confirmed at assembly? |
|---|---|---|---|
| `typecheck` | rc=0 | rc=0, output **byte-identical at every step** | **no** — not re-run |
| `next lint` | — | **byte-identical** to pre-merge | **no** — not re-run |
| unit | **2475 passed** | **2502 passed, 0 failed, 0 skipped** | **no** — not re-run (≈15 min; another agent may hold the toolchain) |
| `next build` | never run | never run; the merged standalone guard refused the attempt | **partly** — the guard and its worktree refusal exist (`scripts/standalone-guard.mjs:4-13`); that it fired is not recoverable from the tree |

**The +27 delta WAS re-derived, read-only, and it reconciles exactly.** Static `test(` declarations across
the eight changed test files go **33 → 58 (+25)**; the profile page's one `test()` inside a
`for…of PROFILE_FAILURES` loop (`tests/unit/analytics/analytics-dashboard-profile-page.test.tsx:328`)
contributes **3** runtime cases for **1** declaration, i.e. **+2** more — 25 + 2 = **27**, which is
2502 − 2475. The §7 loss proof ("no test name removed anywhere") also holds **as stated**: for each of
the three test files ours already had, the set of test names at `4a2898b` minus the set at `3afd524` is
**empty**. Its scope limitation is its own row (B2-8) — it is a diff against **ours**, not against the
merge, and exactly one test the merge contributed was removed.

**Tier 0 is empty here too** (ninth file running). §2.3a grants tier 0 only to work that is mechanical,
additive or doc-only **and** mutation-proved. Every mutation-proved guard in this range protects a
consequential decision — a tenant-wide id namespace, a degraded-read widening, a content-prose leak — so
none qualifies; the additive and doc-only rows (the docstring correction, the plan corrections) have no
guard at all.

**The 13 mutation proofs reconcile exactly** — see the assembly notes for the itemised reconciliation.

---

## Phase B2 — dashboard, `4a2898b..3afd524`

| # | File:line | Decision taken | Why | Evidence it is right | Guard test | Mutation-proved? | Tier | Chunk | Claim state |
|---|---|---|---|---|---|---|---|---|---|
| B2-1 | merge commit `42b3380` (parents `4a2898b` ours, `0f1715a` theirs) | Merge `origin/obs-telemetry-merge` into `obs-merge` and carry **no resolution**, because git raised none. Every defect the merge brings in is fixed in a later commit, never in the merge | §2.3's doctrine, and §2.2a's whole premise: "a merge with no raised conflicts" is precisely the condition the silent-merge register exists for | **Re-derived at assembly, not trusted.** `git merge-tree --write-tree 4a2898b 0f1715a` exits 0, emits a bare tree oid and no conflict block; that oid is `514769dac5ace9810f812401f67306e9561be74d` and `git rev-parse 42b3380^{tree}` is **the same oid** — the merge commit is byte-for-byte the unresolved auto-merge. The file sets also reconcile with §2.2a exactly: theirs touched **15** paths against merge-base `b87ced0`, of which **4** were also touched by ours (`analytics-panel-registry.ts`, `analytics-api.ts`, `product-events.ts`, `analytics-panel-registry.test.ts`) and **11** were theirs-only | none — a merge topology is not a testable property | not recorded | 1 | F2 | OPEN |
| B2-2 | `lib/telemetry/product-events.ts` (both-sides) — merged `enqueueProductEvent` at `42b3380:228-243` | **The register's justification, made concrete.** Ours added the E9.9b never-throw `try/catch` around `parseProductEvent`; theirs added the `event_id` mint at the `state.queue.push(...)` line. Git merged both with no conflict and placed the mint **outside** the catch. B2.1 reverses that | This is the only both-sides file where the two sides' changes interact semantically, and the interaction is invisible in the diff: ours moved a boundary, theirs added a call, and neither edited the other's line | Re-verified at assembly against `git show 42b3380:lib/telemetry/product-events.ts`: the `try` closes at `:234`, `getToken()` is checked at `:235-238`, and `globalThis.crypto.randomUUID()` sits at **`:241`** — after the boundary. Ours' own diff shows the boundary was new on our side (`if (isDevelopment()) throw error;` → `reportTelemetryBug(error)`); theirs' diff shows the mint was new on theirs | `tests/unit/telemetry/product-events-client.test.ts::an origin without crypto.randomUUID still enqueues and ships, without an event id` (:115) | **yes** — the unguarded-mint mutation (`acd8b78`): restoring theirs' bare call makes the test throw `globalThis.crypto.randomUUID is not a function`. Shared with B2-9 | 1 | F2 | SETTLED |
| B2-3 | `lib/api/analytics-api.ts` (both-sides) | Accept the auto-merge unchanged; add to it in B2.2 rather than re-resolving it | Both sides are **purely additive** and share no symbol: ours +26/−0, theirs +16/−0 against `b87ced0`. There is nothing to resolve and nothing lost | Re-measured at assembly (`git diff b87ced0 4a2898b` / `… 0f1715a` per file). Theirs' addition is `DashboardProfileTab` / `DashboardProfileResponse` / `fetchDashboardProfile`, all new names | none for the merge itself; the function it added is later guarded by B2-18 | not recorded | 1 | F2 | OPEN |
| B2-4 | `components/features/analytics/analytics-panel-registry.ts:64-70` (`DashboardProfileView`), `:962-991` (`PANEL_BY_ID`, `panelsForProfileTab`, `visibleProfileTabs`) | **Accept theirs' profile-render helpers exactly as merged.** No B2 commit touches this file | Both sides additive (ours +80/−0, theirs +37/−0), so the auto-merge is textually safe — and B2's items were scoped to the page, the card and the telemetry client | **This is the slice's largest unreviewed surface, and it embodies a decision.** `panelsForTab(tab, hasCapability, isTenantOwner)` (`:942`) applies the local capability gate; `panelsForProfileTab(profile, tab)` (`:971`) takes **no capability argument at all** and renders whatever the server profile names, filtered only by "does this build know this panel id and is it on this tab". So the `profile` path and the `capability` path of `panelsOf` (`page.tsx:431-442`) enforce different things by construction. That is defensible — core's `panel_service.get_panel_data` re-checks `authorize_panel` on every request (verified: `core-obsm/core/resources/analytics/panel_service.py:117-121`) — but it is a decision nobody in B2 wrote down | none in B2. Theirs' own two tests (B2-5) exercise the helpers, and neither passes a capability | not recorded | 2 | F2 | OPEN |
| B2-5 | `tests/unit/analytics/analytics-panel-registry.test.ts:373` and `:408` (theirs' two new cases) | **A guard file both sides changed, left exactly as git merged it.** Ours +104/−6, theirs +68/−0; no B2 commit touches it | §2.3a's amendment says a guard is trustworthy only once shown to fail when the property is removed, and the C2 analogue (C2-7) found a **relaxed** detector arriving inside this same observability merge. Here the arithmetic is benign — theirs added cases, removed none — but that was established by reading the diff, not by a guard | Re-verified at assembly: theirs' additions are two `test(` blocks and two import names (`panelsForProfileTab`, `visibleProfileTabs`), with no edit to any existing case. Both new cases assert the **ungated** profile path (`panelsForProfileTab(tenantOwner, 'improvement')` → `['improvement_findings']` with no capability argument), which is the property B2-4 flags | the file **is** a guard; nothing guards **it** | not recorded | 1 | F2 | OPEN |
| B2-6 | the 11 theirs-only files | Take all eleven, each audited against seven named repo guards — `logger-message-constant`, `api-route-pattern-bounded`, `route-pattern-table`, `logger-client-import-graph`, `mutation-response-contract`, `route-access`, `standalone-guard` — with none violated | §2.2a's own lesson from Phase D: "the theirs-only audit … is the method gap that let flightops through." A theirs-only file conflicts with nothing and so is reviewed by nobody | **All seven guards resolve in the tree** and their scan roots cover the changed paths: `tests/unit/telemetry/logger-message-constant.test.ts:28` scans `['app','components','hooks','lib']`; `api-route-pattern-bounded.test.ts:50` walks `lib/api` recursively; `route-pattern-table.test.ts:22-35` walks `app/`; `mutation-response-contract.test.ts:544` walks the mutation API layer; the other three resolve at `tests/unit/telemetry/logger-client-import-graph.test.ts`, `tests/unit/auth/route-access.test.ts`, `scripts/standalone-guard.mjs` + `tests/unit/build/standalone-guard.test.ts` | the seven above | not recorded — "none violated" rests on the green lane, and the lane was not re-run | 1 | F2 | ASSERTED |
| B2-7 | `tests/unit/analytics/analytics-dashboard-profile-page.test.tsx:320-349` | **Delete theirs' defect-pinning test and replace it with a table.** Their `failed profile request is fail-closed and fetches no panel or LLM turns data` asserted the empty-profile-on-failure outcome, so restoring correct behaviour would have read as breaking a test (the P0-INERT class) | §2.3a's RV4 finding: three of their tests "now *assert the defect* so restoring correct behaviour reads as breaking a test". §7 calls this "rewritten rather than deleted" — accurate for the scenario; the **name** is gone | Re-verified at assembly by comparing test-name sets at `42b3380` and `3afd524`: the merge's six names minus the head's seven leaves exactly `failed profile request is fail-closed and fetches no panel or LLM turns data` as removed, and the M-FALLBACK table (3 runtime cases) plus `M-FALLBACK does not widen` as added | `::M-FALLBACK: ${name}, so the capability gate is offered instead of an empty dashboard` (:328, 3 cases), `::M-FALLBACK does not widen: the degraded read offers only what the capabilities open` (:351) | **yes** — setting the empty profile on failure again fails all three M-FALLBACK cases (`9002ed9`). Shared with B2-15 | 1 | F2 | SETTLED |
| B2-8 | §7's loss proof (`git show 4a2898b:<f>` per changed test file) | **Prove no test was lost by diffing every changed test file against OURS**, because the background runner truncates output to its tail and no failing-id set difference could be quoted | `fail 0` on both sides makes the id-set difference empty by construction, so the only real question is whether a case was silently dropped. A per-file name diff answers it locally | The proof **holds as stated** — re-run at assembly, the ours-minus-head name set is empty for all three files ours had. But the baseline is **`4a2898b`, not `42b3380`**, so the check is structurally blind to a test that arrived *with the merge* and was then removed. Exactly one was (B2-7), deliberately and with its reasoning in the commit body — but the proof did not find it, the diff did | none — a verification method carries no guard | not recorded | 1 | F2 | OPEN |
| B2-9 | `lib/telemetry/product-events.ts:258-271` (the `try`), `:264` (`...eventIdOf()`) | **B2.1, clause 1.** Move the id mint **inside** the never-throw region, so nothing in `enqueueProductEvent` can reach the caller's stack | E9.9b: facts are enqueued from the handler that does the write, and the Document Hub upload runs inside a `mutationFn` — where a throw **is** the write failing. `crypto.randomUUID` is a secure-context api, absent on `http://` and on a bare LAN address, so theirs' merged placement refused a real chat send and took the document viewer into an error boundary on every insecure origin | **The defect was reproduced before it was fixed** (`acd8b78`): the new test fails on the merged tree with `globalThis.crypto.randomUUID is not a function`. The docblock at `:247-251` is explicit that the guard, not the catch, is the first line — "a guard that is the only thing standing between a browser api and a real write is a guard someone will move" | `tests/unit/telemetry/product-events-client.test.ts::an origin without crypto.randomUUID still enqueues and ships, without an event id` (:115) — the harness asserts at `:138-142` that it really removed `randomUUID` | **yes** — unguarded mint → the test throws the TypeError (`acd8b78`) | 1 | F1 | SETTLED |
| B2-10 | `lib/telemetry/product-events.ts:145-149` (`eventIdOf`), `:129-132` (`event_id?: string`) | **B2.1's deliberate omission, and the tier-2 call of this slice.** Adopt the repo's `ChatInput`/`useChatAttachments` mint idiom **with its `Date.now()-Math.random()` fallback REMOVED**. An origin that cannot mint a UUID sends **no `event_id` key at all** | Two independent server facts make a weak id strictly worse than no id: (a) the field has a **shape the server checks**, and (b) one bad member kills the whole batch. §6 separately warns B2.1 not to escalate the tenant-wide `(tenant_id, event_id)` collision class — omitting the key escalates nothing | **The core side was verified here, not taken from the plan.** `core-obsm/core/resources/analytics/schemas.py:78-80`: `model_config = ConfigDict(extra="forbid")` and `event_id: Optional[UUID] = None`. `core/resources/analytics/events_endpoints.py:94-105`: `ProductEventBatch(**data)` is constructed **for the whole batch** and any `ValidationError` is a flat `400 bad_request` — one malformed member destroys every event in the POST. `events_store.py:82`: `str(event.event_id or uuid4())` covers the omission. The repo idiom's fallback is real and was dropped on purpose — `components/features/chat/ChatInput.tsx:304-306` and `hooks/chat/useChatAttachments.ts:196-198` both carry `` `${Date.now()}-${Math.random()…}` ``. The key's scope is tenant-wide as claimed: `core/db/table_definitions.py:1447` declares `primary_key=["event_id"]` on a `tenancy="tenant"` table and `utils-obsm/utils/table_builder.py:677` leads the key with the injected tenancy columns, giving `(tenant_id, event_id)` — the same pair `events_store.py:154` uses as its `ON CONFLICT … DO NOTHING` target | same test (:115); the assertion is `'event_id' in raw[0] === false` at `:150-154` | **yes** — with a `Date.now()-Math.random()` fallback the test fails on `a non-UUID id reached the tenant-wide id namespace` (`acd8b78`). *Verified at assembly: that exact assertion message is the one present at `acd8b78:tests/unit/telemetry/product-events-client.test.ts:133`* | 2 | F1 | SETTLED |
| B2-11 | `lib/telemetry/product-events.ts:145` (`eventIdOf(): { event_id?: string }` returns the **spread**) | **Omit the KEY, not set it to `undefined`** — and make the guard assert it through a transport that does **not** serialise | `JSON.stringify` drops an `undefined`-valued key on its own, and the only production transport stringifies — so an assertion made through a serialising double proves the serializer, not this module. A later `sendBeacon` Blob or a worker's structured clone would carry the key back and the batch would 400 | Re-verified at assembly: the guard installs its own non-serialising `post` (`:129-135`) whose comment states exactly this, and asserts `'event_id' in raw[0] === false` on the raw object. The module's own docblock at `:140-143` records the reasoning | same test (:115), via the raw-transport double | **yes** — `event_id: undefined` → the omission guard fails (`3afd524`) | 1 | F1 | SETTLED |
| B2-12 | `lib/telemetry/product-events.ts:120-132` (the `event_id?` docblock) | **The accepted cost, stated in code and not measured anywhere.** Insecure-origin browsers now enter core's "client that omits `event_id`" compatibility window: a batch retried after a network timeout double-inserts, `duplicates` reads 0, and the 202 says everything was accepted | The trade is right (B2-10), but it moves a population **into** a window §6 already records as having "no expiry, no metric and no test" | §6's own entry ("A client that omits `event_id` gets no idempotency and no signal", B1, 2026-09-20) names the complete solution: "count omissions until then so the window can be closed on evidence." B2.1 created new omitters and **counted none** — re-verified at assembly, `countDroppedProductEvent` / `countDrop` exist (`:213-219`, `:230-233`) and are called for `invalid`, `unauthenticated`, `evicted`, `rejected` and `exhausted`, and **never** on the `eventIdOf()` omission path | **none** — nothing observes how many facts ship without an id | not recorded | 2 | F3 | OPEN |
| B2-13 | `lib/telemetry/product-events.ts:246-251` | Accept that a `randomUUID` which **exists and raises** (a polyfill, a locked-down context) is booked as `invalid` and reported as a caller bug, and document the misattribution rather than split the catch | "That misattribution is the price of one catch, and it is the right trade while the alternative is the throw reaching a `mutationFn`" | The docblock says so in the code, at the site. The alternative — a second `try` around the mint alone — would buy a correct drop reason at the cost of a second never-throw boundary to maintain | **none** — no test drives a throwing `randomUUID` | not recorded | 1 | F1 | OPEN |
| B2-14 | `tests/unit/telemetry/ingest-contract.test.ts:59-66` | Replace the merged contract assertion `assert.match(String(event.event_id), /^[0-9a-f-]{36}$/i)` with **"absent, or a real UUID"** | Two defects in one line: it **demanded** a field the ruling makes optional, and the pattern it demanded is satisfied by **36 hyphens** — a guard that passes on a string no minter could produce | Re-verified at assembly: the head reads `if ('event_id' in event) { … }` at `:62`, and the comment states the ruling it now encodes | `::product events POST the merged path with the { events } envelope, 50 at most` (:26) | **not recorded.** This change is in `3afd524` but is **not** one of the six mutations that commit itemises | 1 | F1 | ASSERTED |
| B2-15 | `app/(dashboard)/settings/department/dashboard/page.tsx:145-150` (`DashboardOffer`), `:431-453` (`panelsOf` / `tabs`), `:595-616` (the degrade) | **B2.2 / M-FALLBACK.** Replace a nullable profile with a **three-valued** `DashboardOffer` (`profile` \| `capability` \| `none`); only the middle value degrades, to `visibleTabs` / `panelsForTab` | "A nullable profile cannot distinguish 'core said offer nothing' from 'core said nothing' from 'nothing to ask yet'." Theirs replaced the capability gate outright and set an **empty profile** on any failure, so a core outage rendered a blank analytics page indistinguishable from "you have no access" — the wrong sentence, and the one the user acts on. This also retires §5.5's P1 "`canViewPanel` / `panelsForTab` / `visibleTabs` become dead code" | Re-verified at assembly: `panelsOf` (`:431-442`) and `tabs` (`:444-453`) are single `switch` statements over the three kinds, so the two paths cannot drift; `panelsForTab` and `visibleTabs` are live again on the `capability` arm | `tests/unit/analytics/analytics-dashboard-profile-page.test.tsx::M-FALLBACK: …` (:328, 3 cases), `::ordinary dashboard profile renders its panel…` (:267), `::explicit empty profile is fail-closed…` (:281) | **yes** — setting the empty profile on failure again fails all three M-FALLBACK cases (`9002ed9`) | 2 | F1 | SETTLED |
| B2-16 | `page.tsx:120-122` (the `profile` clause of the docblock), `:433-435` | **An explicitly empty profile stays authoritative and fail-closed** — it is a tenant's own configuration, not an outage, and it does **not** degrade | This is the half that makes the three-valued type worth having: degrading an empty profile would silently overrule a tenant's narrowing every time they used it | The behaviour is pinned by a test that predates B2 (theirs', kept unmodified through all five commits) | `::explicit empty profile is fail-closed and fetches no panel or LLM turns data` (:281) | **not recorded.** The empty-profile mutation proves the **failure** path; no mutation removes the authoritative-empty behaviour | 2 | F1 | ASSERTED |
| B2-17 | `page.tsx:569-578` | `!canViewDashboard` (or no tenant identity) yields **`none`**, not a synthesised empty profile — "the page never asked core, so it holds no answer of core's to represent" | A synthesised empty profile would make the three-valued type carry two values again, and would make "you hold no capability" indistinguishable from "your tenant configured nothing" | The code states the reasoning at the site; the change landed in `3afd524` as part of making the type carry three values | **none.** Re-verified at assembly: every case in `analytics-dashboard-profile-page.test.tsx` supplies `permissionValue(...)` with `VIEW_DASHBOARD` (default at `:85`, explicit at `:353`); **no case drives `canViewDashboard === false` through the page.** The registry-level analogue (`::visibleTabs: a user without view_dashboard sees nothing`, `analytics-panel-registry.test.ts:353`) guards the helper, not this branch | not recorded | 1 | F1 | OPEN |
| B2-18 | `lib/api/analytics-api.ts:238-243` (`dashboardProfileReply`), `:245-250` | Give `fetchDashboardProfile` a **read response contract**, so a malformed 200 is a rejection the fallback already handles rather than a `TypeError` inside a `useMemo` | "The profile decides what the whole page offers, and every field of it is read" — through a bare cast a 200 without `tabs` reaches `profile.tabs.find` and takes the analytics page out through an error boundary. Through the contract it is simply a third way into the degraded path | Re-verified at assembly: the schema pins `version` and `tabs[]{tab,panels[]}` — exactly the fields the page reads | `tests/unit/analytics/analytics-dashboard-profile-page.test.tsx::M-FALLBACK: the reply breaks its contract, …` (:328, the third `PROFILE_FAILURES` row at `:325`) | **yes** — replacing `parseResponse` with the bare cast fails the contract case (`9002ed9`) | 1 | F1 | SETTLED |
| B2-19 | `page.tsx:125-131` | **Corrected false claim #1 (the implementer's own).** The M-FALLBACK 403 rationale was **fiction**, taken from this plan's B1.2 wording ("tenant-admin gate") rather than from core's body. The branch **stays** — "a gate is a thing that changes" — but is documented as **unreachable today** rather than as the motivating case | The original commit (`9002ed9`) justified the whole degrade with "the ordinary 403 a `view_dashboard` holder who is not a tenant admin gets from that route". Both reviewers proved that caller cannot exist. A rationale nobody re-checks is exactly the thing that ossifies | **Verified against core at assembly, not against the plan.** `core-obsm/core/resources/analytics/analytics_endpoints.py:44-53`: `is_tenant_admin` returns `is_tenant_owner OR has_capability(auth_context, "view_dashboard")`; `:81` applies it to `GET /analytics/dashboard-profile`. The page asks only when `hasCapability(PERMISSION_NAMES.VIEW_DASHBOARD)` (`page.tsx:405`, `:569`). Same predicate, so the 403 cannot fire. The route enumeration also holds: the analytics router declares exactly two GETs (`:56`, `:97`), both gated | `::M-FALLBACK: the caller is refused, …` (:328, `PROFILE_FAILURES[1]` at `:322`) — it asserts the wire status is **403**, which is what makes the case the thing it names | **yes** — the fixed-503 harness makes the refusal case fail (`3afd524`). Shared with B2-22 | 2 | F1 | SETTLED |
| B2-20 | `page.tsx:127-129` vs `core-obsm/core/resources/analytics/analytics_endpoints.py:44-53` | **Owed, and not stated anywhere: the 403's unreachability is a two-sided property.** It holds only while the browser's capability resolution and core's `has_capability` agree about `view_dashboard` | The docblock says "the same `canViewDashboard` this page checks before asking", which is true of the *intent* and unverified as an *invariant*. core's own docstring warns the check "reads the resolved CAPABILITY, not the legacy permission rows" — i.e. the two halves have already diverged once | Re-verified at assembly: the page reads `hasCapability(PERMISSION_NAMES.VIEW_DASHBOARD)` from the permissions context; core reads `authz_context.has_capability(auth_context, "view_dashboard")`. Nothing in either repo asserts the two resolve alike | **none** — and none is possible inside one repo | not recorded | 2 | F3 | OPEN |
| B2-21 | `page.tsx:133-140` | **Corrected false claim #2 (the implementer's own).** "No widening" was **half true**: the degraded read does **not** widen *capability*, and **does** widen past a tenant's own **narrowed profile**. Now stated precisely in the docblock; the trade is kept | `9002ed9` had asserted flatly "No widening: … the server still refuses every one it should." That is true of `authorize_panel` and says nothing about the profile, which is the thing the degrade replaces. It "remains the right trade … but the claim as written was wrong and is exactly the kind of thing nobody re-checks" | **Verified against core at assembly.** `core-obsm/core/resources/analytics/panel_service.py:117-121` — `get_panel_data` calls `authorize_panel(auth_context, spec)` then `feature_enabled(spec)`; a repo grep shows **zero** occurrences of `dashboard_profile` anywhere in `panel_service.py`. So the capability half is real and the profile half is real | The capability half is guarded: `::M-FALLBACK does not widen: the degraded read offers only what the capabilities open` (:351), which leads with the **positive** assertion because "an empty dashboard satisfies every negative assertion". **The profile half — the widening that actually exists — is guarded by nothing**: no test asserts that a tenant with a narrow profile sees the un-narrowed offer while the route is down, which is the behaviour the correction admits to | not recorded for the profile half; the capability half's companion test was **inert on its first draft** and was repaired without an itemised mutation | 2 | F1 | OPEN |
| B2-22 | `tests/unit/analytics/analytics-dashboard-profile-page.test.tsx:320-326` (`PROFILE_FAILURES`), `:336-344` | **A test that named three failure routes exercised two.** The harness answered **503 for every `Error`**, so "the caller is refused" was an outage with different prose. Each failure now carries its own status, the wire records it, and each case asserts the status it names | All three routes degrade **identically by design**, so the status is the only thing that can tell them apart — without it the three rows were one scenario wearing three labels | Re-verified at assembly: `PROFILE_FAILURES` is `[..., 503], [..., 403], [..., 200]` and the case asserts `page.wire.find(call => call.kind === 'profile')?.status === expectedStatus` with the message "the harness did not answer this case with the status it names" | `::M-FALLBACK: ${name}, …` (:328) | **yes** — the fixed-503 harness makes the refusal case fail (`3afd524`) | 1 | F2 | SETTLED |
| B2-23 | `tests/unit/analytics/analytics-dashboard-profile-page.test.tsx:311-317` | **401 is recorded, not asserted.** Driving it found that 401 never reaches this fallback at all — `fetchWithAuth` refreshes once and then navigates to sign-in | jsdom answers the navigation with `Not implemented: navigation to another Document`. "Asserting a jsdom navigation would be pinning an environment" | The finding is written into the test file's own docblock, where the next person changing the table will read it, rather than into a commit message | **none**, deliberately | not recorded | 1 | F1 | OPEN |
| B2-24 | `components/features/observability/LlmTurnSummariesCard.tsx:47-54` (`LLM_TURNS_ERROR_COPY`), `:283` (`errorState={error ? classifyAnalyticsError(error) : null}`) | **B2.3.** The card renders a **typed failure state**, never the server's sentence. The prop changed from `error: string` to `errorState: AnalyticsErrorState \| null` | `error.message` is FastAPI's `detail`, on the read path over **captured conversation content**, whose prose can name a tenant, a department or a capability — on a surface whose entire claim is that it shows previews and counts. `classifyAnalyticsError` keys on the HTTP status, a closed set of four, rather than on prose that moves | The guard drives the **real hook** against a real 403 whose `detail` names a department, because once the prop is a state the presentational half cannot carry prose and asserting its absence there "would be asserting a type" | `tests/unit/observability/llm-turn-summaries-card-render.test.tsx::the server's own sentence never reaches the card` (:128) | **yes** — rendering `error.message` fails the prose guard (`cc17cf4`) | 2 | F1 | SETTLED |
| B2-25 | `LlmTurnSummariesCard.tsx:47-54` | The four sentences are **this card's own**, not `ANALYTICS_ERROR_COPY`'s | "'panel' is not what a reader of this card is looking at" — the shared table's wording describes an analytics panel | The copy table is `Record<AnalyticsErrorState, string>`, so adding a fifth state is a type error rather than a silently missing sentence | `::errors and loading states are visible without opening another tool` (:85), `::a refusal shows the typed sentence and offers no Retry; an outage offers one` (:109) | not recorded | 1 | F1 | ASSERTED |
| B2-26 | `LlmTurnSummariesCard.tsx:268-276` | A **constant-message** `logger.error` breadcrumb on failure. The card logged nothing at all as merged | "The wrapper's ship-dedupe is keyed on message + endpoint + status, so an interpolated one is a new series per failure." The server's sentence is deliberately kept out of it, for the same reason B2-24 keeps it off the screen | Re-verified at assembly that the repo's AST guard reaches this file: `tests/unit/telemetry/logger-message-constant.test.ts:28` declares `ROOTS = ['app','components','hooks','lib']` and `:29` exempts only `components/features/rostering/` | `tests/unit/telemetry/logger-message-constant.test.ts::every shipped-level logger call in the app takes a constant message` (:258) | **yes** — interpolating the message fails `logger-message-constant`, **naming this file and line** (`cc17cf4`) | 1 | F1 | SETTLED |
| B2-27 | `lib/api/llm-observability-api.ts:65-67`, `:84` | The **response contract** in place of the bare cast on the LLM-turns read | A 200 without `rows` reached `rows.map` and took the improvement page out through a boundary; through the contract it is a `ResponseContractError` the card states in place | Re-verified at assembly: the schema demands only `rows`, which is "the only field the card reads" — the deliberate minimum, because every row key is optional on the wire and the card renders each absence | `tests/unit/observability/llm-turn-summaries-api.test.ts::a 200 without rows is a response-contract failure, not a render crash` (:85) | **yes** — the bare cast fails the contract test (`cc17cf4`) | 1 | F1 | SETTLED |
| B2-28 | `lib/api/llm-observability-api.ts:66` (`z.array(z.record(z.unknown()))`) | A row must be an **object**, not merely present | "`null` is not the absence of a key, it is a value the row renderer dereferences" — `row.usage` and `row.selected_document_ids` are read unconditionally (`LlmTurnSummariesCard.tsx:120`, `:165`). An **empty** object must stay valid, because every field is optional and the card renders each absence | The guard asserts both directions in one case, so a fix that made `{}` invalid would fail too | `tests/unit/observability/llm-turn-summaries-api.test.ts::a null row fails the contract; an empty object row does not` (:105) | **yes** — `z.unknown()` rows → the null-row guard fails (`3afd524`) | 1 | F1 | SETTLED |
| B2-29 | `hooks/api/useObservability.ts:33-40`, `lib/query/query-keys.ts:346-359` | Replace hand-rolled `useState`/`useEffect` with TanStack `useQuery` under a new key `queryKeys.observability.llmTurns(limit)` — and keep the **tenant out of the key** deliberately | The hand-rolled version refetched on every mount with no cache, no dedupe against a sibling reader, no retry rule and **no participation in the identity-change cache clear** — so a signed-out-then-in reader could be shown the previous identity's rows. `limit` is in the key because two callers asking for different row counts hold different result sets | The key's own docblock states the tenant exclusion and its reason: "the row set is scoped by the token server-side, and the whole cache is thrown away on an identity change (`shouldClearQueryCache`)" | **none for the property that motivated the change.** `tests/unit/observability/llm-turn-summaries-hook.test.tsx` drives the real `makeQueryClient()` but asserts retry and toast behaviour only; nothing asserts that an identity change evicts these rows | not recorded | 1 | F1 | OPEN |
| B2-30 | `lib/api/llm-observability-api.ts:1-10`, `LlmTurnSummariesCard.tsx:188-194` | **Correct the docstring in the direction that matters.** "Full content stays hidden from this product dashboard" described a card that renders **180-character previews of the user's question and the model's answer** | A false privacy statement at the top of the module is worse than none: it is the sentence the next reviewer will trust instead of reading the renderer | Re-verified at assembly: `input_preview` / `answer_preview` are declared at `:31-34` as "the first 180 characters of the user's message, as stored", and the card renders both at `:144` and `:152`. The card's own `CardDescription` now says "the first 180 characters of each question and answer as stored … Not the full stored turn" | **none** — a docstring carries no guard, and the repo has no test that reads either module's prose | not recorded | 2 | F1 | OPEN |
| B2-31 | `hooks/api/useObservability.ts:27-31` | **The retry rule CHANGED during triage.** It no longer reads `ANALYTICS_ERROR_COPY[...].retryable`; only `unavailable` auto-retries, and `rate_limited` no longer does | The shared table answers "is offering the user a **button** worth it" — a human, a minute later. A machine re-asking a 429 at TanStack's ~1 s backoff "adds load at the moment core asked for less", while the card's own copy says "try again in a minute". The button keeps the shared table; the machine gets a narrower rule | Re-verified at assembly against `cc17cf4:hooks/api/useObservability.ts:26-28`, which read `if (!ANALYTICS_ERROR_COPY[classifyAnalyticsError(error)].retryable) return false;` — and `analytics-error-state.ts:77-92` marks **both** `rate_limited` and `unavailable` retryable, so the old rule auto-retried a 429 | `tests/unit/observability/llm-turn-summaries-hook.test.tsx::the retry rule: an outage is re-asked once, a refusal and a rate limit never` (:101) — the 429 case carries the reasoning in its assertion message | **yes** — no `retry` → the once-only guard fails (`3afd524`, via `::a 403 is stated in place and never toasted, and is asked exactly once` at :121) | 1 | F1 | SETTLED |
| B2-32 | `hooks/api/useObservability.ts:38` (`meta: { suppressGlobalError: true }`) | The query opts **out of the global error toast** and states its failure in place | The route answers 403 to a caller without `view_dashboard` — "a settled fact about who they are rather than an incident", and the generic toast would announce it on every mount of a page whose job in that case is to say so once, calmly | The property lives in the **QueryClient**, which a render test stubs out: the card's only mounted test built `new QueryClient({ defaultOptions: { queries: { retry: false } } })` with **no `QueryCache`**, so `meta.suppressGlobalError` was never read. Verified at `cc17cf4:tests/unit/observability/llm-turn-summaries-card-render.test.tsx:127-128`. The new file drives the real `makeQueryClient()` (`lib/query/query-client.ts:7-12`) | `tests/unit/observability/llm-turn-summaries-hook.test.tsx::a 403 is stated in place and never toasted, and is asked exactly once` (:121) | **yes** — no `meta` → the toast guard fails (`3afd524`) | 1 | F1 | SETTLED |
| B2-33 | `LlmTurnSummariesCard.tsx:198-209` | The **header Refresh** button obeys the same refusal rule as the in-error Retry | "The card has two buttons that do the same thing under different labels" — the error box hid its button while the header kept one that only says a different word, which is why the "offers no Retry" assertion used to pass. A refusal is a settled fact, so an enabled re-ask is an invitation to keep being refused | The guard asserts **both words**, and balances it with the converse (`unavailable` still offers `Refresh`) so the fix cannot become "never offer a re-ask" | `tests/unit/observability/llm-turn-summaries-card-render.test.tsx::a refusal offers NO re-ask at all — neither Retry nor Refresh` (:98) | **yes** — unconditional Refresh → the re-ask guard fails (`3afd524`) | 1 | F1 | SETTLED |
| B2-34 | — (absence, repo-wide) | **M-FRONTEND needs no dashboard-repo action** — recorded after verifying **both** halves | M-FRONTEND rules "take our `fn-frontend` board wholesale, graft their Slowest Pages panel as a 19th". If the dashboard repo held board ids or panel counts, the ruling would have a site here | **Both halves re-verified at assembly.** (a) A case-insensitive repo-wide grep for `grafana` and `fn-frontend` outside `node_modules` and `.git` returns **zero** hits. (b) `git diff --name-only 4a2898b 3afd524` over `lib/telemetry/events.ts`, `lib/telemetry/use-route-telemetry.ts`, `components/providers/TelemetryProvider.tsx` and `tests/unit/telemetry/events-catalogue.test.ts` is **empty** — no B2 commit touched any of the four. `browser.app.boot` is unchanged and still carries no flag or sampling gate (`lib/telemetry/use-route-telemetry.ts:105-135`: once per document, guarded only by `bootEmitted` and by the Performance API being present) | none — a verified absence has no guard | not recorded | 1 | F3 | OPEN |
| B2-35 | `app/(dashboard)/settings/tenant/improvement/page.tsx:181-184`, `:212`, `:226` | **B2-R1 — the card's placement. B2 deliberately did not change it**, because it is a product decision | The card sits inside the page's internal-only branch, but that branch is chosen by `notInternal`, which is derived from **the other four queries' errors**. On first paint nothing has been refused yet, so the card mounts and fetches unconditionally | **Re-verified at assembly, and §7 is now stale about it.** `:181-184` is exactly `const notInternal = [findingsQuery.error, runsQuery.error, runNow.error, triage.error].find(isInternalOnly) ?? null;` and the card is mounted at `:226` inside the else-branch at `:212`. The card's own route gates on `view_dashboard`, not on the internal-domain check, so its fetch succeeds while its siblings are refused. `lib/query/query-client.ts:26` is `gcTime: 5 * 60_000`, so the rows survive the unmount for five minutes. **§4b's M-TURNCARD (owner ruling, 2026-09-20) rules this: refuse the card on the SERVER, on its own route, and it explicitly "Closes B2-R1 and D.9's open clause."** §7's B2 section still lists B2-R1 as an owed ruling | **none** — and the fix is server-side, in another repo, in a later phase | not recorded | 2 | F3 | OPEN |
| B2-36 | `page.tsx:133-140` | **B2-R2 — owner ruling owed.** M-FALLBACK's true scope, now that the widening is stated correctly: the degraded read shows a tenant the **un-narrowed** offer while the profile route is down | M-FALLBACK (§4b) was taken on the reasoning "it is not a privilege widening". That reasoning survives (B2-21), but the ruling was taken **without** the fact that it widens past the tenant's own profile | core's own module calls the profile "presentation state only… It never grants panel access", so the ruling reads as still standing — but it was not taken in the light of this | **none** — and none is possible until the ruling lands | not recorded | 2 | F3 | OPEN |
| B2-37 | `core-obsm/core/resources/analytics/dashboard_profiles.py` (whole module) | **B2-R3 — `dashboard_profiles` still has NO WRITER anywhere in the estate**, so no tenant can narrow anything yet. §5.5 P1, unchanged by B2 | The entire M-FALLBACK design — three-valued offer, authoritative-empty, the profile-widening trade — is reasoning about a table nothing populates | **Verified across the whole workspace at assembly, not trusted.** A repo-wide search for `dashboard_profiles` across `.py/.ts/.tsx/.sql/.tf/.md/.json` returns 19 files: 8 in `core-obsm` (4 of them tests), 1 in `dashboard-obsm` (the page), 1 stale `copilot-mro-obsm` plan and 8 docs. The module declares exactly three functions — `resolve_dashboard_profile` (:68), `_coerce_panel_ids` (:99), `load_dashboard_profile` (:120) — all read-side. The **only** `INSERT INTO dashboard_profiles` / `DELETE FROM dashboard_profiles` in the estate are in `core-obsm/tests/db/analytics/test_dashboard_profiles_db.py:62,70`. `analytics_endpoints.py` declares two routes, both `@router.get` | **none** — nothing asserts the absence, so a writer could land without anyone revisiting this | not recorded | 2 | F3 | OPEN |
| B2-38 | `components/features/analytics/analytics-panel-registry.ts:971-981` | **Recorded, deliberately not fixed: version skew.** A **200** profile naming panel ids this build does not know intersects to `[]` and renders exactly the blank page M-FALLBACK exists to eliminate — **with no degrade**, because the fetch succeeded | The fallback is keyed on the request failing. A successful answer this build cannot render is the one shape it cannot see | Re-verified at assembly: `panelsForProfileTab` is `entry.panels.flatMap(id => { const p = PANEL_BY_ID.get(id); return p && p.tab === tab ? [p] : []; })`, and `visibleProfileTabs` filters tabs to those with a non-empty result — so an all-unknown profile yields `tabs = []` and `tabPanels = []`, which is the blank page. Theirs' own registry test asserts this silent dropping is intended (`analytics-panel-registry.test.ts:373`, `'unknown_server_panel'` vanishes with no signal) | **none** | not recorded | 2 | F3 | OPEN |
| B2-39 | `lib/query/query-client.ts:23-33`, consumed by `LlmTurnSummariesCard.tsx:281` (`isLoading={query.isPending}`) | **Recorded, deliberately not fixed: the offline wedge.** `isPending` plus TanStack's default `networkMode: 'online'` leaves "Loading captured turns…" forever with no exit | The fix is a third **paused** branch, not a swap to `isLoading` — the card genuinely has no data and genuinely is not loading | Re-verified at assembly by **absence**: `makeQueryClient()` sets `staleTime`, `gcTime`, `retry` and `refetchOnWindowFocus` and **does not set `networkMode`**, so the library default `'online'` applies and the query never leaves `pending` while offline | **none** | not recorded | 1 | F3 | OPEN |
| B2-40 | `analytics-panel-registry.ts:971-981` vs `:942-951` | **Recorded, deliberately not fixed: panel-order drift.** `panelsForProfileTab` returns panels in **profile order**; `panelsForTab` returns them in **registry order** | The same tenant sees a different panel order depending on whether the profile route answered — a presentation inconsistency the degrade introduces silently | Re-verified at assembly: `panelsForProfileTab` maps over `entry.panels` (the server's array); `panelsForTab` filters `PANEL_REGISTRY` | **none** | not recorded | 1 | F3 | OPEN |
| B2-41 | `tests/unit/api/mutation-response-contract.test.ts:544` | **Recorded: both read contracts B2 added are voluntary.** No guard requires a **read** api to return through `parseResponse` | The existing sweep is titled "every API function a **mutation** calls…", so the two contracts B2.2 and B2.3 added (B2-18, B2-27) are held in place by their own per-function tests and by nothing structural | Re-verified at assembly: the guard's own docblock at `:21-23` scopes it to functions a mutation calls, and its sweep test is `::every API function a mutation calls returns through the contract, returns nothing, or is exempted with its reason` | **none** — this row records a guard's *absence*, which is why the two contracts are ASSERTED-by-their-own-test rather than structurally enforced | not recorded | 1 | F3 | OPEN |
| B2-42 | `analytics-panel-registry.ts:64-70` (`DashboardProfileView`) vs `lib/api/analytics-api.ts:222-230` (`DashboardProfileTab` / `DashboardProfileResponse`) | **Recorded: the profile wire shape is declared twice** — the dashboard analogue of B1's two-copies finding | Structural typing makes `DashboardProfileResponse` assignable to `DashboardProfileView`, so the duplication is invisible until one of the two gains a field | Re-verified at assembly: both declare `{ version: number; tabs: { tab: string; panels: string[] }[] }`, one `readonly` and one not. `page.tsx:146` holds the api's type and passes it to the registry's helpers | **none** — no test asserts the two agree | not recorded | 1 | F3 | OPEN |
| B2-43 | `lib/query/query-keys.ts:357-358` vs `lib/query/identity-change.ts:20-25` | **Recorded, proved neither way.** The `llmTurns` query key omits the tenant while `shouldClearQueryCache` keys on **user id** — one reviewer suspected a hole, the other proved it unreachable | The cached rows are 180-character previews of real questions and answers. If a tenant switch can occur without a user-id change, the previous tenant's content survives in a cache keyed by neither | Re-verified at assembly: `shouldClearQueryCache(previousUserId, currentUserId)` returns `previousUserId && previousUserId !== currentUserId` — **user id only**; the `llmTurns` key is `['observability','llm-turns', limit ?? 'default']` — **no tenant**. An in-session tenant switch is a real operation in this app: the dashboard page keys four pieces of state on `tenantIdentityKey` (`page.tsx:407-425`) and two tests drive `page.setPermissions(permissionValue('tenant-b'))`. Whether that path can run without a user-id change was **not** settled here either | **none** | not recorded | 2 | F3 | OPEN |
| B2-44 | `LlmTurnSummariesCard.tsx:165` | **Recorded, deliberately not fixed:** `content_bytes: 0` renders `—`, i.e. "captured nothing" is displayed as "unknown" | The row renderer uses truthiness, and `0` is a meaningful value on this field | Re-verified at assembly: `Size: {row.content_bytes ? \`${row.content_bytes} bytes\` : '—'}` | **none** | not recorded | 1 | F3 | OPEN |
| B2-45 | `LlmTurnSummariesCard.tsx:270-275` | **Recorded, deliberately not fixed:** `logger.error` books an ordinary `forbidden` as an **ERROR** record, one per mount | The same reasoning that suppresses the toast (B2-32) — a settled authorization fact is not an incident — is not applied to the log sink, so the breadcrumb that exists for outages also books every refusal | Re-verified at assembly: the `useEffect` fires on **any** truthy `error` with no classification, so a 403 produces an ERROR record on every mount of the improvement page | **none** | not recorded | 1 | F3 | OPEN |

**B2 counts — 45 rows.** Tier: 0 = **0**, 1 = 30, 2 = 15. Chunk: F1 = 22, F2 = 9, F3 = 14.
Claim state: **SETTLED = 16 · ASSERTED = 4 · OPEN = 25.**

SETTLED: B2-2, B2-7, B2-9, B2-10, B2-11, B2-15, B2-18, B2-19, B2-22, B2-24, B2-26, B2-27, B2-28, B2-31,
B2-32, B2-33.
ASSERTED: B2-6, B2-14, B2-16, B2-25.
OPEN: B2-1, B2-3, B2-4, B2-5, B2-8, B2-12, B2-13, B2-17, B2-20, B2-21, B2-23, B2-29, B2-30, B2-34, B2-35,
B2-36, B2-37, B2-38, B2-39, B2-40, B2-41, B2-42, B2-43, B2-44, B2-45.

Tier 2 splits **4 SETTLED · 1 ASSERTED · 10 OPEN**. No guard in this range was marked
`CLAIMED BUT NOT FOUND`: every guard named by §7, by a commit body or by the silent-merge register
resolved in the tree by file **and** by test name.

---

## Open claims, tier 2 first

### Tier 2, OPEN — no guard, judgment only

| id | Claim | What an auditor has to settle |
|---|---|---|
| **B2-4** | **`panelsForProfileTab` applies no capability gate; `panelsForTab` does.** Theirs' profile-render helpers were merged into a both-sides file that **no B2 commit touches**, and the two arms of `panelsOf` therefore enforce different things by construction. core re-checks `authorize_panel` per request, so nothing is granted — but the asymmetry is undocumented and untested | Is "the server refuses it anyway" a sufficient answer for a client render path that skips the gate its sibling applies — and is a both-sides file that no phase commit touched actually *audited*, or merely *identified*? |
| **B2-12** | **B2.1 moved a population into an unmeasured compatibility window.** Every insecure-origin browser now ships product facts with no `event_id`, so a retried batch double-inserts, `duplicates` reads 0 and the 202 says everything was accepted. §6 already names the complete solution — "count omissions until then so the window can be closed on evidence" — and B2.1 counted none, although `countDroppedProductEvent` exists and is used for five other reasons | Is it acceptable to enlarge a window whose stated exit condition is *evidence*, while adding no instrument that could produce that evidence? |
| **B2-20** | **The 403's unreachability is a two-sided property nobody asserts.** It holds only while the browser's `hasCapability('view_dashboard')` and core's `authz_context.has_capability(..., "view_dashboard")` agree — and core's own docstring records that the two halves diverged once already (resolved capability vs legacy permission rows) | Is "documented as unreachable today" a safe thing to write about an invariant that spans two repos and no test? If they diverge, the 403 branch becomes live and the fallback silently widens past the profile for a real population |
| **B2-21** | **The widening that actually exists is guarded by nothing.** The correction admits the degraded read widens past a tenant's own narrowed profile; the only guard (`M-FALLBACK does not widen`) asserts the **capability** half, which is the half that was never in doubt. And that test was **inert on its first draft** — an empty dashboard satisfied every assertion in it | Does a corrected claim need a guard for the half that is now admitted to be true? Today the repo tests the reassuring half and states the worrying half in a comment |
| **B2-30** | **The docstring said the card was content-free; it renders 180-character previews of real questions and answers.** Corrected in two modules, guarded in neither, and no repo test reads either module's prose | Nothing stops the next edit restoring a false privacy statement above a content-rendering module. Is a privacy claim in a docstring a thing this estate is willing to leave unguarded? |
| **B2-35** | **B2-R1, the card's placement — and §7 is stale about it.** `notInternal` is derived from the *other four* queries' errors (`improvement/page.tsx:181-184`), so on first paint the card mounts, fetches and holds captured previews for a 5-minute `gcTime` before the notice replaces it. §4b's **M-TURNCARD (owner ruling, 2026-09-20)** rules this and explicitly closes B2-R1; §7's B2 section still lists it as owed | The ruling exists; the **fix does not**, and it is server-side in another repo. Until it lands, "a screen that hides data it has already downloaded is not a gate" describes the shipped behaviour |
| **B2-36** | **B2-R2, M-FALLBACK's true scope.** The ruling was taken on "it is not a privilege widening"; that survives, but it was taken without knowing the degrade widens past a tenant's own profile | Does M-FALLBACK still read the same way now that the fact it was decided without is on the table? core calls the profile "presentation state only", which suggests yes — but the ruling has not been re-taken |
| **B2-37** | **B2-R3, `dashboard_profiles` has no writer anywhere in the estate.** Verified workspace-wide: the module is read-only (three functions, all read-side), the router declares two GETs and no write route, and the only INSERT/DELETE in the estate is in a `core-obsm` db test | The whole M-FALLBACK design — three-valued offer, authoritative-empty, the widening trade — reasons about a table nothing populates. Is the design being validated against a behaviour no tenant can yet produce? |
| **B2-38** | **Version skew renders the exact blank page M-FALLBACK exists to eliminate.** A 200 profile naming panel ids this build does not know intersects to `[]` and degrades nothing, because the fetch succeeded. Theirs' own registry test asserts the silent dropping is intended | The fallback is keyed on the request failing. Does it need a second trigger — "answered, but nothing renderable" — or is that a deployment-ordering problem rather than a client one? |
| **B2-43** | **Captured conversation previews are cached under a key with no tenant, in a cache cleared on user id.** `llmTurns` is `['observability','llm-turns',limit]`; `shouldClearQueryCache` compares user ids only. In-session tenant switching is a real operation in this app (the dashboard page keys four state slices on it and two tests drive it). One reviewer suspected a hole, the other proved it unreachable; it was recorded, **proved neither way**, and this assembly could not settle it either | Can a tenant switch occur without a user-id change? If yes, the previous tenant's question-and-answer previews survive in a cache keyed by neither — and this is the one row in the slice where the disputed quantity is real user content |

### Tier 1, OPEN

| id | Claim |
|---|---|
| **B2-1** | The merge rests on the lane and on the §2.2a register. Re-derived at assembly (`merge-tree` reproduces the commit's exact tree oid, zero conflicts, 4 both-sides + 11 theirs-only) — but no test asserts anything about merge topology. |
| **B2-3** | `analytics-api.ts`'s auto-merge was accepted because both sides were purely additive with no shared symbol. Measured, not guarded. |
| **B2-5** | A **guard file** both sides changed, taken exactly as git merged it and touched by no B2 commit. The arithmetic is benign (theirs added two cases, removed none) but that was established by reading the diff. §2.3a's C2-7 analogue is the reason this is not a formality. |
| **B2-8** | The loss proof is a diff against **ours** (`4a2898b`), so it is structurally blind to a test the merge contributed and B2 then removed. Exactly one was (B2-7). The proof is sound for what it measures and does not measure the merge. |
| **B2-13** | A `randomUUID` that exists and *raises* is booked as a caller bug. Documented at the site as the accepted price of one catch; no test drives it. |
| **B2-17** | `!canViewDashboard` → `none` is unguarded. **No page-level case drives a capability-less caller** — every case in the profile page suite supplies `VIEW_DASHBOARD`. |
| **B2-23** | 401 never reaches this fallback (`fetchWithAuth` refreshes, then navigates to sign-in). Recorded in the test file's docblock rather than asserted, because pinning a jsdom navigation pins an environment. |
| **B2-29** | The query-layer move was motivated by identity-change cache participation, and **nothing asserts it**. The new hook test covers retry and the toast only. |
| **B2-34** | M-FRONTEND's "no dashboard action" is a verified **absence** (zero `grafana`/`fn-frontend` hits; the four telemetry-contract files untouched by all five commits; `browser.app.boot` still ungated). An absence has no guard, so a later board reference would land unnoticed. |
| **B2-39** | The offline wedge, confirmed by absence: `makeQueryClient()` never sets `networkMode`, so the library default `'online'` applies and `isPending` never resolves offline. |
| **B2-40** | Panel-order drift between the profile path (server order) and the capability path (registry order). |
| **B2-41** | **No guard requires a read api to use `parseResponse`** — the sweep walks `useMutation` only. Both contracts B2 added are therefore voluntary, held by their own per-function tests. |
| **B2-42** | The profile wire shape is declared twice (`DashboardProfileView` / `DashboardProfileResponse`), kept in step only by structural typing. |
| **B2-44** | `content_bytes: 0` renders `—`: "captured nothing" is displayed as "unknown". |
| **B2-45** | `logger.error` books an ordinary `forbidden` as an ERROR record, one per mount — the toast's own reasoning (a settled authorization fact is not an incident) is not applied to the log sink. |

### ASSERTED claims worth reading anyway

- **B2-6** — the theirs-only audit. All seven named guards resolve and their scan roots demonstrably cover
  the changed paths, but **"none violated" rests on the green lane**, and the lane was not re-run at
  assembly. This is the slice's single largest inherited-trust claim: eleven files, no conflict, no
  per-file record.
- **B2-16** — an explicitly empty profile stays authoritative. Guarded by a test that came in **with the
  merge** and was never modified, which is the shape RV4 catalogued 35 times on their branch. It is very
  likely fine; nothing in B2 re-derived it, and the empty-profile mutation proves the *other* direction.
- **B2-14** — the `ingest-contract` regex fix (a pattern **36 hyphens satisfied**) landed in `3afd524`
  but is **not** among the six mutations that commit itemises. It is the one triage change with no proof
  attached.
- **B2-25** — the card's own copy table. Type-safe by `Record<AnalyticsErrorState, string>`, exercised by
  two render cases, never mutated.

---

## Owed out of this phase

1. **M-TURNCARD** (B2-35) — **owner ruling already taken (2026-09-20)**, fix not built. Refuse the
   LLM-turn-summaries card on the **server**, on its own route, with the internal-domain check the
   improvement page's other four routes apply. §7's B2 section must stop listing B2-R1 as owed.
2. **B2-R2** (B2-36) — **owner ruling owed.** M-FALLBACK's scope re-read in the light of the corrected
   widening.
3. **B2-R3** (B2-37) — **owner ruling owed.** `dashboard_profiles` has no writer anywhere in the estate;
   verified workspace-wide at assembly.
4. **An omission counter for `event_id`** (B2-12) — §6's own stated exit condition for the
   no-idempotency compatibility window, now carrying a larger population because of B2.1.
5. **A guard for the profile-widening half** (B2-21) and **a page-level case for the capability-less
   caller** (B2-17) — the two properties this slice states in prose and tests nowhere.
6. **The nine §6 Future-Improvement items** (B2-38 … B2-45 and the `randomUUID`-that-throws note) — §7
   says they were "Recorded for §6 Future Improvements". They are recorded in **§7's B2 narrative**, not
   in §6 (see assembly notes).

### Assembly notes — things the source documents assert that could not be corroborated

- **The "13 mutation proofs" count RECONCILES — exactly, unlike C2's.** Itemised from the commit bodies:
  `acd8b78` **2** (unguarded mint → TypeError; `Date.now()-Math.random()` fallback → "a non-UUID id
  reached the tenant-wide id namespace"), `9002ed9` **2** (empty profile on failure → all three
  M-FALLBACK cases; bare cast → the profile contract case), `cc17cf4` **3** (render `error.message` → the
  prose guard; interpolated log message → `logger-message-constant`; bare cast → the rows contract test),
  `3afd524` **6** (fixed-503 harness → the refusal case; `event_id: undefined` → the omission guard; no
  `meta` → the toast guard; no `retry` → the once-only guard; unconditional Refresh → the re-ask guard;
  `z.unknown()` rows → the null-row guard). 2 + 2 + 3 + 6 = **13**, and **every one maps to a named test
  that resolves in the tree**. Two caveats, both minor: (a) the proofs carry **no label series** (no
  M1…M13), so they are addressable only by their prose clause; (b) two pairs land on **assertions inside
  one test** rather than on two tests — "the toast guard" and "the once-only guard" are both
  `llm-turn-summaries-hook.test.tsx:121`, and "the refusal case" and B2-19's 403 proof are both the same
  parametrised case.
- **"Every one restored from a scratchpad copy; `git checkout --` never used" is a generalisation.**
  Only `3afd524` makes that statement, about its own six. The seven in `acd8b78`, `9002ed9` and `cc17cf4`
  record the mutation and its failure but say nothing about how the tree was restored. §7 applies the
  clause to all thirteen.
- **"Recorded for §6 Future Improvements" — they are not in §6.** All nine items (the offline wedge,
  version skew, panel-order drift, the `logger.error` ERROR record, the `llmTurns` key, the missing read
  contract guard, the two type declarations, the throwing `randomUUID`, `content_bytes: 0`) appear only
  in **§7's B2 narrative at lines 1303-1315**. §6 (lines 883-1112) carries B1, C1 and D entries and none
  of B2's. A grep for `offline wedge`, `version skew`, `panel-order`, `panelsForProfileTab` and
  `content_bytes: 0` finds hits nowhere but §7.
- **§7 lists B2-R1 as an owed ruling; §4b has already ruled it.** M-TURNCARD (owner ruling, 2026-09-20)
  states in its own text that it "Closes B2-R1 and D.9's open clause." Two sections of one plan disagree
  about whether the ruling is outstanding. The *fix* is outstanding either way.
- **"Rewritten rather than deleted" understates one case and describes the other exactly.** For the card
  test (`cc17cf4`) their defect-pinning **assertion** was replaced inside a kept test name — accurate.
  For the profile page test (`9002ed9`) the whole test — `failed profile request is fail-closed and
  fetches no panel or LLM turns data` — is **gone by name**, replaced by a four-case table. §7's loss
  proof, being a diff against ours, did not see it.
- **The lane figures were NOT re-run** (typecheck rc=0 byte-identical, `next lint` byte-identical, unit
  2475 → 2502/0/0), per the read-only brief and to avoid contending for the tree's toolchain. They are
  recorded identically in §7. The **+27** was independently re-derived and reconciles exactly
  (25 static declarations + 2 extra loop iterations); the **no-test-name-removed** claim was
  independently re-run and holds against `4a2898b`.
- **"`next build` … the merged standalone guard fired correctly when it was attempted" is half
  verifiable.** `scripts/standalone-guard.mjs:4-13` exists and documents the worktree symlink refusal,
  and `tests/unit/build/standalone-guard.test.ts` guards it. That it *fired on the day* is not
  recoverable from the tree.
- **A consequence of B2-31 that no source document records:** `ResponseContractError` carries **no HTTP
  status** (`lib/api/response-contract.ts:16` says so explicitly, "the server answered 2xx"), and
  `classifyAnalyticsError` maps a statusless error to **`unavailable`** — the one class the new retry
  rule auto-retries. So a deterministic schema failure (B2-27, B2-28) is silently re-asked once. One
  extra request, not a defect, but it is the exact case the rule's own reasoning ("nothing was decided
  and a blip can self-heal") does not cover, and nothing guards it.
- **The §2.2a register's audit sentence covers the 11 theirs-only files and not the 4 both-sides
  files.** Three of the four received a B2 commit as a side effect of B2.1/B2.2;
  `analytics-panel-registry.ts` and `analytics-panel-registry.test.ts` received **none** (B2-4, B2-5).
  For a phase whose spine is "a merge with no raised conflicts is the condition the register exists for",
  two silently merged files — one of them a guard — going untouched is the register's own blind spot on
  this slice.
