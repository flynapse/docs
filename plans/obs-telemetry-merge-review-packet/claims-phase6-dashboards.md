# Claims table — Phase 6: dashboards, alerts, runbooks

**Scope.** Six+two Grafana boards under
`copilot-mro-obsm/deployment/observability-local/grafana/provisioning/dashboards/flynapse/`; the four
Prometheus rule files under `.../rules/prometheus/`; the Loki browser rule tree under
`.../rules/loki/flynapse/`; `copilot-mro-obsm/deployment/otel/dashboards/CATALOGUE.md`; the four
runbooks under `copilot-mro-obsm/docs/runbooks/observability/`; the cloud equivalents
`iac/dashboards/*.tftpl` and `iac/alarms.tf`; the guards under
`copilot-mro-obsm/tests/integration/otel/`.

**Date.** 2026-09-20. **Reviewer.** Independent adversarial review, run late, against the merged tree.
**Mode.** Read-only. No test suite was executed; no container was started. Every "Mutation-proved?"
cell is therefore either a mutation recorded *in the tree itself* or `not recorded`.

**Tiers.** Tier 2 = the signal contract (metric/span names, units, attributes) plus anything touching
tenancy or RLS. Tier 1 = consequential but reversible. Tier 0 = a mutation-checked guard proves it.
**Chunks.** F1 contract+privacy · F2 the merge · F3 residual.
**Claim state.** SETTLED = guard exists AND is recorded as failing when the property is removed ·
ASSERTED = guard exists, no mutation proof · OPEN = no guard.

All 48 rows are present; the `#` column is a stable identifier, and rows are grouped by area
(copilot-mro-obsm signal contract → guards → alerts → iac → runbooks), not sorted numerically.

---

| # | Repo | File:line | Decision taken | Why | Evidence it is right | Guard test | Mutation-proved? | Tier | Chunk | Claim state |
|---|------|-----------|----------------|-----|----------------------|-----------|------------------|------|-------|-------------|
| 1 | copilot-mro-obsm | `tests/integration/otel/_emitted_series.py:63-140` | Panel/alert signal state comes from ONE derived inventory (`SERIES`), not from hand-maintained allow-lists | Two prior tuples (`PENDING_RULE_SERIES`/`EMITTED_RULE_SERIES`) left any unlisted series checked by nothing | The replaced tuples are named in the docstring; `classify()` makes an unexplained token a failure rather than a silent pass | `test_emitted_series_inventory.py` (whole module) | not recorded | 2 | F1 | ASSERTED |
| 2 | copilot-mro-obsm | `_emitted_series.py:196-214` (`base_name`/`legal_suffixes`) | Prometheus name model: dots→underscores, UCUM unit appended unless already present, counter `_total`, histogram `_sum`/`_count`/`_bucket` | The unit is part of the exported NAME; a wrong cell empties every panel silently | Verified by hand against `telemetry.py` instrument declarations; `_UNIT_SUFFIX` maps only `s`/`ms`/`By`, and all live units are `""`,`1`,`s`,`{USD}`,`{token}` | `test_oss_profile_smoke.py:851 test_prometheus_serves_the_exact_names_the_inventory_computes` — pushes one synthetic datapoint per instrument through the pinned collector and asserts Prometheus serves the predicted names | not recorded (marker-gated `compose_stack`; not run here) | 2 | F1 | ASSERTED |
| 3 | copilot-mro-obsm | `agent_shared/telemetry.py:1377-1381`; `_emitted_series.py:96` | `gen_ai.client.token.usage` is a **histogram** (M-TOKENUSAGE), so boards query `_sum` | semconv defines it as a histogram; a `_total` query matches nothing | `create_histogram(...)` confirmed at source; `llm-agents.json:31,105` both query `_sum` | `signal_state_offenders` wrong-family limb + `test_emitted_series_inventory.py::test_every_declared_instrument_has_an_inventory_entry_that_matches_it` | not recorded | 2 | F1 | ASSERTED |
| 4 | copilot-mro-obsm | `telemetry.py:1344-1348` | Token-usage histogram carries an explicit bucket advisory topping out at 6.7e7 | SDK default boundaries top out at 10 000 — below one ordinary prompt, so every quantile would be meaningless | Boundaries read at source; the reasoning is in the comment | none found for the boundary values themselves | not recorded | 1 | F1 | OPEN |
| 5 | copilot-mro-obsm | `telemetry.py:1790-1799`; `dispatcher.py:96` | Tool failures are `tool_outcome="failure"` — never `"error"` | The dispatcher's `Literal["success","failure"]` is the only vocabulary | `dispatcher.py:96` `outcome: Literal["success","failure"]`; `llm-agents.json:150` filters `tool_outcome="failure"` | `signal_state_offenders` checks the SERIES token, **not** the label value | not recorded | 2 | F1 | OPEN (value unguarded; verified correct by hand) |
| 6 | copilot-mro-obsm | `agent_shared/pipeline.py:466,493-497,748,793` | Turn failures are `agent_outcome="error"`, and the span attribute and the metric label take the SAME value | One `outcome` local feeds both `span.set_attribute("agent.outcome", …)` and `record_turn(outcome=…)` | Read at source: `outcome="success" if outcome.error is None else "error"` (`:748`), `outcome="error"` (`:793`) | `agent_outcome` is in `KNOWN_LABELS`; the VALUE is unguarded | not recorded | 2 | F1 | OPEN (value unguarded; verified correct by hand) |
| 7 | copilot-mro-obsm | `agent-turn-explorer.json:38-40`; `_emitted_series.py:169-175` | Failed-turn TraceQL selects `span.agent.outcome = "error"`, NOT intrinsic `status = error` | The runtime does not set intrinsic span status for a handled failed turn, so a `status` selector finds nothing | `pipeline.py:466` sets only the attribute; no `set_status` on the turn span | `SPAN_SIGNALS` entry + `signal_state_offenders` state-note limb | not recorded | 2 | F1 | ASSERTED |
| 8 | copilot-mro-obsm | `_emitted_series.py:113-140` | Five inventory entries are DARK: `agent.ledger.write_failures`, `agent.subagent.calls`, `agent.subagent.duration_seconds`, `claude_code.token.usage`, `claude_code.cost.usage` | Ledger + CLI have no instrument in-repo; subagent instruments exist but nothing reaches `record_subagent` | `grep`/AST: no `agent.ledger.write_failures` instrument anywhere; `record_subagent` has no caller outside the facade | `test_emitted_series_inventory.py::test_a_recorder_has_a_production_call_site_exactly_when_the_series_is_not_dark` and `::test_entries_without_an_instrument_really_have_none` | not recorded | 1 | F1 | ASSERTED |
| 9 | copilot-mro-obsm | `_emitted_series.py:176-190` (`STATE_NOTES`) | Three-state description grammar: `DARK until `, `WIRED <date>, retrieval unproved: `, `LIVE since <probe> (<date>)` | Two states could not express "emitter real, retrieval unproved"; an undated LIVE is the claim the vocabulary exists to forbid | Regexes read at source | `signal_state_offenders` — but only fires when the query names an INVENTORIED metric | not recorded | 2 | F1 | ASSERTED (with a scope hole — see rows 10, 22) |
| 10 | copilot-mro-obsm | `_emitted_series.py:191-195` (`FAMILY_TOKEN`) | The lint's scope is three prefixes: `agent.*`, `gen_ai.*`, `claude_code.*` | Bounds the inventory to what this repo emits | Regex read at source | none beyond the regex | not recorded | 1 | F1 | **OPEN** — a panel repointed at any other name is not caught as unknown; five of eight boards are effectively unlinted |
| 11 | copilot-mro-obsm | `_emitted_series.py:330-346` | The "prose vouches for a signal the query no longer reads" check runs even when the query reads nothing inventoried | An early return made the check inert | **The tree itself records a mutation**: "An early return here made that check inert, and only a mutation showed it." (`:343`) | `signal_state_offenders` final limb | **yes — recorded in-tree at `_emitted_series.py:343`** (mutation described; the failing test is not named) | 2 | F1 | SETTLED (weakly — the mutation is described, the failing test is not named) |
| 12 | copilot-mro-obsm | `test_grafana_dashboards.py:95-106` (`DARK_F_PATTERN`/`BROWSER_STATE_NOTE`) | `browser.*` panels must carry a state note, checked by GRAMMAR only | `browser.*` is emitted from the dashboard repo, outside this inventory | Regex is a three-way alternation with no truth check | `test_dark_panel_notes_are_present` | not recorded | 1 | F2 | **OPEN** — would still pass if every browser state note were inverted; two stale notes confirmed (findings P0-1, P1-1) |
| 13 | copilot-mro-obsm | `test_grafana_dashboards.py:340-365` (`FRONTEND_PHASE9_EVENTS`) | The browser events fn-frontend must chart are a hand-maintained tuple of nine | Plan §2.1 "Consumer" column | Tuple read at source | `test_frontend_board_charts_every_phase9_browser_event` | not recorded | 1 | F2 | **OPEN** — exactly the hand-maintained-allow-list shape row 1 abolished for metrics; six emitted events fall outside it (`browser.auth.login`, `browser.upload.started`, `browser.discovery.job_started`, `browser.automation.run_triggered`, `browser.chat.feedback_submitted`, `browser.pdf.render`) |
| 14 | copilot-mro-obsm | `test_grafana_dashboards.py:480-499` | Browser LogQL may read only keys the collector's browser allow-list delivers | A pruned key leaves a panel silently empty | The allow-list is DERIVED (`_browser_loki_label_regex()` imports `test_collector_base_config._record_allowlist_regexes`), not restated | `test_browser_logql_reads_only_keys_the_collector_delivers`, with `assert seen` so an empty scan cannot pass, and `test_logql_label_extraction_covers_every_stage_form` self-testing the extractor | not recorded | 2 | F2 | ASSERTED (strong — derived source + extractor self-test + non-vacuity assert) |
| 15 | copilot-mro-obsm | `datasources.yml:51`; `test_grafana_dashboards.py:302-313` | The four exact-spend/product panels ride `flynapse-postgres` as `flynapse_readonly` | Metered counters have no incompleteness companion; spec §7.1 forbids a USD tile without one | `provision_rls.py:262` + `tests/unit/db/test_readonly_role_grants.py` confirm the role is `NOSUPERUSER BYPASSRLS` | `test_flynapse_postgres_datasource_is_declared_kept_and_used` (asserts the user and that the four panel ids survive) | not recorded | 2 | F1 | ASSERTED — **note: the role deliberately BYPASSES RLS, so Grafana reads every tenant's spend. Guarded on the role side; the grant SCOPE reachable from Grafana is not guarded** |
| 16 | copilot-mro-obsm | `llm-agents.json:203,224`; `frontend.json:123,139`; `CATALOGUE.md:341` | The four Postgres panels claim LIVE as `LIVE (Phase 5 grants)` / `LIVE at Phase 5` | Phase 5 granted the reads | — | **none**: these panels query no inventoried metric and no `browser.*` token, so both lints skip them | not recorded | 1 | F3 | **OPEN** — an undated, probe-less LIVE, which is the one claim `_emitted_series.py:76-77` says must never be made |
| 17 | copilot-mro-obsm | `test_alert_rules_layout.py:405-452` (`OSS_THRESHOLDS`) | Every alert's severity, `for:`, comparisons, windows and quantile are pinned in an independent table | A threshold that drifts from the plan silences or floods an on-call | The table is hand-written in the test, NOT derived from the rule files — so it is a real second source | `test_every_threshold_matches_the_pinned_table` (+ `test_guarded_thresholds_state_their_guard`) | not recorded | 1 | F2 | ASSERTED (strong — genuinely independent) |
| 18 | copilot-mro-obsm | `flynapse-api-alerts.yml:26-31`; `test_alert_rules_layout.py:741-864` | `ApiP95LatencyHigh` excludes exactly the two gateway streaming/agent routes | A streamed turn's HTTP duration is the whole agent turn plus its save | The exclusion list is DERIVED from the gateway's real route table by AST, and a promtool run proves a sibling route sharing the prefix still pages | `test_p95_exclusions_are_exactly_the_gateway_agent_routes` + `test_promtool_p95_pages_only_busy_non_agent_routes` | not recorded, but the promtool test is a behavioural proof with a deliberately-adversarial sibling route | 1 | F2 | ASSERTED (strongest guard in the suite) |
| 19 | copilot-mro-obsm | agent/api/satellite rule files, denominator `(… > N)` | Ratio alerts guard the denominator inside the division so the ratio does not exist below volume | Division by a zero/near-zero denominator pages on one bad call | Read at source on all four ratio alerts; each guard value is pinned in `OSS_THRESHOLDS` and demanded in prose by `test_guarded_thresholds_state_their_guard` | as above | not recorded | 1 | F2 | ASSERTED |
| 20 | copilot-mro-obsm | `test_alert_rules_layout.py:331-343` | Every alert's `runbook:` annotation is `alerts.md#<lowercased alert name>` and a matching heading exists | A runbook link that 404s wastes an on-call's first minute | Read at source | `test_every_alert_is_fully_annotated` + `test_every_alert_has_a_runbook_anchor` | not recorded | 1 | F2 | ASSERTED — **one direction only**: an orphan runbook heading for a deleted/renamed alert is not caught |
| 21 | copilot-mro-obsm | `flynapse-agent-alerts.yml:38-53` | `LedgerWriteFailures` ships enabled on a series that cannot fire | Better to author it now and wire Task R later than forget it | Inventory row 8; the description says so plainly | `signal_state_offenders` forces the `DARK until ` note | not recorded | 1 | F1 | ASSERTED |
| 22 | copilot-mro-obsm | `flynapse-api-alerts.yml`, `flynapse-platform-alerts.yml`, `flynapse-satellite-alerts.yml` (whole files) | The three-state vocabulary is NOT applied to api/platform/satellite alert descriptions | Their series lie outside the inventory's three prefixes | `signal_state_offenders` returns no references for these, so no note is demanded | none | not recorded | 1 | F3 | **OPEN** — the "every alert description" claim holds only for `flynapse-agent-alerts.yml` |
| 23 | copilot-mro-obsm | `flynapse-platform-alerts.yml:1-8`; `platform-health.json:4` | `otelcol_*` internal-telemetry families are SUFFIXLESS (0.160.0 Prometheus reader) | A `_total` spelling would match nothing | Claimed as "live-verified on a 2026-09-05 scrape"; no artefact of that scrape is in the tree | none | not recorded | 1 | F3 | **OPEN** — rests on a one-time human observation |
| 24 | copilot-mro-obsm | `platform-health.json:157-200` | Loki/Tempo/Prometheus storage series spellings (`loki_distributor_*`, `tempo_distributor_*`, `prometheus_tsdb_head_series`) | Operators need ingest + cardinality trend | "live-verified" per the panel prose; not re-derivable from config | none | not recorded | 1 | F3 | OPEN |
| 25 | copilot-mro-obsm | `tempo.yaml:58-64`; `test_tempo_span_metrics.py` | Span-metrics dimensions are exactly `db.system, peer.service, server.address, rpc.service, url.template`, and no per-span/identity attribute may be one | `http.response.status_code` or `tenant.id` as a dimension multiplies every series | `FORBIDDEN_DIMENSIONS` includes `tenant.id`, `enduser.id`, `session.id` | `test_span_metrics_dimensions_are_the_bounded_set`, `test_no_per_span_or_identity_attribute_is_a_dimension`, `test_every_board_label_on_span_metrics_is_emitted` | not recorded | 2 | F1 | ASSERTED (tenancy-relevant: identity attributes are forbidden as dimensions) |
| 26 | copilot-mro-obsm | `tempo.yaml:86`; `flynapse-platform-alerts.yml:72-88` | Generator cap 100 000 active series, alert at 80 % of it | Past the cap Tempo folds new series into `metric_overflow="true"` silently | The alert threshold is computed from the cap in the guard, so a cap change that leaves the threshold behind fails | `test_metrics_generator_active_series_are_capped` + `test_series_cap_alert_fires_at_80_percent_of_the_cap` | not recorded | 1 | F2 | ASSERTED (good — threshold is derived, not restated) |
| 27 | copilot-mro-obsm | `agent-turn-explorer.json:66`; `dependencies.json:25,41,57,73`; `frontend.json:109,114,161`; `telegram-bot.json:252` | Tempo span-metrics families are `traces_spanmetrics_latency_*` / `traces_spanmetrics_calls_total` | Classic-histogram spellings for the pinned Tempo 3.0.3 | Consistent with `tempo.yaml`'s own arithmetic comment ("14 latency buckets + +Inf, sum, count, calls, size"); no `histogram: native` is configured | The tempo guard checks LABELS only, never the family NAME | not recorded | 1 | F3 | OPEN (family name unguarded; plausible-correct by config reading — a native-histogram flip would empty every one of these with no test failing) |
| 28 | copilot-mro-obsm | `shift-optimizer.json`, `telegram-bot.json`, `service-overview.json` | Satellite/API metric names and label values (`optimizer_runs_total{status="failed"}`, `telegram_turns_total{outcome!~"answered\|clarification"}`, `auth_rejections_total`, `http_server_*`) | Phase 8 / phase 1 emitters | **Verified by hand this review**: `shift-optimizer/…/run_telemetry.py:32-68` (names, units, `RUN_STATUSES`), `telegram-bot/…/chat.py:950-960` (all 11 `Outcome` literals), `api/…/middleware/telemetry.py:34` (`auth.rejections`), `api/…/telemetry/http_server.py:9,174` | none — these names are outside `FAMILY_TOKEN` | not recorded | 1 | F3 | **OPEN** (verified correct by hand; nothing would catch a rename) |
| 29 | copilot-mro-obsm | `shift-optimizer.json:213` | Failed optimizer runs ARE selectable by intrinsic TraceQL `status = error` (unlike agent turns) | The executor sets it explicitly | `run_telemetry.py:161` `self.span.set_status(Status(StatusCode.ERROR, …))` — confirmed, so the selector works and the asymmetry with row 7 is real, not a mistake | none | not recorded | 2 | F3 | OPEN (verified correct by hand) |
| 30 | copilot-mro-obsm | `flynapse-platform-alerts.yml:90-110` | `AlertmanagerNotificationsFailing` uses `for: 10m` equal to its `[10m]` lookback, deliberately | A broken integration re-fails on every 5 m `group_interval` flush, so the window always holds the next failure; a one-off recovered failure leaves before the hold ends | The reasoning is written into the rule comment, and a guard keeps the window and `group_interval` in step | `test_alertmanager_config.py` / `test_alertmanager_delivery_failures.py` (present; the specific window↔`group_interval` assertion not read in full) | not recorded | 1 | F2 | ASSERTED |
| 31 | copilot-mro-obsm | `flynapse-agent-alerts.yml:23-37` | `UnpricedModelCalls` uses `for: 30m` equal to its `[30m]` lookback, with NO recurrence argument | Intent is "sustained", per the description | The pattern is pinned in `OSS_THRESHOLDS`, so drift is caught — but no rationale is recorded, unlike row 30 | `test_every_threshold_matches_the_pinned_table` | not recorded | 1 | F3 | ASSERTED (pinned) / defensibility OPEN — see finding P2-2 |
| 32 | copilot-mro-obsm | `CATALOGUE.md` (whole file); `test_grafana_dashboards.py:263-276` | CATALOGUE.md is the shared spec governing BOTH the oss and aws profiles | One spec, two renderings | — | `test_catalogue_and_dashboards_agree` compares **board UIDs only** | not recorded | 1 | F2 | **OPEN at panel grain** — the catalogue's per-panel state column can contradict the board and nothing notices; confirmed contradiction at `CATALOGUE.md:367` vs `frontend.json:359` |
| 33 | iac | `iac/dashboards/*.tftpl` (8 files) | The cloud boards mirror the local ones | Two profiles, one spec | **Contradicted**: three boards spell application metrics in the oss Prometheus dialect (`telegram.turns_total`, `optimizer.runs_total`, `http.server.request.duration_count`) while `alarms.tf:197,209` in the SAME repo selects `{"telegram.turns"}` / `{"optimizer.runs"}`; `CATALOGUE.md:67-72,493-494` mandates the unsuffixed dotted form | `iac/scripts/validate_dashboards.sh` — **JSON shape only** (widget type, non-empty `properties`); never reads a metric name, namespace, dimension or signal state | not recorded | 1 | F3 | **OPEN — and wrong** (cloud finding P0-1) |
| 34 | iac | `iac/alarms.tf` | The cloud alarms mirror the local rules, incl. thresholds and `treat_missing_data` | Two profiles, one threshold table (`CATALOGUE.md:580-583` maps them) | **Thresholds verified: all 18 alarms agree with their oss rule** on value, window, volume guard, operator and severity; both coverage directions documented via `local.oss_only_alerts` | `iac/scripts/validate_alarms.py` — real and scoped: coverage vs §2.1, thresholds, guards, windows, operators, severity routing, arming gates. `_query_failures()` (`:510`) parses only `local.alert_thresholds.*` refs — **never a metric name** | not recorded | 2 | F3 | ASSERTED for the NUMBERS; **OPEN for metric names** |
| 39 | iac | `alarms.tf:96,98,110,115` vs `:164,174,181,188,197,209` | Metric naming inside `alarms.tf` itself | One convention per backend | **Self-contradictory**: unsuffixed counters (`{"agent.turn.calls"}`, `{"telegram.turns"}`) beside suffixed histograms (`{"http.server.request.duration_count"}`), while the file's own header `:28-34` argues "it cannot do both, so at most one convention is right as written". `CATALOGUE.md:573-574` specifies the unsuffixed form | `validate_alarms.py` does not inspect metric names | not recorded | 2 | F3 | **OPEN — and internally inconsistent** (cloud finding P1-7); if the `_count` spelling is wrong, `ApiHighErrorRate` — the estate's critical 5xx pager — is inert |
| 40 | iac | `alarms.tf:238-245`, `:352-375` | `AutomationRunErrors` ships ungated with `treat_missing_data = "notBreaching"` | Log-metric-filter alarms default to not-breaching | **Wrong**: the filter targets `service.name = "automation-worker"`, which `alarms.tf:44` and `dev.tfvars:128` both state iac never deploys. The sibling `AutomationWorkerSilent` IS gated on `automation_worker_deployed` (`:296`) for exactly this reason | `validate_alarms.py` pins `ALARM_MAPS` / `LOG_FILTER_FOR_EACH` but never checks the gate against the filter's target service | not recorded | 1 | F3 | **OPEN — and wrong** (cloud finding P0-2): the alarm reports OK forever on a service that does not exist |
| 41 | iac | `dashboards/llm-agents.json.tftpl:19-22` | Bedrock throttles are the one aws-only panel | `CATALOGUE.md:183-185`; no oss counterpart and no throttle alert exist anywhere | **Wrong twice**: `ThrottledCount` is not an `AWS/Bedrock` metric (it is `InvocationThrottles`, per this estate's own research file `docs/plans/observability-rebuild-research/05-llm-observability-options.md:346-347`), and the widget carries no `ModelId` dimension — the only dimension Bedrock publishes on | none | not recorded | 1 | F3 | **OPEN — and wrong** (cloud finding P1-3); the wrong name propagated into `CATALOGUE.md:184` and `aws-profile.md:13` |
| 42 | iac | `dashboards/platform-health.json.tftpl:12-19`, `:25`, `:35` | Cloud platform-health charts collector export errors and the automation worker | Mirrors the oss board | **Wrong**: the collector widget `SOURCE`s `/flynapse/<env>/otel`, which `backend-aws.yaml:98-131` shows carries only exported OTLP logs, never the collector's own stdout (that goes to `/flynapse/<env>/otel-gateway`); and the two worker widgets chart a service iac never deploys, one titled "zero = silent" | none | not recorded | 1 | F3 | **OPEN — and wrong** (cloud findings P1-4, P1-5) |
| 43 | iac | `dashboards/agent-turn-explorer.json.tftpl:15-17` | A log widget selects failed agent turns by `@message like /(?i)invoke_agent/`, labelled WIRED | Cloud has no Tempo, so a log proxy stands in | **Wrong**: `invoke_agent` exists only as a span NAME and a span-attribute VALUE (`telemetry.py:1499,1502`); nothing logs the string, and `@message` is the OTLP log body | none | not recorded | 1 | F3 | **OPEN — and wrong** (cloud finding P1-6): an empty widget wearing a WIRED label, which `CATALOGUE.md:35-38` reserves for a reached call site |
| 35 | copilot-mro-obsm | `flynapse-api-alerts.yml:2`; `flynapse-agent-alerts.yml:5` | Threshold rationale lives in `docs/plans/observability-rebuild-phase-6-dashboards.md §10.2` | Points a tuner at the reasoning | **False**: the file has no `10.2` anywhere, and the path does not resolve from the repo the rule file lives in (the plan is at workspace `docs/plans/`, not `copilot-mro-obsm/docs/plans/`) | none | not recorded | 1 | F3 | **OPEN — and wrong** (finding P2-1) |
| 36 | copilot-mro-obsm | `test_emitted_series_inventory.py:203-228` | The two construction paths (`__init__` via Meter, `from_current_provider` via registry) must declare the same instruments | Which path runs depends on how the facade was built | Compares `(attr, name, kind)` across paths, plus a unit-clash check | `test_both_construction_paths_declare_the_same_instruments`, with `assert meter` so an AST-shape change cannot silently empty it | not recorded | 2 | F1 | ASSERTED — **partial**: units are compared only where the Meter path spells one, so the six counters declared without `unit=` have no cross-path unit check |
| 37 | copilot-mro-obsm | `test_emitted_series_inventory.py:120-139` (`_call_sites`) | "Production call site exists" is decided by AST, not grep | `# TODO: re-enable telemetry.record_turn` satisfies a grep and would leave a dead series inventoried as WIRED | The rationale is written into the docstring; the scan is `ast.Attribute` with a text prefilter | `test_a_recorder_has_a_production_call_site_exactly_when_the_series_is_not_dark` | not recorded | 2 | F1 | ASSERTED — **partial**: any attribute named e.g. `record_turn` anywhere under `copilot_mro/` counts, regardless of receiver type or whether the module is reachable |
| 44 | copilot-mro-obsm | `docs/runbooks/observability/oss-profile.md:137-138`, `:145` | The three Alertmanager delivery secrets "exist in every checkout that has run the switch", each holding a `REPLACE_THIS_LINE_WITH_…` placeholder to be replaced | Gives the owner a single edit-in-place step | **False, and refuted by this layer's own guard**: `deployment/observability-local/alertmanager/secrets/` holds only `.gitignore` (body `*` + `!.gitignore`), and `test_alertmanager_secrets_dir.py::test_only_the_inner_gitignore_is_tracked_under_the_secrets_dir` asserts `tracked == [".gitignore"]` and `committable == []` — i.e. it PROVES the three files cannot be in a checkout. The string `REPLACE_THIS_LINE_WITH_` occurs exactly once in the repo: in this runbook line | `test_alertmanager_secrets_dir.py::test_the_runbook_sends_the_owner_to_the_ignored_directory` — checks only that the runbook NAMES the directory and the three filenames | not recorded | 1 | F3 | **OPEN — and wrong** (runbook finding P0-R1). The guard is the clearest example in the layer of a check that would still pass if the property it names were removed: it verifies vocabulary presence, never claim truth |
| 45 | copilot-mro-obsm | `docs/runbooks/observability/alerts.md:86`, `:270` | `AutomationWorkerSilent` and `AutomationRunErrors` triage points at "the runbook SQL automation check" | Cross-checking `automation_runs` against the log stream is the whole triage for a silent worker | **Dangling**: exactly one ```sql``` block exists in the whole runbook set (`oss-profile.md:55-62`, the `llm_usage` spend query). The sibling phrase "the runbook SQL *ledger* check" resolves to it; the *automation* check resolves to nothing | none | not recorded | 1 | F3 | **OPEN — and wrong** (runbook finding P1-R1) |
| 46 | copilot-mro-obsm | `docs/runbooks/observability/oss-profile.md:194` | "The provisioning smoke proves the six boards LOAD" | Phase-close manual verification step | **Wrong — there are eight**: `test_grafana_dashboards.py:67-77` `EXPECTED_UIDS` lists 8, `test_grafana_provisioning_smoke.py:70` asserts all 8 load, and `aws-profile.md:5,44` correctly say "eight". The two uncounted boards are `fn-telegram-bot` and `fn-shift-optimizer` — the boards behind the two satellite alerts | The guard counts 8; the runbook prose is not checked against it | not recorded | 1 | F3 | **OPEN — and wrong** (runbook finding P1-R2) |
| 47 | copilot-mro-obsm | `alerts.md` headings vs all 5 rule files + `iac/alarms.tf` | Every alert's runbook anchor resolves, and no runbook section is orphaned | A 404 link wastes an on-call's first minute | **Verified CLEAN both directions this review**: all 19 Prometheus+Loki `runbook:` annotations and all 17 `iac/alarms.tf` `Runbook:` references resolve; the only two heading-without-rule sections are the documented aws-only alarms | `test_every_alert_has_a_runbook_anchor` — **one direction only** | not recorded | 1 | F2 | ASSERTED (clean today; the orphan direction is unguarded) |
| 48 | copilot-mro-obsm / iac | `aws-profile.md:31`; `flynapse-api-alerts.yml:2`; `flynapse-agent-alerts.yml:5`; `iac/alarms.tf:21` | Plan references are written as `docs/plans/…`, repo-relative in form | Consistency with every other path in these files | **Broken**: the observability plans live at WORKSPACE `docs/plans/`, not `copilot-mro-obsm/docs/plans/` — and that directory exists and holds ~90 unrelated plans, so the reference resolves to a real directory with the file missing. Worst case for a reader: it looks deleted | none | not recorded | 1 | F3 | **OPEN — and wrong** (runbook finding P1-R3 + my row 35) |
| 38 | copilot-mro-obsm | `frontend.json:4` | The fn-frontend board header declares browser panels carry LIVE **or** DARK | Written before WIRED existed for browser signals | **Contradicted by its own panel**: `frontend.json:359` now carries WIRED | none (board-level `description` is not linted) | not recorded | 1 | F3 | **OPEN — and wrong** (finding P2-3) |

---

## Counts

48 rows.

**By tier** — Tier 2: 16 (rows 1, 2, 3, 5, 6, 7, 9, 11, 14, 15, 25, 29, 34, 36, 37, 39) ·
Tier 1: 32 · **Tier 0: 0** — nothing in this layer is mutation-proved to the Tier-0 bar.

**By claim state** — SETTLED: 1 · ASSERTED: 21 · OPEN: 26.
Of the 26 OPEN rows, **12 are not merely unguarded but demonstrably WRONG** (rows 16, 33, 35, 38,
39, 40, 41, 42, 43, 44, 45, 46, 48 — and rows 12 and 32 carry confirmed stale claims of their own).

The single SETTLED row is row 11, and only weakly: the mutation is *described* in a code comment
(`_emitted_series.py:343`) and the failing test is not named.

**Tier-2 OPEN claims, by name**
1. **Row 5** — `tool_outcome="failure"` label VALUE. Verified correct by hand this review; nothing
   would catch a flip to `"error"`, which matches nothing.
2. **Row 6** — `agent_outcome="error"` label VALUE. Same: verified by hand, unguarded.
3. **Row 29** — the optimizer board's intrinsic TraceQL `status = error` selector. Correct
   (`run_telemetry.py:161` sets it), but it is the exact selector the agent board documents as
   *not* working, and nothing guards the asymmetry.
4. **Row 34 (metric-name half)** — `iac/alarms.tf`: `validate_alarms.py` pins every threshold and
   never inspects a metric name.
5. **Row 39** — `alarms.tf`'s two metric-naming conventions, which the file's own header argues
   cannot both be right.

*(Rows 15 and 25 are Tier 2 for tenancy reasons but are ASSERTED, not OPEN. Rows 36 and 37 are
Tier 2 and ASSERTED-with-a-named-partiality. Row 16 is Tier 1 by content but is the spend contract,
and is the one claim the vocabulary explicitly forbids — treat it with Tier-2 seriousness.)*

**Correction to the review brief.** The brief states the iac side has "no guard at all". That is
wrong in a way that matters: `iac/scripts/validate_alarms.py` is a real guard covering alarm
coverage, thresholds, guards, windows, operators, severity routing and arming gates — and every
threshold it pins agrees with the oss rules. What is unguarded on the cloud side is narrower and
sharper: **metric names, dimensions, log-group targets and signal state**. That is precisely where
all six cloud defects sit.

**Never written as `yes`.** No mutation proof is recorded anywhere in this layer except the one
in-tree note at `_emitted_series.py:343`. Every other cell reads `not recorded`, which is the
correct answer, not a gap in this review.

**What could not be checked without running the stack**
- Whether any query returns data (all eight boards). The only live proof in the tree,
  `test_oss_profile_smoke.py:851`, is `compose_stack`-marked and was not run.
- Tempo's actual span-metrics family names under the pinned 3.0.3 (row 27).
- The `otelcol_*` / `loki_*` / `tempo_*` spellings (rows 23, 24).
- Everything in `iac/` (rows 33, 34) — no plan/apply was run.
