# Claims packet — Phase C2 (api)

Assembled 2026-09-20 from `docs/plans/observability-telemetry-merge-and-completion.md` §3, §4e, §5.6, §7
and the SDD ledger `.superpowers/sdd/observability-telemetry-merge-and-completion/progress.md`
("PHASE C2 (api) — CLOSED"). This is the **backfill** file: C2 closed just before the rule that a phase
does not close without its claims table. Every `file:line` below was resolved in the worktree on the day
of assembly. Nothing was committed, edited or run against the tree.

| phase | repo | worktree | branch | range | merge commit | review-response commit |
|---|---|---|---|---|---|---|
| C2 | api | `/home/aditya/Code/api-obsm` | `obs-merge` | `4da716f..9812f44` | `37121a3` | `9812f44` |

Eight commits: `37121a3` (the merge, conflict resolution only), `55439b4` (C2.1/C2.2), `e77c730`
(C2.3 / M-WARN), `62b76e1` (C2.4), `ee6f4d0` (C1.5's first clause at api's site), `ab81a48`
(self-review), `6bb1b11` (C2.4 follow-through), `9812f44` (response to the adversarial reviewer).

Test lane, as recorded (shared `api` Poetry env, `DEBUG=false`, all four merged worktrees on
`PYTHONPATH`):

| pre-merge baseline | at head `9812f44` |
|---|---|
| **1134 passed / 4 skipped** | **1170 passed / 4 skipped / 0 failed / 0 errors** |

The set difference of failing ids is empty. **pyflakes 51 findings before and 51 after**, identical
modulo line numbers; ruff 70 → 71, the delta being one `E402` of the same kind as 24 already present.
The lane was **re-measured** after the reviewer pointed out that the first run had resolved `utils` and
`core` from their pre-merge checkouts — the figures above are the re-measured ones.

**Tier 0 is empty here too** (sixth file running). §2.3a's tier-0 category is "mechanical, additive,
doc-only **and** mutation-proved". Every mutation-proved guard in this range protects a decision that is
itself consequential — a privacy property, a tenant-isolation gate, an anonymous route's disclosure
surface — so nothing qualifies, and the additive/doc-only rows (`.env.example`, the plan corrections)
have no guard at all.

---

## Phase C2 — api, `4da716f..9812f44`

| # | File:line | Decision taken | Why | Evidence it is right | Guard test | Mutation-proved? | Tier | Chunk | Claim state |
|---|---|---|---|---|---|---|---|---|---|
| C2-1 | merge commit `37121a3` (parents `4da716f` ours, `6f22498` theirs) | Merge origin/obs-telemetry-merge into `obs-merge` as **resolution only** — no behaviour change in the merge commit itself; every item lands in a later commit | §2.2's ordering: api must not merge before copilot-mro, because api's `main.py` imports symbols that exist only on their copilot-mro branch, so api alone fails at import and the gateway will not boot | Re-measured at assembly: the merge touches **7 paths** relative to ours — `main.py` (both sides), and six theirs-only files. **No textual conflict is recorded for api**; both both-sides files are registered under §2.2a as *silent* merges, which is the register's whole point | none — a merge topology is not a testable property | not recorded | 1 | F2 | OPEN |
| C2-2 | `poetry.lock` (whole file), `pyproject.toml:94-95` | **M-LOCK: take OURS wholesale.** Discard theirs, including the header and the reordered marker lists git had spliced in | §5.6: "api's lock moves **zero** version pins and its content-hash predates our psycopg addition". Git's silent splice had produced a lock describing a resolution **no `poetry` run ever produced** — our content-hash over their Poetry-2.4.0 header | Premise verified, not assumed: `poetry check --lock` **FAILS on theirs** and **passes on ours** (plan §7). Re-verified at assembly: theirs is header `Poetry 2.4.0` / content-hash `2da3000d…`, ours `Poetry 2.3.2` / `b6bb54cc…`; the pin delta is entirely **ours-adds** — `flynapse-otel 0.1.1`, `flynapse-utils 0.1.39` (vs their 0.1.38), `opentelemetry-instrumentation-psycopg`, `psycopg`, `psycopg-binary`. `psycopg = {extras = ["binary"], version = "^3.3.5"}` is in the **dev** group at `pyproject.toml:95`, which is the addition their hash predates. `git diff 4da716f..9812f44 -- poetry.lock pyproject.toml` is **empty** — byte-identical to ours | **none.** The repo has no lock-consistency test; `poetry check --lock` was run by hand and its result recorded in prose | not recorded | 1 | F2 | OPEN |
| C2-3 | `flynapse_api/main.py:112-125` | **Both-sides silent merge, corrected.** Their partition-check block auto-merged into a region ours never touched; at `37121a3` the helper's return value was **discarded** (`run_weaviate_partition_startup_check(` at merged `:110`, no assignment). C2.3 assigns it to `app.state.degraded_components … or {}` | A clean auto-merge is not a reviewed merge. Theirs left the entire degradation as one log line — the fact that the process is serving with a known tenant-isolation hole reached nothing outside the log | Re-verified at assembly against `git show 37121a3:flynapse_api/main.py`: the call is present and unassigned at the merge, assigned at head | `tests/startup/boot_checks/test_weaviate_partition_startup_mode.py::test_the_gateway_startup_runs_the_real_partition_check` (:280) — asserts the **arguments** are the real check and the real mode reader, not stand-ins | not recorded for the argument identity (W4 covers the assignment; see C2-31) | 1 | F2 | ASSERTED |
| C2-4 | `flynapse_api/middleware/logging.py` (theirs-only) | Take the file, rewrite the failure line | **This is where the risk sat.** No api R22 guard swept this file — re-verified at assembly: at `4da716f` the repo's **only** `failure_fields` site was `main.py:43`/`:291`, so their `error=str(e)` + `traceback=format_exc()` would have merged with nothing to catch it | At head there are four `failure_fields` modules (`main.py`, `middleware/logging.py`, `middleware/auth.py`, `startup/weaviate_partitions.py`) and a repo-wide AST scan that names this file | `tests/middleware/telemetry/test_logging_context_middleware.py::test_the_failure_line_carries_frames_and_not_the_exception_text` (:92) + the repo scan `tests/unit/telemetry/test_log_messages_are_not_format_strings.py::test_no_gateway_module_hands_loguru_an_interpolated_format_string` (:164) | **yes** — M3 (`error_type=type(e).__name__`, no frames → the R22/frames test fails) and M4 (plant a violation in `run.py` → the repo scan fails, naming it), both in `55439b4` | 1 | F2 | SETTLED |
| C2-5 | `flynapse_api/startup/weaviate_partitions.py` (theirs-only, new file) | Take the helper and its narrowing; replace its `error_type=type(error).__name__` with `failure_fields(error)` | A **new** file using the exact P0-GUARD anti-pattern, in a file no sweep lists. P0-GUARD is assigned to copilot-mro (D.8); nothing in api would ever have reached this | The line at `:90-99` now binds `**failure_fields(error)`, and the guard asserts both `error_type` and a real `stack` | `tests/startup/boot_checks/test_weaviate_partition_startup_mode.py::test_the_warning_carries_the_errors_own_remediation_and_no_refusal_text` (:203) — asserts `attributes["error_type"] == "WeaviatePartitionError"` **and** `"weaviate_partitions.py" in attributes["stack"]` | **yes** — W7 (`error_type` instead of `failure_fields` → the frames/R22 assertion fails), `e77c730` | 2 | F2 | SETTLED |
| C2-6 | `flynapse_api/startup/__init__.py:1` (theirs-only, new file) | Accept a new **bare top-level module name** `startup` on the gateway's import path | A one-line package docstring, but the name is unqualified and the gateway imports `core`, `copilot_mro`, `utils`, `telemetry`, `middleware`, `routers` and `config` the same way | Plan §7: "checked for collision, none" | **none** — the collision check was done by hand and never encoded. Nothing fails if a sibling repo later ships its own top-level `startup` | not recorded | 1 | F2 | OPEN |
| C2-7 | `tests/startup/boot_checks/test_startup_boot_check.py:151-285` (theirs-only edit) | **Our** guard, edited by them alone, and rewritten here | They relaxed `_guarded_calls` so a handler containing any top-level `raise` scored clean. A guard nobody on our side reviewed, changed inside an observability merge, over a tenant-isolation gate | The relaxed predicate is refuted by construction: `logger.error(...); return; raise` and the conditional-return spelling both scored **0 offenders** under it | `::test_swallowed_boot_check_detector_rejects_a_return_that_outruns_the_raise` (:324), `::test_swallowed_boot_check_detector_rejects_a_break_that_outruns_the_raise` (:359) | **yes** — G1 (revert `_handler_reraises` to `any(isinstance(stmt, ast.Raise))` → fails exactly those two cases and nothing else), `62b76e1` | 1 | F2 | SETTLED |
| C2-8 | `tests/startup/boot_checks/test_weaviate_partition_startup_mode.py` (theirs-only, new file) | Keep the file; replace its exact-log-tuple assertion | Theirs asserted an exact log tuple **containing `error_type`**, which would have **locked in** the R22 regression — restoring frames-only would then have read as breaking a test | The rewritten assertion pins `error_type` *and* `stack` *and* the absence of the refusal text (`REFUSAL_TEXT not in repr(message) + repr(attributes)`) | `::test_the_warning_carries_the_errors_own_remediation_and_no_refusal_text` (:203) | **yes** — W7, `e77c730` | 2 | F2 | SETTLED |
| C2-9 | `tests/middleware/telemetry/test_logging_context_middleware.py` (theirs-only, new file) | Keep the file and add the assertion it lacked. In practice their single `test_exception_logging_preserves_errors_with_mapping_text` was **replaced** by three cases over a captured-record fixture | Theirs proved the `KeyError` was gone; it never asked whether the exception's text reached a sink, which is the R22 half | The new R22 case asserts over the **whole record** (`message` + `extra` + `exception`), not over the fields this line happens to bind, so a field added back is caught | `::test_a_brace_bearing_exception_is_re_raised_as_itself` (:84), `::test_the_failure_line_carries_frames_and_not_the_exception_text` (:92), `::test_the_identity_context_still_rides_on_the_failure_line` (:117) | **yes** — M2 (revert to THEIR shape → the re-raise test and the R22 test fail), M3, `55439b4` | 1 | F2 | SETTLED |
| C2-10 | plan §3, C2 preamble | Correct the plan: §3 says their `main.py` imports **five** symbols that exist only on their copilot-mro branch; measured, **four** | A premise nobody re-measures becomes a fact | Re-verified at assembly against `e26be7dd^1:copilot_mro/app/services/weaviate_boot_check.py` (our pre-merge copilot-mro): **zero** matches for `partition_boot_check_mode`, `PARTITION_REFUSAL_ERROR`, `PARTITION_REFUSAL_REMEDIATION`, `WEAVIATE_PARTITION_BOOT_CHECK_MODE_ENV` — exactly four, with `assert_partitions_provisioned` and `WeaviatePartitionError` already present | none — a plan correction carries no guard | not recorded | 1 | F2 | OPEN |
| C2-11 | `flynapse_api/middleware/logging.py:103` | **C2.1/C2.2.** Take their `logger.bind(...)` mechanics; **reject their message.** Constant `"Request failed"`, `duration` and `**failure_fields(e)` bound; `import traceback` dropped | Two rules meet here. (a) Loguru runs `message.format(*args, **kwargs)` whenever a call carries fields, so an already-interpolated message plus structured fields makes the **exception's own text** a format string — a Pydantic error renders `{'type': 'string_type', …}` and `format` raises `KeyError` *inside the handler*, so the request's real exception never reaches the `raise`. (b) R22: type and frames, never the text. Their fix kept the text as a `{}` argument, which buys back the same re-raise | **The defect was reproduced before it was fixed**, against the pinned loguru, not taken on faith. This line runs for every 500 on every route, and a request's exception message quotes whatever the request carried | `::test_a_brace_bearing_exception_is_re_raised_as_itself` (:84) and `::test_the_failure_line_carries_frames_and_not_the_exception_text` (:92) in `tests/middleware/telemetry/test_logging_context_middleware.py` | **yes** — M1 (revert to the original f-string+kwargs → 2 of 3 middleware tests fail), M2, M3, `55439b4`. *M1 is partially recorded: a count, not a name* | 2 | F1 | SETTLED |
| C2-12 | `flynapse_api/main.py:520` | Same fix at the **global exception handler**, a site **their diff never touched** | The `KeyError` replaces the JSON 500 with an unhandled one — the last thing standing when everything else has failed | The site was found by the repo-wide scan, not by a failing test; `REPAIRED_SITES` at `tests/unit/telemetry/test_log_messages_are_not_format_strings.py:161` names `main.py` so the scan cannot pass by not reaching it | the repo scan `::test_no_gateway_module_hands_loguru_an_interpolated_format_string` (:164) | **indirect only** — M5 reverted the *auth* site and the scan failed; F3b proved the scan reaches `main.py`. **No mutation reverted `main.py:520` itself** | 2 | F1 | ASSERTED |
| C2-13 | `flynapse_api/middleware/auth.py:530-531` (import at `:27`, `traceback` dropped at `:8`) | Same fix at the **auth middleware's unhandled-exception handler** — `logger.bind(session_id=…, **failure_fields(exc)).error("Unhandled exception behind the auth middleware")` | This is the handler that builds the **CORS-decorable 500 the dashboard depends on**. The `KeyError` escaped before the crafted 500 was built, so the response that exists to carry CORS headers to the browser was never returned — and the population it happened to is exactly the one most likely to reach here: requests whose payloads failed validation | The guard drives a downstream raise whose message is a literal Pydantic-shaped dict and asserts a 500 with `error == "internal_error"` and `"string_type"` absent from the body | `tests/middleware/auth/test_downstream_failure_is_not_an_auth_failure.py::test_a_brace_bearing_exception_still_gets_the_500_rather_than_killing_the_handler` (:137) | **yes** — M5 (revert the auth handler's line → the new 500 test AND the scan fail), `55439b4` | 2 | F1 | SETTLED |
| C2-14 | `tests/unit/telemetry/test_log_messages_are_not_format_strings.py:137-179` | Pin the fix with a **repo-wide AST guard** rather than with three assertions — and **deliberately NOT** as an R22 check | Three assertions pin three lines; the defect is a shape. Scoping: whether a line may carry exception text *at all* is the estate-wide sink-level question (B-R1), and dozens of gateway sites still do — a guard that conflated the two would have been unshippable here | The scan sees through `bind`/`opt`, allows both safe shapes (interpolated with no fields; a real placeholder bound to a real value), and finds **zero** offenders in `flynapse_api/` | `::test_no_gateway_module_hands_loguru_an_interpolated_format_string` (:164), `::test_the_detector_sees_every_spelling_of_the_shape` (:181) | **yes** — M4 (plant a violation in `run.py` → the repo scan fails, naming it), `55439b4` | 1 | F1 | SETTLED |
| C2-15 | `tests/unit/telemetry/test_log_messages_are_not_format_strings.py:68` (`_runtime_text`), `:116` (`_names_holding_runtime_text`) | **Self-review + review response.** Widen the detector from `JoinedStr` only to `"…" + str(exc)`, `"…%s" % exc`, `"…{}".format(exc)`, implicit concat — then to **names assigned runtime text anywhere in the module**. Deliberately do **not** flag a bare name that holds a constant | The three string idioms are what somebody reaches for after being told not to f-string; `message = f"failed: {exc}"` on one line and `logger.error(message, code=1)` on the next is the same defect with an `ast.Name` in front of it, and it is the natural next move. Flagging a module-level message constant would push people back to inlining | Widened, the scan still finds **zero** offenders in `flynapse_api/`, so none of the four shapes exists today either | `::test_the_detector_sees_every_spelling_of_the_shape` (:181) — the rule stated as code so it cannot drift into "flags nothing" | **yes** — F1 (drop name-assignment tracking → the spellings case fails), `9812f44`; the four-idiom widening itself is from `ab81a48` and has **no itemised mutation** | 1 | F1 | SETTLED |
| C2-16 | `tests/unit/telemetry/test_log_messages_are_not_format_strings.py:222` (`aliases_in`), `:249` | Guard the scan's **own assumption**: its receiver test is the name `logger`, so assert no gateway module rebinds the loguru logger — by import alias **or** by assignment, including `self._log = logger.bind(component=…)` | An assumption nothing checks is the hole the scan reads as cleanliness. The alias check initially saw only `log = logger`, not the spelling that looks like good practice | It holds today: all 20 sites are `from loguru import logger` | `::test_no_gateway_module_rebinds_the_logger_under_another_name` (:249), `::test_the_alias_detector_sees_every_spelling` (:268) | **yes** — F2 (alias check on `ast.Name` only → the alias-spellings case fails), `9812f44` | 1 | F1 | SETTLED |
| C2-17 | `tests/unit/telemetry/test_log_messages_are_not_format_strings.py:161`, `:176-178` | Replace a **population count** vacuity guard with **named sites**: `REPAIRED_SITES = ("middleware/logging.py", "middleware/auth.py", "main.py")` must each appear in the scanned set (the `> 40` count is kept beside it) | "A population count says how many files were opened, not which." The count would have survived the scan quietly dropping 26 modules including all three repaired ones | The assertion is per-site and names the site in its message | `::test_no_gateway_module_hands_loguru_an_interpolated_format_string` (:176) | **yes** — F3b (make the scan skip `middleware/` → the repaired-sites assertion fails), `9812f44`. *Note: the label series records `F3b` with no `F3`* | 1 | F1 | SETTLED |
| C2-18 | `flynapse_api/startup/weaviate_partitions.py:74-79` | **M-WARN clause 1.** `warn` is **REFUSED** when `settings.is_production`, not silently downgraded to strict — and the refusal fires **before** the check runs | P0-PARTITION: a skip flag was added to a tenant-isolation boot check inside an observability phase. A deployment carrying this variable is misconfigured whether or not its partitions happen to be complete today, so refusing only on the day they are short is refusing on the wrong day. `settings.debug` is the gateway's **existing** environment signal — what `setup_logging` already labels the process with — rather than a new one invented here | The guard asserts the check was **not run** (`checked is False`) on the refusal path, which is what makes "before, not after" a property instead of a comment. Local `.env` is `DEBUG=True`, so the mode stays available exactly where it is meant to be | `tests/startup/boot_checks/test_weaviate_partition_startup_mode.py::test_warn_mode_is_refused_in_a_deployed_environment` (:121); the converse `::test_a_deployed_environment_still_boots_with_the_variable_unset` (:144) keeps the gate on the mode, not the environment | **yes** — W1 (drop the deployed-environment gate → the refusal test fails), `e77c730` | 2 | F1 | SETTLED |
| C2-19 | `flynapse_api/startup/weaviate_partitions.py:84-85` | Keep **their** narrowing: only `WeaviatePartitionError` degrades. A database error, a transport failure or a bug inside the check propagates even in `warn` | "Cannot check" is not "checked and degraded" — reading the first as the second is the defect family this whole effort keeps finding. `WeaviatePartitionError` is the type the check raises when it has **successfully measured** a real shortfall | An unknown mode value is still fatal (the check's own refusal, kept): a misspelling must not read as "no mode, carry on" | `::test_warn_mode_propagates_everything_that_is_not_a_partition_answer` (:236), `::test_an_invalid_mode_value_is_fatal` (:150), `::test_the_check_is_strict_when_the_variable_is_unset` (:104, driven through the **real** mode reader) | **yes** — W6 (widen the except to `Exception` → the "not a partition answer" test fails), `e77c730` | 2 | F1 | SETTLED |
| C2-20 | `flynapse_api/startup/weaviate_partitions.py:90-99` | The warn line logs the **error's own** `remediation` (which names the database it measured), falling back to the module constant `PARTITION_REFUSAL_REMEDIATION` (a `<database>` placeholder); and R22 — `**failure_fields(error)`, never `error_type=type(error).__name__` | The constant is a placeholder form; the operator reading the boot needs the database that was actually measured. And the refusal's **message** quotes the partition and collection names it found missing, which name the tenant and the operator — so the message may not reach a sink | Both properties are asserted on one record, including `REFUSAL_TEXT not in repr(message) + repr(attributes)` | `::test_the_warning_carries_the_errors_own_remediation_and_no_refusal_text` (:203), `::test_a_refusal_with_no_remediation_falls_back_to_the_constant` (:229) | **yes** — W2 (log the constant remediation → the remediation and returned-entry tests fail) and W7, `e77c730` | 2 | F1 | SETTLED |
| C2-21 | `flynapse_api/startup/weaviate_partitions.py:100-113` | **The adversarial reviewer's severest finding.** The degraded readiness entry carried the error's own `remediation`; it now carries `{"status": "degraded", "detail": PARTITION_REFUSAL_ERROR}` and **nothing else**. The remediation stays on the log line | `/health/ready` sits **outside `api_prefix` and is skipped by the auth middleware** — an anonymous route. `remediation` is `REMEDY.format(database=…)` where the database comes from `SELECT current_database()`, plus an internal script path. **Three lines above, the same function already strips `debug` for exactly this class of leak (G28)** — handing it a live database name would put one back. **The phase built the leak and the review caught it** | Asserted as an **EXACT dict**, because "a subset assertion would let that back in silently", plus explicit absence of the remediation text and of the database name | `::test_warn_mode_returns_the_degraded_component_for_the_readiness_body` (:174) | **yes** — P1 (put `remediation` back in the entry → the exact-entry case fails), `9812f44` | 2 | F1 | SETTLED |
| C2-22 | `flynapse_api/main.py:118-125`, `flynapse_api/routers/health.py:105-107`, `:92` | **M-WARN clause 3.** The helper's return value is the `/health/ready` entry (not a bool); `main.py` records it on `app.state.degraded_components` (a **mapping**, so a second degradable boot step has somewhere to go); `/health/ready` folds it in, lowering `healthy` → **`degraded`**, still answering **200** | The aggregate probe measures the stores this process **talks to** and can never see a boot check it was allowed to continue past — so without this the body says `healthy` about a process that knows it is not. Not `unhealthy`: the verdict still returns 200, which is the whole point of a mode that exists so the process can be worked with. "A tenant-isolation gate may be locally deferred, but it may not be quietly deferred" | The clean case is pinned separately so a clean boot cannot read as degraded, and the no-lifespan case (a `TestClient` stack without one is a real configuration) must not 500 | `tests/api/health/test_health_probes.py::test_a_degraded_boot_component_reaches_the_readiness_body` (:114), `::test_a_clean_boot_leaves_the_readiness_body_exactly_as_the_probe_returned_it` (:142), `::test_readiness_survives_an_app_that_never_ran_the_startup_hook` (:156); `tests/startup/boot_checks/test_weaviate_partition_startup_mode.py::test_warn_mode_returns_the_degraded_component_for_the_readiness_body` (:174), `::test_a_clean_check_returns_nothing_to_report` (:199) | **yes** — W3 (return `None` on the warn path → the degraded-component test fails), W5 (drop the merge in `/health/ready` → the readiness body test fails), H3 (drop the merge from the route → all three readiness cases fail) | 2 | F1 | SETTLED |
| C2-23 | `flynapse_api/routers/health.py:58-75` | **Self-review + review response.** `merge_degraded_components` is **total on BOTH arguments** — a non-mapping `services`, and a truthy non-mapping `degraded_components` | It runs **outside** the probe's `try`, so either shape would raise in the route body and **the one route that must never 500 would answer FastAPI's 500 instead of this module's contractual 503**. Neither is reachable from the code that writes them today; both are one careless assignment away | Asserted **both** at the function and **end to end through the route** — "the totality is only worth anything if the ROUTE survives it". Also pinned: an already-`unhealthy` aggregate is never promoted to `degraded` | `tests/api/health/test_health_probes.py::test_merge_degraded_components_rules` (:167), `::test_a_malformed_aggregate_body_does_not_500_the_readiness_route` (:193) | **yes** — H1 (revert to `body.setdefault("services", {}).update(...)` → both new malformed-body cases fail, `ab81a48`) and H2 (drop the `degraded` type check → the merge-rules case fails, `9812f44`) | 1 | F1 | SETTLED |
| C2-24 | `.env.example:194-206` | Document `WEAVIATE_PARTITION_BOOT_CHECK_MODE`: its default, what `warn` is for, that any other value is fatal, and — after the review — **that this file ships with `DEBUG=false` at line 4, so uncommenting the mode on its own refuses the boot** | P0-PARTITION's second half: the flag was "undocumented in every `.env`, compose and iac file". The `DEBUG` caveat was added because the block otherwise "offered a mode its own `DEBUG=false` forbids", read where someone would uncomment it | Re-verified at assembly: `.env.example:4` is `DEBUG=false`; the mode line is commented at `:206` | **none** — the repo has no `.env.example` guard of any kind (zero test files reference it) | not recorded | 1 | F1 | OPEN |
| C2-25 | `.env.example:186` (`WEAVIATE_GRPC_PORT=50051`), `:190` (`# WEAVIATE_GRPC_SECURE=`) | **C1.5's first clause, api's named site** (`ee6f4d0`) | C1 added both knobs to `utils.config` and documented them only in utils' README, recording the mirroring into every repo that deploys Weaviate as owed to later phases. Nothing is broken today — every `WEAVIATE_URL` in the estate is `http://` — the gap is that an operator facing a non-default gRPC port or a TLS-terminating proxy has no declared knob and no reason to know one exists. Weaviate's batch and query paths are gRPC, so the symptom is those calls failing against an HTTP endpoint that answers fine | Re-verified at assembly: `WEAVIATE_GRPC_PORT` / `_SECURE` now match in `api-obsm/.env.example` and nowhere else in `api-obsm`. **This contradicts claim C1-15 in `claims-C1-utils-B1-core.md`, which records "0 hits in `api-obsm`" as re-verified at assembly on the same day** — C1-15's sweep is stale with respect to `ee6f4d0`, and one of the nine owed sites is now closed | **none** | not recorded | 1 | F3 | OPEN |
| C2-26 | `tests/startup/boot_checks/test_startup_boot_check.py:179-190`, docstring at `:25-32` | **C2.4 — the deviation.** §5.6 says "Phase C2: **revert to the strict rule**." The relaxation was **KEPT** instead, and hardened | §5.6's premise — "nothing in the diff put `assert_rls_enforced` in a try" — is **true of their api diff and false of their copilot-mro branch**. The merged copilot-mro lifespan legitimately wraps the call to stamp `error.type` and an ERROR status on the startup span, then re-raises. Reverting to strict would have shipped a guard that fails the moment copilot-mro's merge reaches the mainline, over a wrapper that changes nothing about whether the process comes up. **A plan premise about one repo's diff does not transfer to the estate.** The later plan item (C2.4) is also the correct one | **Measured with both detectors** against the merged `copilot-mro-obsm/copilot_mro/app/main.py`: **1 offender under strict, 0 under relaxed.** Corroborated structurally at assembly — that lifespan's `postgres_rls` handler is `logger.error(...)` then `raise` at `:125-135`. The hardened detector additionally scores **0 swallowed calls across all five startup modules in the estate**, merged copilot-mro and merged core included | `::test_swallowed_boot_check_detector_allows_telemetry_wrappers_that_reraise` (:288) pins the allowance; `::test_the_boot_check_is_not_swallowed` (:604) runs it against all four `STARTUP_MODULES` including copilot-mro via `sibling_repo` | **not recorded for the deviation itself.** G1 proves the *hardening*; no mutation restores the strict rule and shows what it would break — the 1-vs-0 measurement is the whole evidence | 2 | F2 | ASSERTED |
| C2-27 | `tests/startup/boot_checks/test_startup_boot_check.py:155-190` | The real defect in their relaxation: `any(isinstance(stmt, ast.Raise))` is satisfied by a `raise` a `return` never reaches. `_handler_reraises` now requires an **unconditional top-level `raise` AND** no `return`/`break`/`continue` anywhere in the handler **outside a nested definition** | Both escape spellings read as fatal to a human and serve an unenforced database to the process; their detector scored both 0. `break`/`continue` are included for the retry-loop shape; the nested-definition exclusion is there so a callback's own `return` does not make the honest wrapper unwritable | Reproduced before fixing, both spellings, in the commit message | `::test_swallowed_boot_check_detector_rejects_a_return_that_outruns_the_raise` (:324), `::…_rejects_a_break_that_outruns_the_raise` (:359), `::…_still_allows_a_return_inside_a_nested_callback` (:377), `::…_rejects_a_conditional_raise` (:513, the half that was already right, kept so the rewrite could not lose it) | **yes** — G1, `62b76e1` | 1 | F2 | SETTLED |
| C2-28 | `tests/startup/boot_checks/test_startup_boot_check.py:193-218` | **C2.4 follow-through.** `try/except` is not the only way to discard an exception: add `contextlib.suppress` (`_suppresses`) and `return`-in-`finally` (`_finally_escapes`) | "A guard that knows one of the three ways to discard an exception reports the other two as cleanliness." Both were invisible to the **strict** rule this replaced too, so neither is a regression. Neither shape is in any tree today — they are there so adding one is a failing test rather than a green one | Balanced by a case asserting an **ordinary** `with` (a span, a lock, a file) is still clean, because the cheap version of this fix treats every `with` as a guard | `::test_swallowed_boot_check_detector_rejects_the_two_ways_to_swallow_without_an_except` (:398), `::test_swallowed_boot_check_detector_allows_an_ordinary_with_block` (:499) | **yes** — G2+G3 applied as one run (drop both limbs → fails exactly the new case and nothing else), `6bb1b11` | 1 | F2 | SETTLED |
| C2-29 | `tests/startup/boot_checks/test_startup_boot_check.py:221` (`_TRY_NODES`), `:224-241` (`_functions_reaching`) | **Review response.** Handle `except*` (`ast.TryStar`, a different node class, live on `python = "^3.11"`) and **one level of local-helper indirection** | The indirection shape is not hypothetical: **extracting the call into a helper is the refactor THIS PHASE performed** on the partition check, and `def _checked(): assert_rls_enforced(s)` + `try: _checked() except: log()` scored clean | Both closed; the detector still scores 0 across all five startup modules | `::test_swallowed_boot_check_detector_sees_except_star` (:459), `::test_swallowed_boot_check_detector_follows_one_level_of_indirection` (:426) | **yes** — G4 (drop `ast.TryStar` → the except* case fails), G5 (drop the indirection limb → the helper case fails), `9812f44` | 1 | F2 | SETTLED |
| C2-30 | `tests/startup/boot_checks/test_startup_boot_check.py:476-495` | Pin the **remaining limitation** — a helper in **another** module is not followed — with a test asserting it is **NOT** caught | "The guard's true coverage is legible instead of assumed." If the boot path ever grows a cross-module helper, this case is where the next person finds out it is not covered. One further knowing false positive is documented at `:243-251`: `except Exception: sys.exit(1)` scores as a swallow although `SystemExit` does kill the boot | The case asserts `_offenders(...) == 0` on an imported-helper shape | `::test_swallowed_boot_check_detector_cannot_follow_a_cross_module_helper` (:476) | not recorded — and the natural mutation here (making it follow cross-module) would *fix* the limitation, not break the guard | 1 | F2 | ASSERTED |
| C2-31 | `tests/startup/boot_checks/test_weaviate_partition_startup_mode.py:245-309` | Close a **theirs-only audit gap**: `assert_partitions_provisioned` is no longer **CALLED** in `main.py`, it is **PASSED**, so the repo's call-by-name boot guard cannot see it — and nothing in the suite asserted the gateway runs the Weaviate check at all. Two AST cases now pin the call site: the real check and the real mode reader are the arguments, and the answer is assigned to the name `/health/ready` reads | A tenant-isolation gate that the guard can no longer see is a gate nobody is guarding. As merged, the answer was discarded (see C2-3) | Both cases parse `main.py` **once** per call, because two parses produce equal trees of different objects and an identity test across them reads as "the call site is gone" — a guard failing for a reason unrelated to its subject | `::test_the_gateway_startup_runs_the_real_partition_check` (:280), `::test_the_gateway_startup_records_the_degraded_answer` (:292) — the latter asserts the assignment target is exactly `{"app.state.degraded_components"}` | **yes** — W4 (discard the answer at the call site → the call-site assignment guard fails), `e77c730` | 2 | F2 | SETTLED |
| C2-32 | `tests/api/health/test_health_probes.py:206-254` | **A test that passed for the wrong reason.** The new real-gateway readiness case reached Postgres, S3 and Weaviate **for real** when run in the same process as `tests/startup`. It now locates the served module **through the route** (`sys.modules[route.endpoint.__module__]`) and asserts the fake probe's **service-key fingerprint** | `main.py` imports the health router under the **flat** spelling (`from routers.health import router`); the test patched the **qualified** one (`flynapse_api.routers.health`) — two module objects, two `_aggregate_probe` globals, one green test. Found **only** because a fingerprint assertion had been added. **A patch that does not assert it took effect is indistinguishable from no patch** | The case exists because `request.app` was never exercised on the real gateway — the one process that `mount`s three sub-apps, and Starlette rebinds `scope["app"]` inside a mount, so a `/health/ready` under one would read a sub-app's empty state and report every degraded boot as clean | `::test_the_real_gateway_resolves_request_app_to_itself` (:206) | **yes** — H3 (drop the merge from the route → all three readiness cases fail, **the real-gateway one included**), `9812f44` | 1 | F2 | SETTLED |
| C2-33 | `flynapse_api/startup/weaviate_partitions.py:74` vs `copilot-mro-obsm/copilot_mro/app/main.py:136-161` | **Owed: M-WARN is split-brain across two processes.** Not fixed in C2 | The gateway refuses `warn` in a deployed environment and logs the error's own remediation; **copilot-mro's own `main.py` honours the identical variable with no environment gate at all and logs the `<database>` placeholder constant** — M-WARN clauses 1 and 3 are unimplemented there. Mounted under the gateway copilot-mro gets no lifespan, **but it runs standalone in the dev stack and in its own deployment.** The elegant fix moves the gate into `weaviate_boot_check.partition_boot_check_mode()` so both callers inherit it, leaving api's helper to shape the readiness entry only | Re-verified at assembly: `copilot_mro/app/main.py:140` is `if partition_check_mode != "warn": … raise`, with no `is_production` anywhere in the file, and `:161` logs `remediation=PARTITION_REFUSAL_REMEDIATION` — the placeholder constant. `error_type=type(error).__name__` is also still used there (D.8's scope) | **none** — api's guards are scoped to api's helper; nothing asserts the two callers agree | not recorded | 2 | F3 | OPEN |
| C2-34 | `flynapse_api/config/config.py:171-174` (`is_production` = `not self.debug`), consumed at `flynapse_api/startup/weaviate_partitions.py:74` | **Owed: `settings.is_production` is api's ONLY environment discriminator, and it is wrong in both directions.** Not fixed — **owner ruling owed (Q-ENVIRONMENT)** | A `DEBUG=false` local stack cannot use the mode the feature exists for; a dev tier running `DEBUG=true` makes `warn` selectable **and** returns 200 from `/health/ready` over a known isolation hole. Mitigating: `DEBUG=true` already disables `SecurityHeadersMiddleware` and flips the telemetry environment, so it is already a severe misconfiguration in a deployment. `iac` has a real `var.environment`; api's settings expose **no `ENVIRONMENT` field** and App Runner sets no `DEBUG`. Adding one is a **deployment-contract change**, correctly not made unilaterally | Re-verified at assembly: `is_production` resolves to `flynapse_api/config/config.py:172`, and it and the single consumer at `weaviate_partitions.py:74` are the **only two** non-test occurrences in the repo | **none** — and none is possible until the contract is ruled | not recorded | 2 | F3 | OPEN |
| C2-35 | `flynapse_api/main.py` (11 sites), `flynapse_api/routers/health.py:98` | **Owed: B-R1 stays open in this repo, explicitly.** 12 `error=traceback.format_exc()` sites still ship a rendered traceback whose last line is `Type: message` | The new guard (C2-14) is scoped to the **format-string** defect and will never see them — that scoping is deliberate (see C2-14's "Why"), and stating it here is what keeps it from being implicit. Owner-owed under §4c | Re-verified at assembly: `grep -c "error=traceback.format_exc()"` is **11** in `flynapse_api/main.py` and **1** in `flynapse_api/routers/health.py` — 12 repo-wide, exactly as the plan and the ledger both state | **none.** api has **no repo-wide R22 sweep** at all: only five test files in the repo reference `error_type` / `failure_fields` / R22, and none of them scans | not recorded | 2 | F3 | OPEN |

**C2 counts — 35 rows.** Tier: 0 = **0**, 1 = 20, 2 = 15. Chunk: F1 = 14, F2 = 17, F3 = 4.
Claim state: **SETTLED = 22 · ASSERTED = 4 · OPEN = 9.**

SETTLED: C2-4, C2-5, C2-7, C2-8, C2-9, C2-11, C2-13, C2-14, C2-15, C2-16, C2-17, C2-18, C2-19, C2-20,
C2-21, C2-22, C2-23, C2-27, C2-28, C2-29, C2-31, C2-32.
ASSERTED: C2-3, C2-12, C2-26, C2-30. OPEN: C2-1, C2-2, C2-6, C2-10, C2-24, C2-25, C2-33, C2-34, C2-35.

---

## Open claims, tier 2 first

### Tier 2, OPEN — no guard, judgment only

| id | Claim | What an auditor has to settle |
|---|---|---|
| **C2-33** | **M-WARN is split-brain across two processes.** The gateway refuses `warn` in a deployed environment; `copilot-mro`'s own `main.py` honours the same variable with no environment gate and logs the `<database>` placeholder. Clauses 1 and 3 are unimplemented there, and copilot-mro runs standalone in the dev stack and in its own deployment | Is it acceptable to ship a tenant-isolation gate whose enforcement depends on **which process read the variable** — while the fix (moving the gate into `partition_boot_check_mode()` so both callers inherit it) is deferred to Phase G? |
| **C2-34** | **`settings.is_production` (`not DEBUG`) is api's only environment discriminator**, and it is wrong in both directions: a `DEBUG=false` local stack cannot use the mode the feature exists for, and a dev tier on `DEBUG=true` makes `warn` selectable *and* returns 200 from `/health/ready` over a known isolation hole. api exposes no `ENVIRONMENT` field; App Runner sets no `DEBUG`; `iac` has a real `var.environment` | Is `DEBUG` an acceptable production discriminator for a **tenant-isolation** gate given that it is already load-bearing for `SecurityHeadersMiddleware` and the telemetry environment — or must an `ENVIRONMENT` field land first? **This is a deployment-contract change and an owner ruling is owed (Q-ENVIRONMENT).** |
| **C2-35** | **B-R1 is open in api**: 12 `error=traceback.format_exc()` sites (11 in `main.py`, 1 in `routers/health.py`) still ship a rendered traceback ending in `Type: message`, and the repo has no R22 sweep that would ever see them. The new guard's scoping to the format-string defect is deliberate | Is "this phase fixed three sites and declared the other twelve out of scope" the right boundary, or does a gateway carrying twelve known exception-text sinks make the three fixes cosmetic until B-R1 lands estate-wide? |

### Tier 1, OPEN

| id | Claim |
|---|---|
| **C2-1** | The merge itself rests on the lane and on the §2.2a register; no test asserts anything about the merge topology. **No textual-conflict count is recorded for api** — the two both-sides files are registered as *silent* merges. |
| **C2-2** | **M-LOCK.** `poetry check --lock` failing on theirs and passing on ours was run by hand and recorded in prose. The repo has **no lock-consistency test**, so nothing prevents a future merge re-splicing the lock the same way. |
| **C2-6** | `startup/__init__.py` adds a bare top-level module name `startup` to the gateway's import path. The collision check was done by hand and never encoded; nothing fails if a sibling repo ships its own top-level `startup`. |
| **C2-10** | §3's "five symbols" is corrected to four. Verified at assembly, but a plan correction carries no guard, and the next premise about another repo's diff will be just as unverified. |
| **C2-24** | `.env.example`'s documentation of `WEAVIATE_PARTITION_BOOT_CHECK_MODE` — including the `DEBUG=false` caveat the review asked for — is unguarded. **Zero test files in the repo reference `.env.example`.** |
| **C2-25** | C1.5's first clause at api's site (`WEAVIATE_GRPC_PORT` / `_SECURE` in `.env.example`) is unguarded, and it **contradicts `claims-C1-utils-B1-core.md`'s C1-15**, which records a same-day re-verification of "0 hits in `api-obsm`". |

### ASSERTED claims worth reading anyway

- **C2-26** — the §5.6 deviation. A guard exists on both sides of the decision (the wrapper allowance and
  the all-modules sweep), but **no mutation restores the strict rule to show what it would break.** The
  1-offender-under-strict / 0-under-relaxed measurement is the entire evidence, and it was taken against a
  tree (`copilot-mro-obsm`) that had itself only just merged.
- **C2-12** — `main.py`'s global exception handler was fixed, but no mutation reverted that specific line.
  Its settledness is inherited from M5 (the auth site) plus F3b (proof the scan reaches `main.py`).
- **C2-3** — the both-sides `main.py` auto-merge: the *arguments* to the partition check are guarded but
  not mutation-checked; only the *assignment* is (W4).
- **C2-30** — the cross-module limitation is pinned by a test asserting the guard does **not** catch it.
  That is the honest shape, but it is a test that passes by the guard being weak, so it cannot be
  mutation-proved in the usual direction.

---

## Owed out of this phase

1. **M-WARN's split brain** (C2-33) — **Phase G item**, already recorded in the plan and the ledger. Move
   the deployed-environment gate into `weaviate_boot_check.partition_boot_check_mode()` so both callers
   inherit it, leaving api's helper to shape the readiness entry only.
2. **Q-ENVIRONMENT** (C2-34) — **owner ruling owed**, batched with the queued four. api needs a real
   environment discriminator or an explicit ruling that `DEBUG` is one.
3. **B-R1's twelve remaining sites** (C2-35) — owner-owed under §4c; stated in the plan and the ledger so
   it is not implicit. api additionally has **no** repo-wide R22 sweep to inherit when the ruling lands.
4. **C1.5's remaining sites** — C2 closed api's (`ee6f4d0`). `claims-C1-utils-B1-core.md` C1-15 lists nine
   owed sites; that row's sweep needs re-running now that one is closed.

### Assembly notes — things the source documents assert that could not be corroborated

- **The "18 mutation proofs" count does not reconcile.** Plan §7 says "**18 mutation proofs**, each
  restored from a scratchpad copy rather than by `git checkout --`". The commit messages record **24
  distinct labels**: M1–M5 (`55439b4`), W1–W7 (`e77c730`), G1 (`62b76e1`), H1 (`ab81a48`), G2+G3
  (`6bb1b11`, applied as one run), and G4, G5, F1, F2, F3b, P1, H2, H3 (`9812f44`). Counting G2+G3 as one
  run gives 23. No grouping produces 18. The label series also has **`F3b` with no `F3`**.
- **`test_logging_context_middleware.py` was "kept, with the assertion it lacked added"** (plan §7) — in
  the diff their single `test_exception_logging_preserves_errors_with_mapping_text` is **removed** and
  three new cases over a captured-record fixture replace it. The substance of the claim holds; the word
  "kept" understates the rewrite.
- **The lane figures (1170/4/0/0 vs 1134/4) and pyflakes 51 → 51 were not re-run** at assembly, per the
  read-only brief. They are recorded identically in plan §7 and in the ledger.
- **`claims-C1-utils-B1-core.md`'s C1-15 is stale**: it records "0 hits in `api-obsm`" as re-verified at
  assembly on 2026-09-20, but `ee6f4d0` had already added `WEAVIATE_GRPC_PORT` to
  `api-obsm/.env.example:186`.
