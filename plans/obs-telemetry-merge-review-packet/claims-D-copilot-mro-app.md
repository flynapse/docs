# Claims — Phase D, copilot-mro application half

**Scope.** Worktree `/home/aditya/Code/copilot-mro-obsm`, branch `obs-merge`. Range `e26be7dd..6dc3160e`,
14 commits on this branch (the ledger's "16" counts from `417df303`): `e26be7dd` (merge, conflict resolution only, per §2.3), then `36467f29`, `12b899ed`, `248590dd`,
`04ad6284`, `2879a1cf`, `186d78cf`, `e3e31e14`, `fb09ca63`, `b112f860`, `e09f7ac0`, `bfe7d91a`, `bb7cbe35`,
`6dc3160e`. Everything from `2e965ea0` on is Phase E and is NOT covered here. Merge parents: ours
`417df303` (langgraph-merge), theirs `c2fc8bb1` (obs-telemetry-merge).

**The D.13 gate, as recorded** (plan §7 "D.13 — the gate, measured", corrected figure; the earlier
"2 new ids" reading was taken before `tests/unit` finished on both trees and is superseded):

| | |
|---|---|
| pre-merge failing ids | 32 |
| merged failing ids | 41 |
| new on the merged tree | 20 |

Disposition of the 20: 4 real defects fixed in-phase (`e3e31e14`, `e09f7ac0`); 10 import pollution from one
merge-added file (`6dc3160e`); 1 ERROR id a FAILED-only filter never counted (`bfe7d91a`); 2 Phase E (E.0a
Grafana datasource, E.0d dark-note vocabulary); 2 owner-blocked (the SHA-pinned branch-hygiene guard).
Lane numbers at close: `agent_sdk/core` 1371 passed, `unit/agent_shared` 1103 passed, `unit/metering` 83,
`unit/document_hub` 499, `api/observability` 10, `e2e` 594 passed 1 skipped (pre-merge baseline exactly),
`unit/infra` 58 passed, `unit/observability` 2 failed 179 passed (both the blocked guard).

**Conventions used below.** Every `File:line` was resolved in the worktree as it stands today. A guard is
written only where the test file AND the test name resolve. Mutation proofs name their source: `yes (plan)`
= plan or SDD ledger records the mutation and its result; `yes (<sha>)` = the in-range commit message
records the specific mutation and its result; `natural (<sha>)` = the guard was observed RED on the merged
tree before the fix, which is the same evidence; `not recorded` otherwise. Tier 0 is claimed nowhere in
this slice: no guard here has both a recorded mutation and the plan's tier-0 treatment, so every row is
tier 1 or 2 by the "not SETTLED implies tier >= 1" rule, and the SETTLED rows are left at their decision
tier rather than promoted.

**Anchoring caveat.** The worktree's HEAD is `ea0ac559` (Phase E), so every line number was resolved
against a tree that already carries Phase E's edits. Where a Phase-D-era value differs from what HEAD shows,
the row says so (row 8 is the only one).

**Row counts.** 45 rows. Tier 2: 19 (8 SETTLED, 4 ASSERTED, 7 OPEN). Tier 1: 26 (4 SETTLED, 11 ASSERTED,
11 OPEN). Tier 0: none. By chunk: F1 19, F2 13, F3 13.

---

## Claims

| # | File:line | Decision taken | Why | Evidence it is right | Guard test | Mutation-proved? | Tier | Chunk | Claim state |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `copilot_mro/app/config.py:1091` | `llm_content_capture_enabled` defaults **True** (theirs: False) | M-CAPTURE / spec ruling 4: capture on, tenant opts out | Composed with core's opt-OUT column, theirs retained tool I/O nowhere, and M-TOOLIO-2 had been accepted against a store that never ran (plan §7) | `tests/config/settings/test_llm_content_capture_settings.py:9` `test_llm_content_capture_defaults_on_with_thirty_day_retention` | not recorded | 2 | F1 | ASSERTED |
| 2 | `copilot_mro/app/services/agent_shared/llm_content_capture_policy.py:25` | `require_tenant_opt_in` setting AND its policy-reader argument deleted; the tenant row is the only per-tenant gate | One legal value is not a setting; a name that reads as a bypass invites use as one | Policy signature carries no such arg; fail-closed on blank/unknown/failed read | `tests/config/settings/test_llm_content_capture_settings.py:80` `test_no_setting_can_waive_the_tenant_row` | not recorded | 2 | F1 | ASSERTED |
| 3 | `copilot_mro/app/services/agent_shared/pipeline.py:409-416` | D.5 — tenant policy read hoisted ABOVE the accumulator, not at persist time | An opted-out tenant must never have content materialised or redacted at all | Same single indexed lookup, moved earlier; opted-out tenant accumulates nothing all turn | `tests/unit/agent_shared/test_llm_content_capture_lifecycle.py:84` `test_the_policy_is_read_before_the_snapshot_is_built` (+ :42, :58, :70) | yes (plan) — deleting the read fails 3 tests; the first version of this assertion was itself INERT and was rewritten | 2 | F1 | SETTLED |
| 4 | `copilot_mro/app/services/agent_shared/pipeline.py:752-766`, `.../llm_content_capture_tasks.py:33` | D.6 — capture leaves the request critical path; task HELD by a registry, never awaited by the turn | A slow Postgres was withholding an answer already generated (RV1 P0) | Same shape as `chat_block_saves`, deliberately a separate registry | `tests/unit/agent_shared/test_llm_content_capture_lifecycle.py:159` `test_the_turn_does_not_await_the_capture`, `:106`, `:135` | yes (248590dd) — restoring `await self._capture_llm_turn_content(...)` fails the D.6 guard; dropping `drain_captures()` fails the drain guard | 2 | F1 | SETTLED |
| 5 | `copilot_mro/app/main.py:271-285` | The capture drain and the block-save drain run INSIDE `mro.lifecycle.shutdown`, before the success record | A failed drain must mark the span, and "MRO shutdown completed" must not precede the saves | Merge-invented defect: the drain had landed outside the span (plan §7, two defects the merge created) | `tests/unit/agent_shared/test_llm_content_capture_lifecycle.py:177` `test_the_shutdown_handler_drains_captures` — but it reads `main.py` FROM DISK | not recorded | 1 | F2 | ASSERTED |
| 6 | `copilot_mro/app/main.py:271` (the lifespan itself) | Accepted that copilot-mro's own lifespan never runs under the api gateway; the firing drain was added in `api-obsm` `4da716f` | Starlette does not run a mounted sub-app's lifespan | Reviewer finding, reproduced; the in-repo guard stayed green while the property was false | none in this repo (the disk-reading guard in row 5 cannot see it); the real guard is in `api-obsm`, outside this range | not recorded here | 2 | F1 | OPEN |
| 7 | `copilot_mro/app/services/agent_shared/telemetry.py:1377`, `:1416`, `:1845` | M-TOKENUSAGE — `gen_ai.client.token.usage` becomes a Histogram at both constructors and the record site, with the semconv bucket ladder | Semconv defines it as a histogram; a Counter exports `_total` and every semconv consumer reads nothing | SDK default boundaries top out at 10,000, below an ordinary prompt, so a bucket-less conversion is unreadable | `tests/unit/agent_shared/test_telemetry.py:296` `test_token_usage_is_a_histogram_and_the_counts_are_sums_not_counters` | yes (04ad6284) — reverting to `create_counter` + `_safe_add` fails it | 2 | F1 | SETTLED |
| 8 | `deployment/observability-local/grafana/provisioning/dashboards/flynapse/llm-agents.json`, `deployment/otel/dashboards/CATALOGUE.md`, `iac/dashboards/llm-agents.json.tftpl` | The `_total` board spellings were deliberately NOT updated in Phase D | Phase E rewrites the same files for M-GRAFANA; editing them twice invites one pass being lost | Stated in `04ad6284` and plan §7. Verified: at `6dc3160e` the board still queries `gen_ai_client_token_usage_total`; at worktree HEAD (`ea0ac559`, Phase E) it queries `gen_ai_client_token_usage_sum` — so the deferral was honoured and closed OUTSIDE this range | none in this range | not recorded | 2 | F1 | OPEN |
| 9 | `copilot_mro/app/db/llm_turn_content.py:131` | D.10 — an unscoped read returns `AND FALSE`; only `tenant_owner=True` sees the whole tenant | Forgetting to scope was more permissive than scoping badly — the inversion of fail-closed | The old branch returned no predicate for no-identity/no-capability while an identity without capability got `AND FALSE` | `tests/unit/metering/test_llm_turn_content_dao_outcomes.py:106` `test_a_read_that_scopes_itself_to_nothing_is_refused_not_widened` (+ :125, :135) | yes (plan, 2879a1cf) — restoring the permissive branch fails the new test | 2 | F1 | SETTLED |
| 10 | `tests/api/tenancy/test_cross_store_isolation.py:729` | The whole-application tenancy sweep gains the 12th router the merge mounts (`llm_observability`) | A mounted router outside the sweep makes the coverage claim false | Guard refused to keep claiming coverage; it was RED on the merged tree | `tests/api/tenancy/test_cross_store_isolation.py:729` `test_the_route_table_covers_every_router_the_deployment_mounts` | natural (e3e31e14) — red on the merged tree, green after | 2 | F1 | SETTLED |
| 11 | `tests/agent_sdk/core/test_agent_sdk_ordinary_log_privacy.py:452`, `:330`, `:246` | Their privacy guard was made honest and adopted as ours: ONE body, `**` splat resolved three ways, a dict mutated after its literal reported unresolvable, structural suffixes no longer exempt a bare content value, `message` in a BARE-ONLY class | A guard with two bodies proved nothing; it flagged 15 sites of the estate's own R22 form and could not see through any splat | Reviewer found the duplicate body; `b112f860` merged them, `bfe7d91a` closed `query_type=message` | `:542` `test_served_api_runtime_and_tool_ordinary_logs_do_not_emit_content` (+ `:562`, `:584`) | yes (12b899ed, b112f860, bfe7d91a) — planted `"query": query` caught at both call sites; planted `"first_query": queries[0]` NOT caught until plural spellings were added; 14 probes with controls | 2 | F1 | SETTLED |
| 12 | `copilot_mro/app/services/lang_agent/browser_transport.py:255`, `:275`, `:315`, `:345` | D.8 — four raw-exception sites routed through `failure_fields`; `BrowserSpawnError` gains `cause_type` | The spawn message embeds up to 2000 bytes of subprocess stderr | P0-GUARD's disposition: route the call sites, do not weaken the guard | `tests/agent_sdk/core/test_agent_sdk_ordinary_log_privacy.py:542` (surface sweep) | not recorded for these sites | 2 | F1 | ASSERTED |
| 13 | `.../tools/synthesis/cited_synthesis.py:115-116`, `.../synthesis/amos_synthesis_core.py:416-417`, `.../database/db_query.py:761`, `.../synthesis/document_source_answer.py:373` | D.11 — the bounded failure reason is restored as code + TYPE + FRAMES, never the message | Their sweep over-corrected to `reason_chars=len(message)`, re-creating the "nine failed turns, no why" defect; the message is built from `str(exc)` so it can quote the question | Four sites, not the three D.11's wording implied; the fourth found by the theirs-only audit (`bb7cbe35`) | none — the privacy guard's BARE-ONLY class compares `message` as a bare Name only, so a reverting `reason=message[:200]` would NOT be flagged | not recorded | 2 | F1 | OPEN |
| 14 | `copilot_mro/app/api/chat_management.py:365`, `:386` | `reason` becomes `reason_code` end to end in the log path; the METRIC dimension stays `reason` | A name is the only evidence of a closed set a static guard can read; renaming the counter label would break every query built on it | `b112f860`: the rename is not just guard-satisfaction, it moves the field out of value inspection | `tests/api/chat/test_rag_block_save_refusal.py`, `tests/api/chat/test_stream_final_persistence.py` (both updated in range) | natural (e3e31e14) — the rename left one assertion red in a stream-persistence file | 2 | F1 | SETTLED |
| 15 | `.env.sample:70-81` | Corrected to state capture is ON by default and opt-OUT, and that retention is stamped but its DELETE is scheduled nowhere | It had merged silently to theirs and documented the exact inverse of M-CAPTURE | `debug-rag/REFERENCE.md` was corrected in the same phase, which makes this an oversight not a decision | none | not recorded | 2 | F1 | OPEN |
| 16 | `copilot_mro/app/api/chat_management.py` (all hunks), `copilot_mro/app/api/user_feedback.py` (all hunks) | Resolution took **OURS** at every hunk | Theirs is weaker throughout AND leaks user content twice: into the client's 500 detail and over the SSE error frame | Plan §7 "The merge"; §2.3 base-of-record for user-content removal is theirs EXCEPT where ours already removes more | `tests/agent_sdk/core/test_agent_sdk_ordinary_log_privacy.py:542` surface sweep | not recorded | 2 | F1 | ASSERTED |
| 17 | `copilot_mro/app/services/agent_pipeline.py:53`, `:86`, `:92`, `:222`, `:256-281`, `:675`, `:695` | D.1 — their entire 59-line telemetry delta grafted WHOLE at all four sites (`get_runtime_telemetry`, `_model_usage_sinks`, `_claude_runtime_provider`, the sink splat, `post_tool_observer=`, the five `AgentPipeline` kwargs) | "Ours as base" never means "drop theirs": on our mainline `RuntimeTelemetry` had no production instantiation at all | Acceptance held: `get_runtime_telemetry` survives and their composition-root test passes; independently pinned by a silent merge of THEIR test asserting the exact `AgentPipeline` parameter list | `tests/unit/lang_agent/test_composition_root.py` | not recorded (the adversarial review reports it could not break it) | 1 | F2 | ASSERTED |
| 18 | `copilot_mro/app/services/agent_shared/pipeline.py:22`, `:552`, `:787` | Resolution took **THEIRS** as base (their 341-line delta is the turn telemetry), with our `failure_fields(exc)` restored at the execute-failed site plus the import theirs lacked | §2.3: base of record for the R22-swept sites is ours, type + frames | Both restored sites carry `**failure_fields(exc)` today | `tests/unit/agent_shared/test_pipeline_lifecycle.py:525` `test_pipeline_failure_log_does_not_render_prompt_or_exception_text` | not recorded | 1 | F2 | ASSERTED |
| 19 | `copilot_mro/app/services/improvement/scheduler.py:57`, `:406`, `:432` | Resolution took **OURS** with their `operation_outcome` dimensions grafted in; `STOP_UNWIND_SECONDS` preserved | Our messages are asserted verbatim by an ours-only test, and `api` imports the constant — a "take theirs" resolution breaks the api repo | `api-obsm/flynapse_api/shutdown_budget.py:40` imports it today | none in-repo for the constant; the cross-repo import is the proof | not recorded | 1 | F2 | ASSERTED |
| 20 | `copilot_mro/app/main.py` (route table), guard at `tests/unit/observability/test_nonagent_lifecycle_spans.py:322` | Accepted THEIR silent deletion of the `/metrics` route | The estate is push-only over OTLP; the api gateway removed its own copy earlier | §2.2a register calls this "a good silent change" | `tests/unit/observability/test_nonagent_lifecycle_spans.py:322` `test_mro_subapp_has_no_direct_metrics_route` | not recorded | 1 | F2 | ASSERTED |
| 21 | `pyproject.toml:91` (`prometheus-client`), `README.md:122` (`GET /metrics`) | Left as-is: an orphaned dependency and a stale README line | Consequence of row 20, recorded rather than fixed | Plan "Deferred from Phase D" | none | not recorded | 1 | F3 | OPEN |
| 22 | `copilot_mro/app/main.py:280` | D.3 — the undefined `traceback` was fixed as `**failure_fields(exc)`, NOT by restoring the import | `format_exc()` ends in a `Type: message` line, so restoring it re-introduces the leak the sweep existed to remove | Merge-created defect: their sweep deleted the import, ours was the only remaining caller; `py_compile` passes, only pyflakes sees it | none — the stated gate was a pyflakes run, not a test | not recorded | 1 | F2 | OPEN |
| 23 | `copilot_mro/app/services/agent_claude/orchestrator.py:2311` | Accepted THEIR un-wiring of the tool-I/O archive (`archive=None`) | M-TOOLIO-2 supersedes M-TOOLIO: capture is the single store, one write site, one retention clock | §4d-resolved; restoring a default for a feature with no call site restores nothing | `tests/agent_sdk/core/test_agent_sdk_claude_content_feeders.py` asserts `archive is None` even when the flag is True | not recorded | 1 | F2 | ASSERTED |
| 24 | `copilot_mro/app/config.py:82` | `tool_io_archive_enabled` is still DECLARED after M-TOOLIO-2 | Load-bearing for the one good test in row 23; `tests/e2e/run_explain_latency_e2e.py` still prints it as a diagnostic that can now only say False | Plan "Phase D adversarial review — recorded, not fixed" | none | not recorded | 1 | F3 | OPEN |
| 25 | `tests/e2e/_toollog_index.py:107`, `:127`, `:306-311` | D.2 — the E2E harness join is repointed from the archive to `llm_turn_content.content -> 'tools' -> 'io'`, joined on `tool_use_id` | The join read `settings.tool_io_archive_enabled`, whose default theirs flipped, so 44 argument-level checks across four suites degraded to SKIP against a zero baseline | Found by RUNNING the harness, not by reading the diff; `TurnContentAccumulator.tool_io_records()` added so the fixture reads the real retained list | `tests/e2e/harness/test_toollog_archive_reader.py:163` `test_a_store_failure_is_unavailable_not_empty` | yes (2879a1cf) — swallowing a store outage into an empty record set fails that test | 1 | F2 | SETTLED |
| 26 | `tests/e2e/_toollog_index.py:198-210` | Accepted that the repoint NARROWS the blindness without closing it: the reader checks the deployment switch only | A fixture tenant with no `tenants` row still fails closed at the per-tenant gate and degrades to the same SKIP, with a better reason string | Plan "recorded, not fixed" | none | not recorded | 1 | F3 | OPEN |
| 27 | `copilot_mro/app/services/agent_shared/tools/analytics/forecast.py:435` | Their `forecast_points=len(result.forecast or [])` replaced with `has_forecast=result.forecast is not None` | `result.forecast` is a SCALAR, so `len()` raised on every successful forecast and the tool returned `forecast_error` | A defect THEIR branch shipped, found by our tests | none found in `tests/` for `has_forecast` — CLAIMED BUT NOT FOUND as a named guard | not recorded | 1 | F2 | OPEN |
| 28 | `copilot_mro/app/services/parsers/*.py` (8 sites, e.g. `mel_parser.py:1540`, `amos_parser.py:4658`), `s3_pdf_processor.py:926` | Resolution took **THEIRS**: every parser entrypoint now logs as `setup_logging(name="ingest-parser")` | — the §5.4 P1 disposition said "Live merge conflict — resolve OURS" and Phase D's notes never mention parsers | Contradicted in the tree: ours had per-parser names (`mel-parser`, `amos-parser`); `distribution=` survives only at `main.py:12`. Which parser ran is now recoverable only from the `parser.kind` span attribute, not from logs | none | not recorded | 2 | F1 | OPEN |
| 29 | `copilot_mro/app/services/agent_pipeline.py:69-76` | D.12 — legacy-span suppression is gated on the bootstrap's own state, not on a `try/except` | `from_current_provider` never raises without an SDK, so the facade was always constructible and `suppress_legacy_agent_sdk_spans` was unconditionally true — a collector-less process lost its legacy span and emitted nothing in its place | Confirmed by code-read first, then by test | `tests/unit/lang_agent/test_runtime_telemetry_provider_gate.py:67` `test_the_gate_is_not_the_try_except`, `:116` `test_a_process_without_otel_keeps_its_legacy_span` | yes (04ad6284) — reverting to the bare `try/except` fails 4 of the 5 new tests | 2 | F1 | SETTLED |
| 30 | `copilot_mro/app/services/agent_pipeline.py:45-83` | The FACADE is cached, the GATE is re-read each call | D.12 had put the gate inside an existing `@lru_cache(maxsize=1)`, so a late-bootstrapping process would pin its first `None` for the process lifetime — worse than the always-on behaviour it replaced | Caught in self-review before the adversarial pass; `get_runtime_telemetry.cache_clear` still resolves | `tests/unit/lang_agent/test_runtime_telemetry_provider_gate.py:85` `test_a_late_bootstrap_is_not_pinned_to_none_for_the_process_lifetime` | yes (fb09ca63) — putting `@lru_cache` back on the outer function fails the new test | 1 | F3 | SETTLED |
| 31 | `copilot_mro/app/services/lang_agent/composition` fixture at `tests/unit/lang_agent/test_composition_root.py:41-57` | The fixture clears the third `@lru_cache` the merge added, asymmetrically: SETUP asserts each of the three still has `cache_clear`, TEARDOWN tolerates its absence | A value cached by one test was deciding the telemetry wiring of every test after it; one of their tests monkeypatches the symbol with a bare lambda, so a symmetric teardown raised `AttributeError` | This is the "1 ERROR id" the FAILED-only filter never counted | `tests/unit/lang_agent/test_composition_root.py:41` (the fixture's own setup assertion) | not recorded | 1 | F3 | ASSERTED |
| 32 | `copilot_mro/app/services/improvement/scheduler.py:245-264` | `_error_kinds` logs `unstructured` for an entry with no colon instead of passing the whole entry through | `where` is `[^:]+`, which on a colonless entry matches the entire string, so a bare runner line went verbatim into the warning | OUR code, caught by THEIR test, red on the merged tree | `tests/unit/improvement/test_improvement_operation_telemetry.py:216` `test_scheduler_logs_partial_reports_without_error_bodies` | natural (e09f7ac0) — red on the merged tree, green after | 1 | F3 | SETTLED |
| 33 | `copilot_mro/app/api/flightops_brief.py:476`, `:517`, `:588` | `_log_detail(exc)` restored at three sites their sweep had replaced with `error_type=type(exc).__name__` | §2.3: `error_type` alone is strictly weaker and must not replace a purpose-built sanitiser; `_log_detail` yields host and status with the API-key query string scrubbed | The site is THEIRS-ONLY, so it never entered the §2.2a intersection register — the method gap that motivated the theirs-only audit | `tests/unit/flightops/test_brief_endpoint_logic.py:631` `test_the_log_line_is_written_at_the_call_site` (its docstring predicted this exact regression) | natural (e09f7ac0) — red on the merged tree, green after | 1 | F3 | SETTLED |
| 34 | `tests/unit/observability/test_nonagent_lifecycle_spans.py:15-30` | `import copilot_mro.app.main` moved from module scope into a module-scoped fixture | At module scope it runs during COLLECTION and lands the real `copilot_mro.app.services.*` in `sys.modules`, defeating the dynamic-loader convention for everything collected after it — 10 failures in four other directories | Proved import-time not state: deselecting every test in the file still broke them | none enforces the convention — recorded as belonging in `tests/unit/infra` and not written | not recorded | 1 | F3 | OPEN |
| 35 | `tests/unit/infra/test_no_depth_coupled_paths.py` + 6 fixed files (incl. `tests/unit/observability/test_phase1c_nonagent_scope_guard.py:88`) | D.7 — every `parents[N]` site replaced with `repo_root(...)` per `tests/_root.py` | Their new tests violated the repo's own depth-coupling rule in nine files | `tests/unit/infra` 58 passed at close, the depth guard now green | `tests/unit/infra/test_no_depth_coupled_paths.py` | not recorded | 1 | F2 | ASSERTED |
| 36 | `tests/unit/observability/test_phase1c_nonagent_scope_guard.py:14-17` | D.7's third clause NOT done: the file survives, ARMED and RED, naming ~100 legitimately-changed paths | Deletion was refused by an automated security-test classifier — the right rule, the wrong outcome here | Branch-scoped change-control guard: four hard-coded revisions (`ac680bf2`, `1169af0f`, `9f74a11`, `395a3b5`) and a `obs-telemetry-merge` branch-name disarm at `:110`; our branch is `obs-merge`. It cannot be repaired: widening the allow-list makes it assert that the files that changed are the files that changed, always-skipping makes it inert, and a branch-name check is decoy-defeatable | n/a — this IS the test; it accounts for 2 of the 41 merged failing ids | not applicable | 1 | F3 | OPEN, owner-owed |
| 37 | `deployment/observability-acceptance/**`, `tests/integration/observability_acceptance/**` | M-ACCEPT — dropped entirely (~4,600 lines) | Pinned to a branch name that does not exist here, a macOS temp root, four fixed ports, a fixed docker network name; a second undeclared Grafana with auth disabled | Both paths are absent from the tree today | none (absence) | not recorded | 1 | F2 | ASSERTED |
| 38 | `deployment/docker-compose.yml:163`, `deployment/observability-local/observe-docker-compose.yml:85`, `deployment/otel/VERSIONS.md` | M-PINS — grafana stays `13.2.1` at both compose sites; the "Local-only exception" section and its changelog row removed | Their stated root cause (`plugin.notRegistered` on 13.2.1) was wrong: the real cause was a uid drift in the persisted `grafana.db` that crash-looped Grafana while it exited 0 | E.1 pulled FORWARD into D because the restored guard fails while the floating tag is in the tree | `tests/integration/otel/test_compose_image_pins.py:107` `test_observability_images_match_the_pins`, `:129` `test_compose_pins_agree_with_versions_md` | not recorded | 1 | F2 | ASSERTED |
| 39 | `poetry.lock` (resolved inside `e26be7dd`) | Relocked from a resolved `pyproject.toml`, never hand-merged: 0 packages removed, 12 added, 1 version move | §2.3 doctrine | Plan §7 records the figures | `poetry check --lock` — not recorded as run in-range | not recorded | 1 | F2 | ASSERTED |
| 40 | `copilot_mro/app/services/agent_shared/llm_content_capture_tasks.py:30` | `_PENDING` has no cap | With capture on by default and off the request path, a wedged Postgres accumulates one hanging task per turn with no backpressure; the awaited version at least throttled. `chat_block_saves` has the same shape, so this is precedent-consistent | Plan "recorded, not fixed" | none | not recorded | 1 | F3 | OPEN |
| 41 | `copilot_mro/app/api/llm_observability.py:191` | Left as `error_type=type(exc).__name__` — the §2.3 "strictly weaker" form — in a file the merge brought in | Part of the estate-wide B-R1 backlog (~60 sites), not a new defect; neither sweep reached it | Plan "recorded, not fixed" | none | not recorded | 1 | F3 | OPEN |
| 42 | `scripts/purge_llm_turn_content.py` | Capture is ON for every tenant and NOTHING schedules the purge | Core's column is `DEFAULT true NOT NULL` and Postgres backfills it onto every existing tenant; `expires_at` is stamped per row but the DELETE has no scheduler, automation row, cron, compose or iac reference | The stated 30-day retention is a claim nothing implements, on a store now holding prompts, model answers and tool I/O for every tenant. M-CAPTURE makes the Appendix A contract clause a go-live prerequisite | none | not recorded | 2 | F1 | OPEN, owner-owed |
| 43 | `copilot_mro/app/services/agent_evaluation/**`, `pyproject.toml` `evaluation` group | M-EVALS' "the rest does not merge" never happened; the whole Phoenix suite merged and no phase owns its rejection | E.1's reject list is M-ACCEPT and M-PINS only; G.8 is a BUILD item, not a rejection | M-RESIDENCY is unenforced: a grep for a provider allowlist across the runner and the CLI returns nothing, and the ruling says no eval run touches real traces until it is. The group is `optional = true`, so `poetry install` is unaffected | `tests/unit/observability/test_phoenix_evaluation_dependencies.py` exists but guards dependencies, not the residency allowlist | not recorded | 2 | F1 | OPEN, owner-owed |
| 44 | `copilot_mro/app/services/agent_shared/model_gateway.py:106` (`_error_snapshot`) | Deliberately KEPT storing the exception message | The destination is the governed capture store, which already retains the raw prompts beside it, and it is double-gated | Recorded so a future R22 sweep does not "fix" it into uselessness | none | not recorded | 1 | F3 | OPEN |
| 45 | `tests/architecture/agent_runtime/test_langchain_ambient_surface_policy.py:94` | Left RED, untouched | It scans for `deployment/observability-local/otel-collector-config.yaml`, a path present in none of base, ours or theirs — pre-existing on our mainline, in no phase's lane | Named now so a later fast-forward does not mistake it for merge damage | n/a — this IS the failing test | not applicable | 1 | F3 | OPEN |

---

## Open claims, tier 2 first

**Tier 2, OPEN** (the estate-shaping ones — an auditor should start here):

1. **Row 42 — capture is ON for every tenant and nothing purges it.** Owner-owed. The "30-day retention" is unimplemented on a store holding prompts, answers and tool I/O.
2. **Row 43 — M-EVALS was never applied and M-RESIDENCY is unenforced.** The whole Phoenix eval suite merged; no phase owns the rejection; no provider allowlist exists.
3. **Row 28 — the parser service-name resolution contradicts its own disposition.** §5.4 says resolve OURS; the tree carries theirs (`ingest-parser` at 8 sites plus `s3_pdf_processor`). Signal-contract consequence: logs can no longer say which parser ran.
4. **Row 13 — D.11's restored failure reason has no guard that would catch a revert.** The privacy guard's BARE-ONLY class sees `message` only as a bare Name, so `reason=message[:200]` would pass.
5. **Row 6 — the capture drain only fires through `api-obsm`.** copilot-mro's own lifespan is never run for a mounted sub-app, and the in-repo guard reads the file from disk, so it cannot see this.
6. **Row 8 — the `_total` board spellings were left inconsistent with the new Histogram** pending Phase E. Confirmed deliberate and confirmed closed by Phase E at worktree HEAD, i.e. outside this range.
7. **Row 15 — `.env.sample` has no guard** tying it to `config.py`; it had already merged silently to the inverse of M-CAPTURE once.

**Tier 1, OPEN:** rows 21 (orphaned `prometheus-client` + stale README), 22 (the D.3 shutdown fix is gated by pyflakes, not a test), 24 (`tool_io_archive_enabled` still declared), 26 (D.2 narrows but does not close the harness blindness), 27 (the `forecast.py` fix has no named guard), 34 (nothing enforces the dynamic-loader convention), 36 (the blocked branch-hygiene guard, owner-owed), 40 (`_PENDING` uncapped), 41 (`llm_observability.py:191` weak `error_type`), 44 (`_error_snapshot` keeps the message, deliberately), 45 (pre-existing RED architecture test).

---

## Deferred, with the reason recorded

From the plan's "Deferred from Phase D" and "recorded, not fixed" registers. None of these is a defect
introduced by this range; each is a decision to not act, with its stated reason.

- **`record_subagent` has no production call site.** `RuntimeTelemetry.record_subagent` (`telemetry.py:1899`) and its two instruments are dead — the only caller in the tree is `tests/unit/agent_shared/test_telemetry.py:232`. `lang_agent/backend.py:948` `_record_subagent_runs` is agent-state, not telemetry. The `fn-llm-agents` subagent panel is permanently empty. Reason: this is the plan's owed "subagent call sites" work, genuinely unstarted; the merge did not touch it. `tests/integration/otel/_emitted_series.py:116` already documents it as PENDING.
- **Branch attribution lost on the canonical tool path.** `record_tool_operation` sets neither `gen_ai.tool.branch_id` nor `gen_ai.tool.ref`, which only the LEGACY `emit_tool_spans` path sets. After D.12, suppression is on whenever OTel is configured, so a branch-leaf tool span is indistinguishable from a parent one. Reason: out of Phase D's merge scope.
- **Dispatcher-projected standalone tools are outside the observer.** `post_tool_observer` is threaded into exactly one builder (`build_claude_pilot_tool_runtime`); the ~12 `build_standalone_*` projections take none. Reason: consistent with their design, but our own P6 work moved tools OUT of that set — the seam is the coverage hole, and closing it is not a merge decision.
- **A failed tool can leave a step event with no tool-I/O record.** On the unauthorized branch-leaf leg and on a raising parent dispatch, `lang_agent/nodes.py` emits the failed step but `continue`s past both `_record_tool_io` and `dump_tool_call`. Reason: each side is internally consistent; the union is not, and reconciling it is not a merge resolution.
- **D.6's saving is smaller than the commit claims.** `accumulator.snapshot` — the redaction and serialisation walk, the bulk of the cost — is still awaited on the settle path; what moved off it is one indexed INSERT. There is no S3 put to move (`content_s3_key` is always None), so `purge_llm_turn_content.py`'s `RETURNING content_s3_key` is dead. Reason: recorded as an overstatement in the module docstring and the D.6 commit, not corrected in-phase.
- **The late-bootstrap fix restores the HELPER, not the composition.** `get_agent_pipeline` is still `@lru_cache(maxsize=1)` and freezes `runtime_telemetry` — and with it `post_tool_observer` and `suppress_legacy_agent_sdk_spans` — into the composed objects at first use. A process that composes before it bootstraps is still permanently untraced. Reason: the new test's docstring overstates what it proves; the composition-level fix was not attempted.
- **`BrowserSpawnError`'s stderr tail no longer reaches any log.** D.8 stopped logging the message because it embeds up to 2000 bytes of subprocess stderr. Reason: the tail stays on the exception for the caller; if spawn failures prove hard to diagnose the right fix is a bounded sanitised field, not restoring the message.
- **D.9 (their read API: docstring, RBAC intent, wire-or-hold) deferred by its own terms.** The API exists and is RBAC-correct after D.10; the dashboard consumer is Phase B2's question, so wire-or-hold belongs where the consumer is. Reason: no speculative backend ahead of a consumer.
- **C1.5's first clause (`WEAVIATE_GRPC_PORT` into the copilot-mro env and compose files) still owed** — 0 hits across all six repos. Reason: assigned to Phase E.
- **Three silent-merge reversals found in Phase D were handed to Phase E, not fixed here:** `datasources.yml` lost `flynapse-postgres` and gained `deleteDatasources` (E.0a); `provisioning/dashboards/flynapse/llm-agents.json` lost both exact-spend panels and moved five queries to the Counter spelling; `oss-profile.md` lost the readonly-role rotation row, the board pointer and the "expectedly unhealthy" note. All three are deployment-half files and belong to the Phase E packet. Recorded here because Phase D is where they were discovered, and because two of them are the 2 Phase E items still counted in the D.13 gate.
